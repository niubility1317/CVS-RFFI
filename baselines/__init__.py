"""Paper reproduction baselines for CVS-RFFI.

The package is intentionally isolated from the historical `type*` experiment
folders so paper baselines can be compared without disturbing existing runs.
"""
