# D92-E0D-5arm-Hard12v2实现计划

> 执行方式：继续使用当前隔离Git工作树；每个功能先写失败测试，再做最小实现。科学核心与机械runner分文件并行，primary负责集成和最终裁决。

## Task 1：registered-state D模式

文件：

- 修改`code/scripts/probe_d92_registration_balanced_covariance.py`
- 修改`tests/test_probe_d92_registration_balanced_covariance.py`

实现：

- 新增冻结`registered_d_mode`：`fusion_loo/full_only/block_only/fixed50`；
- 仅在registered且K>2、E关闭时切换；其他状态精确走D92_FULL；
- FULL/BLOCK只调用一个主组件；FIXED50只调用两个主组件并复用D44 RMS固定融合；
- 审计mode、组件活动、LOO fold、actual component calls和解析inventory；
- 成功/异常恢复所有全局patch。

验证：mode输出、K1/K2 alias、fit数2/2/4/24/44、有限性、类置换等变。

## Task 2：五臂方法包装与truth-free evaluator

文件：

- 新建`code/cvsrffi/stage2_d92_e0d_slim.py`
- 新建`code/cvsrffi/stage2_d92_e0d_query_evaluation.py`
- 新建`code/scripts/run_d92_e0d_prediction.py`
- 新建对应测试。

实现：锁死五臂、A/B/C/E/F、资源receipt、query协议审计和全局恢复。CLI不得接收truth、role、quota或score参数。

## Task 3：Hard12-v2与method lock

文件：

- 新建`configs/stage2_d92_e0d_5arm_hard12v2_v1.json`
- 新建`code/cvsrffi/stage2_d92_e0d_hard12v2.py`
- 新建对应测试。

实现：完整canonical payload、selection SHA、v1零交集、12outer覆盖、真实原D92 K5 package定位、五臂轮换和60job×3scene闭合。

## Task 4：隔离runner

文件：

- 新建`code/scripts/run_d92_e0d_hard12v2.py`
- 复用独立`score_d92_be_prediction.py`
- 新建runner测试。

实现：prepare/smoke/run-shard，prediction/scorer进程隔离，输出不可覆盖，job receipt绑定，8 shard，重复pre-prediction异常指纹技术停止。

## Task 5：严格分析器

文件：

- 新建`code/cvsrffi/stage2_d92_e0d_analysis.py`
- 新建`code/scripts/summarize_d92_e0d_hard12v2.py`
- 新建对应测试。

实现：FULL_ONLY分别对E0_FUSION和D92_FULL配对，执行全部冻结性能/资源/协议门；解释臂仅报告，不可晋级。

## Task 6：发布准备

- 在`ssr-gpu`串行运行聚焦测试、`py_compile`和真实运行闭包import测试；
- 建立新run ID、method lock、Git-backed预注册报告与根目录镜像；
- 独立Terra/max P0/P1审查必须为0；
- 只同步一个完整Git运行闭包、method lock和launch，核对最小身份；
- 由唯一Luna/max runner执行N607预检、smoke、60job、监控和取回；无fresh-run retry。

## Task 7：裁决

- 只在60/60job与8/8 shard完整后分析；
- 更新同一报告的五臂聚合、逐outer配对、资源表和严格门；
- 通过则仅冻结FULL_ONLY进入完整125确认；失败则记录`NO_D_GEOMETRY_PROMOTION`，不从解释臂补选。
