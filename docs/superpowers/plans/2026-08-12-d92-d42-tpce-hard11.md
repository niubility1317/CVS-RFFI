# D92 D42 TPCE Hard11实施计划

## Task 1：科学核心（Terra/max）

文件所有权：

- 新增`code/cvsrffi/stage2_d92_d42_tail_pair_code_exchange.py`；
- 修改`code/cvsrffi/stage2_d92_e0d_slim.py`；
- 修改`code/cvsrffi/stage2_d92_e0d_query_evaluation.py`；
- 新增/修改对应core、slim、query测试。

先写RED：单原子精确margin增益、全量干扰但存在安全子集、行序与类置换等变、直接state发布、六旧类/pooled新类/双hinge守卫、码位饱和与数值fallback、K1/K2 alias、query零访问。GREEN只实现冻结的确定性Pareto安全贪心公式，不改Hard11矩阵。

## Task 2：机械闭环（Luna/max）

文件所有权：

- 新增`configs/stage2_d92_full_d42_tpce_hard11_v1.json`；
- 新增`code/cvsrffi/stage2_d92_tpce_hard11.py`；
- 新增`code/scripts/run_d92_tpce_hard11.py`；
- 新增`code/cvsrffi/stage2_d92_tpce_hard11_analysis.py`；
- 新增`code/scripts/analyze_d92_tpce_hard11.py`；
- 新增三份Hard11/runner/analyzer测试。

克隆已验证Hard11的矩阵、共享stop、truth-free smoke、artifact闭包和八指标analyzer，只替换candidate/method receipt。K>2 fit门为`2/1`，K1为真实alias`3/3`。不得修改科学文件、矩阵、阈值或方法。

## Task 3：独立复核（Terra/max）

在Task 1/2完成后只检查真实P0/P1：直接D42 state生命周期、tail/hinge复算、类置换、K边界、query零访问、fit/resource口径、runner smoke先行和analyzer严格三分支。方法作者不得自证。

## Task 4：本地冻结与发布

主代理合并diff，运行最窄聚焦回归、真实checkpoint no-query smoke、`py_compile`和`diff --check`；P0=0/P1=0后提交不可覆盖run报告与release commit。唯一N607 runner执行preflight、精确同步、K>2 smoke、8shard、短连接监控和完整取回；不得调方法或按性能停止。

## Task 5：分析与裁决

独立analyzer最后连接truth，输出10个performance outer×3scene、E0/TPCE/D92同排均值、receiver/scene/K/new-count/六旧类/双向混淆和资源分解。唯一结论为`ADVANCE_TO_TARGET125_CANDIDATE`、`REVISE_ONCE`或`REJECT_ROUTE`；Hard10不自动触发完整125。
