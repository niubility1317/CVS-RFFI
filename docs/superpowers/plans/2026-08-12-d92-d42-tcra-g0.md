# D92 D42 TCRA G0实施计划

## 冻结目标

先实现TCRA科学路径和一个固定真实K10、三场景、truth-free G0。G0只判定机制active/fallback、协议闭包和registration wall P90≤150ms；不实现Hard11、不运行scorer、不读取性能。

## 实施顺序

1. Terra科学owner新增TCRA核心与测试，并接入E0D arm/slim/query；不修改Hard11机械文件或N607报告。
2. 核心测试锁定E0一次性tail、canonical float64聚合、每class×block唯一单行原子、int8边界、真实prefix/final guard、byte-exact fallback和组内label/row置换等变。
3. slim/query测试锁定K>2 two-state fit=2/actual=1、后处理fit=0；K≤2严格FULL alias；所有query禁用访问为false；state/prediction/receipt闭合。
4. 运行聚焦回归、`py_compile`和diff检查；独立P0/P1只审科学正确性与G0入口。
5. 本地Git提交并预注册不可覆盖G0 run ID；封存最小runtime closure和单一K10 outer：`rx_7_7__seed_713106__k_10__new_5`。
6. sole runner在N607执行唯一truth-free prediction命令，覆盖clear/low_elev/rain；不读取truth、不运行scorer。
7. 三场景均active、state非E0、最终guard通过且wall P90≤150ms才进入Hard11机械实现；否则记录失败并直接修订或拒绝路线。

## Ownership

- 科学：TCRA core、E0D arm/slim/query及其测试。
- 主代理：设计冻结、diff整合、P0/P1裁决、Git/report和晋级决定。
- Luna runner：冻结文件落地、唯一launch、结构/资源证据与artifact取回；禁止调参、改方法或读取性能。

## v2执行附录

v1真实G0资源通过但严格七组正增益门未通过。监督裁决冻结`TCRA_SAFE_DIRECTIONAL_v2`：保留全部prefix守卫，只将final门改为七组/pooled-all/hinge在tol内非劣，同时要求至少一个旧类严格正增益、六旧类gain总和严格正、state非E0且selected>0。

执行顺序更新为：TDD修改core/query receipt→聚焦回归→独立P0/P1→新不可覆盖G0 run ID复核三场景active/state/resource→仅在G0通过后构建Hard9+K1单臂矩阵。Hard9从原Hard10中排除已用于G0 support开发的`rx_7_7__seed_713106__k_10__new_5`。Hard9读取完整同排八指标，任一均值未严格朝优方向即拒绝，不允许再次修改门或同run重试。
