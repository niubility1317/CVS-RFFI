# BiSAGE-D92 Historical Target125 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 实现SAGE-D与SAGE-R，在历史D92 E0完全相同的Target125数据、seed、切片和场景上先闭合阶段A，达门槛后自动完成阶段B和125行truth-last验证。

**Architecture:** 新增独立`stage2_bisage_*`模块，复用现有BiNOVA特征输入与正式D92构造，不改写历史BiNOVA代码。历史Target125 builder负责绑定原D92 retry2的125个sealed package；生命周期只允许support拟合和选择，query端只加载冻结状态并逐样本对全部注册类argmax。

**Tech Stack:** Python3.10、PyTorch、NumPy、scikit-learn、pytest、Git、N607/8×GPU。

**Spec:** `E:/codex/home/attachments/92f5217f-e9ef-40f1-b025-090993f9da67/pasted-text.txt`

## Global Constraints

- `protocol_schema=p2_min_v1`且`phase2_data_status=VALIDATED_ONCE`。
- 复用原D92 retry2的125个sealed package；receiver=`20-1,3-19,7-14,7-7,8-8`，seed=`713102–713106`。
- slice=`K1/new20,K5/new20,K10/new5,K10/new10,K10/new20`；场景固定三个`leo_*_weak`。
- query及其视图只读，不参与fit、update、selection、quota、role oracle或global reassignment。
- 四状态仅使用`DA0_REG0,DA1_REG0,DA0_REG1,DA1_REG1`。
- K1不能伪造类内cross-fit；选择器必须回退S0。
- 可微D92与正式D92在同输入上的最大logit绝对误差小于`1e-4`。
- 不增加Exclusive Minimal Experiment Workflow八项白名单外的阻断gate。

---

### Task 1: 历史D92 E0 Target125绑定

**Files:**
- Create: `code/cvsrffi/stage2_bisage_target125.py`
- Create: `configs/stage2_bisage_d92_target125_20260830.json`
- Test: `tests/test_stage2_bisage_target125.py`

**Interfaces:**
- Consumes: historical `matrix_manifest.json` and 125 sealed source job roots.
- Produces: `build_bisage_target125_manifest(config, source_manifest, output_root) -> dict` and `validate_bisage_target125_manifest(manifest, config) -> dict`.

- [ ] **Step 1: Write the failing matrix identity tests**

```python
def test_target125_reuses_historical_d92_axes():
    rows = canonical_target125_rows()
    assert len(rows) == 125
    assert {r["seed"] for r in rows} == set(range(713102, 713107))
    assert {(r["k_shot"], r["new_class_count"]) for r in rows} == {
        (1, 20), (5, 20), (10, 5), (10, 10), (10, 20)
    }

def test_target125_rejects_capsule_or_split_drift():
    manifest = valid_manifest()
    manifest["jobs"][0]["split_id"] = "wrong"
    with pytest.raises(BiSAGETarget125Error, match="split"):
        validate_bisage_target125_manifest(manifest, config())
```

- [ ] **Step 2: Run tests and confirm missing-module failure**

Run: `python -m pytest tests/test_stage2_bisage_target125.py -q`

Expected: FAIL during import because `stage2_bisage_target125` does not exist.

- [ ] **Step 3: Implement canonical axes and strict identity validation**

Implement constants for the five receivers, five seeds, five slices and three scenes. Validate `protocol_schema/capsule_id/split_id/phase2_data_status`, exact source job path binding, unique 125 outer keys and 375 scene units; never open query truth.

- [ ] **Step 4: Run focused tests**

Run: `python -m pytest tests/test_stage2_bisage_target125.py -q`

Expected: PASS.

### Task 2: 正式D92数值等价的可微内层

**Files:**
- Create: `code/cvsrffi/stage2_bisage_d92.py`
- Test: `tests/test_stage2_bisage_d92.py`

**Interfaces:**
- Consumes: `features: torch.Tensor`, contiguous labels, `old_class_count`.
- Produces: `fit_bisage_d92(...) -> BiSAGED92State`, `score(rows) -> torch.Tensor`, `compare_exact_d92_logits(...) -> dict`.

- [ ] **Step 1: Write parity and gradient tests**

```python
def test_torch_d92_matches_exact_numpy_logits():
    state = fit_bisage_d92(torch_rows, labels, old_class_count=6)
    audit = compare_exact_d92_logits(state, numpy_rows, labels, probe_rows)
    assert audit["max_logit_abs_error"] < 1e-4

def test_d92_held_loss_backpropagates_to_features():
    rows = torch_rows.clone().requires_grad_(True)
    fit_bisage_d92(rows, labels, old_class_count=6).score(rows[:3]).sum().backward()
    assert rows.grad is not None and torch.isfinite(rows.grad).all()
```

- [ ] **Step 2: Confirm failure against the current surrogate**

Run: `python -m pytest tests/test_stage2_bisage_d92.py -q`

Expected: FAIL because the new module is absent or fixed shrinkage differs from formal D92.

- [ ] **Step 3: Implement shared-covariance D92 in float64**

Use the formal old/new task-balanced covariance, equal class priors, positive-definite shrinkage, Cholesky solve and affine logits. Preserve autograd and expose covariance eigenvalue/condition audits.

- [ ] **Step 4: Verify parity and gradients**

Run: `python -m pytest tests/test_stage2_bisage_d92.py tests/test_stage2_binova_d92.py -q`

Expected: PASS.

### Task 3: SAGE-D阶段A

**Files:**
- Create: `code/cvsrffi/stage2_bisage_da.py`
- Test: `tests/test_stage2_bisage_da.py`

**Interfaces:**
- Consumes: old-class `BiNOVASupport`, frozen features, `SAGEDConfig`.
- Produces: `fit_sage_d(...) -> SAGEDState`, `evaluate_sage_d_crossfit(...) -> dict`, `stage_a_gate(metrics) -> dict`.

- [ ] **Step 1: Write behavior tests**

```python
def test_role_rotations_cover_every_old_class_as_pseudo_new():
    rotations = build_role_rotations(range(6))
    assert {c for r in rotations for c in r.pseudo_new} == set(range(6))

def test_stage_a_forward_has_no_class_id_input():
    assert "class_ids" not in inspect.signature(SAGEDModule.forward).parameters

def test_k1_stage_a_returns_s0_fallback():
    result = fit_sage_d(k1_support, features)
    assert result.audit["selected_mode"] == "S0"
```

- [ ] **Step 2: Confirm failures**

Run: `python -m pytest tests/test_stage2_bisage_da.py -q`

Expected: FAIL because SAGE-D is not implemented.

- [ ] **Step 3: Implement zero-init nonlinear residual and role consensus**

Implement rank32 late/identity residuals, class-balanced context, deterministic 80/20 sample cross-fit for K>1, three 4-base/2-pseudo-new rotations, coordinate-median consensus with conflicting-gradient projection, pseudo-D92, forgetting, topology, covariance-homogeneity, non-affine and movement losses.

- [ ] **Step 4: Implement the registered Stage A gate**

Gate requires prediction change, `delta_lcb_h_pseudo>0.001`, `nonaffine_energy>=0.1`, non-decreasing pseudo old-post/floor and non-increasing forgetting. Record failure as scientific non-promotion, not technical failure.

- [ ] **Step 5: Run focused tests**

Run: `python -m pytest tests/test_stage2_bisage_da.py tests/test_stage2_binova_features.py -q`

Expected: PASS.

### Task 4: SAGE-R阶段B

**Files:**
- Create: `code/cvsrffi/stage2_bisage_reg.py`
- Test: `tests/test_stage2_bisage_reg.py`

**Interfaces:**
- Consumes: frozen `SAGEDState`, registered support, formal D92 baseline.
- Produces: `fit_sage_r(...) -> SAGERState`, `apply_sage_r(...) -> features`.

- [ ] **Step 1: Write boundary/risk tests**

```python
def test_boundary_gate_suppresses_far_samples():
    gate = boundary_gate(torch.tensor([0.0, 10.0]), tau=1.0, gamma=5.0)
    assert gate[0] > 0.9 and gate[1] < 1e-6

def test_augmented_lagrangian_increases_eta_on_old_risk_violation():
    assert update_eta(eta=0.0, violation=0.2, rho=2.0) == pytest.approx(0.4)

def test_sage_r_does_not_update_sage_d_parameters():
    before = clone_params(stage_a.module)
    fit_sage_r(stage_a, registered_support)
    assert same_params(before, stage_a.module)
```

- [ ] **Step 2: Confirm failures**

Run: `python -m pytest tests/test_stage2_bisage_reg.py -q`

Expected: FAIL because SAGE-R is absent.

- [ ] **Step 3: Implement boundary-conditioned residual and old-risk constraint**

Use the six-dimensional D92 condition vector, rank16/32 joint identity+FFT residual, sigmoid boundary gate, new/old CE, old-to-new/new-to-old, forgetting, topology, CVaR/condition penalties and augmented-Lagrangian old-risk constraint. Freeze SAGE-D parameters.

- [ ] **Step 4: Run focused tests**

Run: `python -m pytest tests/test_stage2_bisage_reg.py tests/test_stage2_binova_reg.py -q`

Expected: PASS.

### Task 5: S0/S1/S2生命周期、阶段A门槛与125自动续跑

**Files:**
- Create: `code/cvsrffi/stage2_bisage_lifecycle.py`
- Create: `code/scripts/run_stage2_bisage_d92.py`
- Create: `code/scripts/run_stage2_bisage_target125.py`
- Test: `tests/test_stage2_bisage_lifecycle.py`
- Test: `tests/test_run_stage2_bisage_d92.py`
- Test: `tests/test_run_stage2_bisage_target125.py`

**Interfaces:**
- Consumes: Task1 manifest, Task3/4 states and immutable query features.
- Produces: stage-A pilot artifacts, automatic stage-B continuation marker, per-row four-state predictions and shard receipts.

- [ ] **Step 1: Write lifecycle and query-isolation tests**

```python
def test_stage_b_runs_only_after_stage_a_pass():
    assert next_stage({"stage_a_gate_passed": True}) == "STAGE_B"
    assert next_stage({"stage_a_gate_passed": False}) == "STOPPED_SCIENTIFIC_GATE"

def test_query_prediction_never_calls_fit(monkeypatch):
    monkeypatch.setattr(module, "fit_sage_d", forbidden)
    monkeypatch.setattr(module, "fit_sage_r", forbidden)
    predict_query_read_only(frozen_states, query)
```

- [ ] **Step 2: Confirm failures**

Run: `python -m pytest tests/test_stage2_bisage_lifecycle.py tests/test_run_stage2_bisage_d92.py tests/test_run_stage2_bisage_target125.py -q`

Expected: FAIL because lifecycle and runners are absent.

- [ ] **Step 3: Implement immutable state selection and four-state prediction**

S0 is direct D92, S1 is SAGE-D, S2 is SAGE-D+SAGE-R. Select by support-only cross-fit; fallback order is S2→S1→S0. K1 is always S0. Query runner loads frozen states and emits all four named states without truth.

- [ ] **Step 4: Implement pilot-to-Target125 orchestration**

Run the fixed Stage A pilot first. If its scored gate passes, create the Stage B continuation marker and dispatch eight immutable Target125 shards. If it fails, preserve predictions/score and exit with a scientific non-promotion state.

- [ ] **Step 5: Run focused tests**

Run: `python -m pytest tests/test_stage2_bisage_*.py tests/test_run_stage2_bisage_*.py -q`

Expected: PASS.

### Task 6: 本地验证、报告、Git与N607发布

**Files:**
- Create: `automation_reports/CV-SincNet/bisage_d92_historical_target125_20260830_v1/report.md`
- Modify: `task_plan.md`
- Modify: `findings.md`
- Modify: `progress.md`

**Interfaces:**
- Consumes: all implementation and tests.
- Produces: versioned code/config/report, release archive, N607 artifacts and final truth-last analysis.

- [ ] **Step 1: Run ssr-gpu focused suite and real-checkpoint no-query smoke**

Run the focused pytest set, `python -m py_compile` on all new modules and both CLIs, then execute one real checkpoint smoke without query inputs.

- [ ] **Step 2: Perform the single P0/P1 review**

Review only direct experiment-breakers: protocol/query leak, wrong matrix/binding, output collision, process ownership, launch/prediction failure. Fix at most once and rerun only affected tests.

- [ ] **Step 3: Commit and push exact files**

Stage only the new BiSAGE code/tests/config/plan/report, run `git diff --check`, commit, push current branch, and verify remote OID equals local `HEAD`.

- [ ] **Step 4: Release to N607**

Run the required direct N607 preflight, create one release archive, compare one local/remote SHA, compile remotely, run truth-free smoke, launch immutable run and perform one PID/CWD/cmdline/GPU/log-growth check.

- [ ] **Step 5: Score and report**

After prediction closure, use the independent scorer to connect truth. Report same-row S0/S1/S2, four states, receiver/seed/slice/scene/per-class/floor/forgetting/resources and the stage-A/Target125 verdict. Commit/push the final report and verify remote OID again.
