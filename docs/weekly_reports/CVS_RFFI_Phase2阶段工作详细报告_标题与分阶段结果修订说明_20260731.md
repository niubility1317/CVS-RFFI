# CVS-RFFI Phase2阶段工作详细报告修订说明

## 修订基准

- 唯一正文基准：用户手动删减后的`CVS-RFFI_Phase2阶段工作详细报告_三轴任务谱系表优化版_截至20260730.docx`。
- Stage2-B结果数据源：`学习进展情况+7.16.docx`。
- Stage2-C结果数据源：`学习进展情况+7.24.docx`。
- 本次不使用旧版构建脚本重新生成正文，只进行局部结构修订、表格替换和必要的口径同步。

## 结构修订

1. 补充第1章总标题，将原有概念章节统一为`1.1`至`1.6`。
2. 将第2章中删减后遗留的`2.3`、`2.4`、`2.5`顺延为`2.2`、`2.3`、`2.4`。
3. 将评价指标下的三级标题编号为`2.3.1`至`2.3.3`。
4. 将Stage2-B方法比较标题明确为“对比方法机制与权限对比”，并在其后增加`3.6`结果章节。
5. 将Stage2-C正式LEO结果与matched无LEO诊断改为逐配置结果。
6. 将参考文献编号为第5章。
7. 删除末尾与Stage2-B正文重复的附录A。
8. 取消一级标题的强制章前分页，保留标题与下一段连排，消除表格续页后产生的半空页。

## 结果表替换

### Stage2-B

在ProtoNet CDA、MRIOR-SDA和DADDA-SDA的机制与权限对比之后，依次放入7.16周报的三张原表：

1. 总体性能；
2. 不同K-shot；
3. 不同target receiver。

三张表分别保留周报中的字段含义、数值、正负号和百分号；指标列名按报告统一符号体系显示为Word公式，不再只在附录给出均值结论。

### Stage2-C

在CSIL与MoPC-HR机制对比之后，使用7.24周报的两张原表替换跨全矩阵总均值表：

1. 正式LEO条件下，按`K-shot×方法×新类数`展开的24行配置结果；
2. matched无LEO条件下，按`K-shot×方法×新类数`展开的18行配对诊断结果。

正文解读同步改为逐配置口径；指标表头使用带上下标的Word公式，并保留`DIAGNOSTIC_NEW_CLASS_NO_LEO_NON_FORMAL`边界。

## 排版与验证要求

- 保留用户删减版的正文样式与公式对象。
- 中文字体使用宋体，英文、数字和变量使用Times New Roman。
- 新增表格使用固定列宽、重复表头、禁止单行跨页拆分。
- 交付前通过Word导出PDF并逐页检查标题、公式、表格、分页和引用。
