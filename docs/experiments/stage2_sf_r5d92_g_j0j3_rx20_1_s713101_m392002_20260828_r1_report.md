# SF-TAPFT-R5D92-G J0–J3实验报告

## 最小预登记

- 状态：`LOCAL_VERIFIED`
- run ID：`stage2_sf_r5d92_g_j0j3_rx20_1_s713101_m392002_20260828_r1`
- 科学问题：在同一receiver、seed、K、新类draw和support/query manifest下，比较J0 D92 E0、J1 R0/E0→D92 E0、J2 R3双Delta→D92 E0和J3 SF-TAPFT-R5D92-G→D92 E0，判断注册感知选择能否改善注册后旧类、新类、H和遗忘。
- 场景与数据：receiver=`20-1`、data seed=`713101`、method seed=`392002`、旧6类K=10、新5类K=10，`leo_clear_weak`、`leo_low_elev_weak`、`leo_rain_weak`；复用`p2_min_v1/VALIDATED_ONCE` capsule=`cvs-full-ablation-phase2c-t1-rx20-1-method7282101-new20-nested-v1`及其new5嵌套切片。
- 共同checkpoint：ADV3B02 CORE90的`best_joint_safe_ssdg.pth`及匹配Phase1 bundle。
- 候选：
  - J0：不做域适应，直接D92 E0无RF32；
  - J1：R0/E0，target head+`t3.norm(weight,bias)`，520步；
  - J2：两个确定性8-shot/class轨迹平均Delta，完整support收尾30步；
  - J3：五个互补8-shot/class轨迹，快照250/350/520，alpha=0.75/1.0，旧类held-out风险Top-2，baseline+Top-2执行D92 E0 cross-fit，按硬约束与LCB(H)选择，必要时回退J1。
- 权限：source-free；训练和选择只读取合法old/new support，不具备query输入能力。prediction完成前不连接query truth/role；scorer只在prediction完整后连接truth。
- D92路径：`M29-FFT96-A4/D92-E0-NORF32`，最终query只使用重新估计中心、任务均衡共享协方差和尺度后的统一D92仿射头；SF target head仅作为训练辅助头。
- 环境/CWD：N607，`/home/szu2070436088/2510044040/CV-SincNet`，项目Conda Python。
- GPU：预计划clear/low/rain分别占用GPU0/1/2；实际发布前以N607短连接资源盘点为准，每GPU不超过2个训练任务。
- 本地实现文件：`code/cvsrffi/stage2_sf_r5d92.py`、`code/cvsrffi/target_only_progressive_adapt.py`、`code/cvsrffi/target_only_progressive_runner.py`、`code/cvsrffi/stage2_sf_erbt_four_state.py`、`code/cvsrffi/stage2_d92_registration_balanced_covariance.py`、`code/scripts/run_sf_r5d92_adapt.py`。
- 本地验证：R5D92、RSE、部署runner、D92、四状态聚焦回归共60项通过；CLI帮助通过；真实checkpoint无query smoke待N607 release完成后执行。
- 独立P0/P1检查：`APPROVE`。定点检查确认五折原型与梯度均不读取held-out、registered old support必须与冻结old support按物理ID和IQ逐行一致、R5D92入口无query参数、D92失败候选安全拒绝、输出目录不可覆盖、最终四状态prediction只使用重建D92统一头；未发现会导致真实实验跑错、越权、覆盖输出、误杀进程、不能启动或不能产生合法prediction的遗留问题。
- 实现Git提交：`4573950d25fa1259b4d4978ba9873fcd6800d5a2`，已自动push并由hook独立核对远端OID。
- 远端输入：复用既有D3 nested input根；发布前只读核对new5 old support、registered support、max query与data handle。
- 远端输出：`runs/stage2_sf_r5d92_g_j0j3_rx20_1_s713101_m392002_20260828_r1`，不可覆盖。
- 预期artifact：每场景J1/J2/J3的`selection.json`、`sf_tapft_delta_bundle.pt`、GNU time和GPU采样；每场景J1/J2/J3的四状态`predictions.npz`、`prediction_receipt.json`；truth-last`score.json`。
- 技术停止：错误协议/数据句柄/checkpoint/scene/seed/K/new draw、support/query物理ID交叠、query泄漏、错误checkout、输出覆盖、确定性异常、无prediction闭合或进程归属不清。低性能不得停止。
- J3时间规则：240秒边界后不再启动第二个Top-2 D92 cross-fit；方法目标300秒，外层技术hard timeout=360秒。已完成候选保留，必要时安全回退J1。
- 分析口径：完整报告四状态`DA0_REG0`、`DA1_REG0`、`DA0_REG1`、`DA1_REG1`；REG0新类指标为`N/A`。同row报告old pre/post、new、H、F、min-old、min-new、old→new、old→wrong-old、NLL、ECE、协方差条件数/正定性/identity与FFT trace、状态字节、墙钟、RSS、GPU峰值和适配计算计数。
- J4边界：MRIOR→D92仅作为宽权限历史上界，不在本轮source-free最小矩阵重复训练，也不与J0–J3混入同一晋级排行榜。

## 当前进度

实现、60项本地聚焦回归、编译检查和独立P0/P1定点检查已闭合，尚未发布N607实验，当前没有真实性能结果。
