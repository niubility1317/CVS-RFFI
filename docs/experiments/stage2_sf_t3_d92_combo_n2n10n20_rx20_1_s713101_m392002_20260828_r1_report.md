# SF-TAPFT t3.norm＋D92组合实验报告

## 预登记

- run ID：`stage2_sf_t3_d92_combo_n2n10n20_rx20_1_s713101_m392002_20260828_r1`
- 状态：`STOPPED_EARLY_SYSTEMIC_TECHNICAL_FAILURE / NO_PERFORMANCE_RESULT`
- 候选：`D0_T3_D92`、`S02_T3_D92`、`R3_DUALDELTA_T3_D92_INLOOP`
- 矩阵：3候选×3场景×`N_new={2,10,20}`，共27格；每格输出四状态。
- checkpoint：`ADV3B02_CORE90_SOFT_E200`。
- 数据：复用匹配`p2_min_v1/VALIDATED_ONCE/capsule_id/split_id`的既有Phase2包；旧类6类、K10。
- D92：E0、RF32关闭；D92公式不变。
- 输入：固定checkpoint、old support、registered support、无truth/role query、data handle。
- 输出：不可覆盖run root下的delta、四状态prediction、selection、GNU time和GPU采样。
- GPU：N607预检后按每卡最多2个训练任务调度。
- 停止规则：只对协议越权、错误输入/checkout、输出覆盖、确定性执行故障或prediction无法闭合进行定点技术停止；不得因低性能停止。
- 评分：全部prediction闭合后由独立scorer连接truth。

## 实现与结果

### 本地实现

- `D0_T3_D92`：复用H6 Compact的`300/150/70`步缓存训练，只导出`t3.norm.weight/bias`，临时target head不进入部署bundle。
- `S02_T3_D92`：复用历史S02的4折4500步grouped OOF选择，再按新row选中的步数完成全support refit；不得硬编码历史clear row的412步。
- `R3_DUALDELTA_T3_D92_INLOOP`：执行2个每类8条support子集的dual-delta＋30步全support polish；只提取`t3.norm`delta，并用注册support上的2折D92-E0-NORF32风险在`alpha={0,0.5,0.75,1}`中选择强度。
- 新部署schema只保存2个`t3.norm`delta、base checkpoint绑定和rank16恒等time adapter结构信息，不保存target head。
- 四状态预测复用既有D92路径；t3-only loader注入DA1模型，D92分类公式保持不变。

### 本地验证

- TDD：三个组合、27格计划和t3-only bundle均先观察到预期红灯，再完成绿灯。
- 聚焦与回归测试：42项通过，覆盖三个新组合、t3-only bundle、四状态D92、D92协方差和既有RSE。
- 配置读回：`inspect-plan`返回27格。
- N607只读preflight：8张RTX3090均空闲；项目根和checkpoint可见。
- 数据只读核对：三场景的`new2/new10/new20`predictor support/query和data handle均存在；query量沿用既有最大合法切片，分别为160/320/520条。
- 一次P0/P1审查：无P0；发现并修复S02在OOF合法选择`zero_adapt`时被误停的P1。现在该分支输出两项零`t3.norm`delta并继续四状态prediction，低性能不再被当作技术故障。定点测试9项通过，定点复审`FIX_VERIFIED`。

### r1技术停止

- release归档本地与N607单次SHA256一致，远端编译及27格计划读回通过。
- 首个`D0_T3_D92/leo_clear_weak`真实checkpoint无query smoke运行约12.4秒，在写出delta前确定性失败：真实模型使用`id_backbone.t3.norm.weight/bias`，首版导出器错误地只识别`t3.norm.weight/bias`。
- r1未产生delta、prediction或性能结果；未读取query内容或truth；原run root和日志保留且禁止覆盖。
- 修复使用新run ID`stage2_sf_t3_d92_combo_n2n10n20_rx20_1_s713101_m392002_20260828_r2_techfix1`继续，r1不得重启。
