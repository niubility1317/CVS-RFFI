# CORE90已完成15行目标测试集评测

## 用户授权与冻结范围

2026-09-11用户明确要求“训练完成的15行进行测试集测试，然后给出详细数据”。固定B0、B1、B2、B3、B3_lr_high、B3_head_slow、B3_head_fast、B7、S1、S3、S4、C1、C2、H1、J1；全部seed392005、E200、final_ssdg.pth。后续完成的其他行不自动加入本次测试。

此前15行的source_scores.json仅是源域V结果。本次新增目标RX0、2、5、7、9、10、11，日期索引0、1、2、3的全量可用样本；不抽样。输出clean、leo_clear_weak、leo_low_elev_weak、leo_rain_weak四视图闭集诊断。不是Phase2增量/未知类评测，不作单seed科学晋级或基于测试结果选模、调参、重跑。

## 输入与运行契约

- 输入训练版本300ff4a9；N607路径`/home/szu2070436088/2510044040/CV-SincNet/runs/core90_game_392005_300ff4a9/runs/<row>_seed392005/final_ssdg.pth`。
- 在读取目标数据前，逐一核对scratch_only、final_only、epoch=200、seed392005、target_contact=false以及全部source_info物理样本角色；与当前source builder一致，并执行真实checkpoint源域V前向smoke。
- 模型eval、全部参数requires_grad=false；不建立optimizer、不使用teacher/EMA、不更新BN；结束核对buffer未变。每个样本对全部6个注册类argmax，无truth/配额/全局重排。
- 15模型共享同一批256个输入及同一LEO扰动，增强seed固定为训练数据split seed。预测文件只含opaque ID、scene、RX、day、prediction、confidence。
- 全15行全部四场景预测关闭后才写独立truth并由纯artifact scorer评分。输出总体、macro-F1、macro-recall、逐RX、逐day、逐TX及弱RX结果。
- 唯一launch owner为本任务；使用N607普通用户、GPU1上的单个推理进程；其余训练继续。仅异常/非有限logits/协议不一致停止本次推理，低准确率不触发干预。
- 新输出目录不可覆盖；发布版本与实际命令、PID、路径、完成状态和结果在运行后追加。

## 本地验证

新增4项负测：标签不进入prediction接口、RX重叠拒绝、物理sample重叠拒绝、污染或非E200/final checkpoint拒绝。复用现有scorer的完整性、重复预测、标签泄漏及确定性测试。

14项相关测试通过；独立P0/P1审查未发现阻断问题。推理代码481b868c49cf8bac1a1838d35f820e2bbbb3b7f6已提交、push并独立核对远端OID。

## 实际发布

- release：`/home/szu2070436088/2510044040/CV-SincNet/releases/core90_target15_481b868c`。
- 输出：`/home/szu2070436088/2510044040/CV-SincNet/runs/core90_target15_392005_481b868c`；stdout为同路径追加`.stdout.log`。
- 命令：`/home/szu2070436088/.conda/envs/CVS-RFFI/bin/python -u <release>/code/scripts/core90_game_target_test.py --run-root /home/szu2070436088/2510044040/CV-SincNet/runs/core90_game_392005_300ff4a9 --output-dir <output> --device cuda:0`。
- 环境：N607普通用户，CWD=`/home/szu2070436088/2510044040/CV-SincNet`，`CUDA_VISIBLE_DEVICES=1`，`OMP_NUM_THREADS=2`。
- PID3630172已独立核对argv、CWD、GPU UUID`GPU-a2d039ff-1792-4222-2b47-140429e10f94`及日志增长。archive传输SHA一致，远端编译通过。
- 15个真实checkpoint的source-only smoke通过；frozen_manifest实际样本数168000/scene，四场景15行合计10080000条预测。训练seed392005与增强seed392002分别记录，后者为冻结的game_split_seed，并非新增训练seed。
- 状态：VERIFIED / ANALYZED。15行四场景均完成；独立读回complete.json、predictions_complete.json及全部15份target_scores.json，推理进程已正常退出。

## 结果与解释

完整表见[目标测试详细报告](evidence/core90_target15_392005_20260911.md)，[5040行CSV](evidence/core90_target15_392005_20260911.csv)与[完整精度JSON](evidence/core90_target15_392005_20260911.json)。报告含60个模型×场景汇总、逐接收机、逐日期、逐TX、Macro-F1以及同模型源域V与目标测试差值；CSV还包含RX×TX和day×TX明细。

- 每个场景168000条：每RX24000、每day42000、每TX28000。各场景所有注册类均存在。15行各672000条预测，共10080000条，预测用时739.92秒；含评分总计805.00秒（13.42分钟）。
- C2的clean最高79.00%；S3的LEO均值最高65.10%，相对B0提高3.30个百分点，其最弱LEO接收机准确率49.03%。这是同一seed的描述性比较，未据此选择模型或更改配置。
- B3与B7的LEO均值分别58.48%、57.98%，低于B0的61.80%；机制已启动不能直接推导目标泛化更强。
- 源域V的clean约98%，目标clean降至73.63%—79.00%；目标LEO均值57.98%—65.10%。源域V到目标测试的差值已逐模型列出，不能以先前源域高分替代目标性能。
- 最弱接收机主要为RX2，部分LEO行转为RX7；各RX完整结果保留，不用平均值掩盖薄弱接收机。
- 独立检查全部60个score单元：RX样本数加权准确率等于总体准确率，TX/day计数均闭合，缺失注册类为空。buffer冻结检查通过，全部prediction关闭后才进入truth评分。

本次没有发布额外训练、没有重启其他健康run，也没有把目标结果回流训练。其余11行不属于这次冻结15行测试范围。
