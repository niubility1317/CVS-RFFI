# 新完成10行目标测试补齐

用户要求“有没有新完成的实验？给出测试详细数据”。本次实时核对25/26行训练完成、0失败；B6在E171继续训练。相对先前15行新增B3_head_scale、B3_adv_low、B3_adv_high、B4、B4_fixedk、B5、B8、S2、S2_random、C3。

B8已有`core90_target_b8_392005_56c4fb92`完整目标测试，复用其固定预测和分数。其余9行固定E200 final_ssdg.pth，使用已有target runner，仅扩展可显式指定的row名单。训练模型不变、seed392005，全部9行预测完再统一评分；不使用已有目标结果选模或调参。B6不进入本次冻结范围。

输入仍为训练版本300ff4a9，当前数据契约不变；逐checkpoint核查scratch_only/final_only/target_contact=false和完整source角色。真实source-only smoke后评测目标RX0、2、5、7、9、10、11及day0—3，全量168000/scene，clean+三LEO弱场景，增强seed392002、batch256。每模型672000条预测。所有预测面向全部6类argmax，不适配、不修改buffer。

唯一launch owner为本任务，N607普通用户GPU1单推理进程，CWD为`/home/szu2070436088/2510044040/CV-SincNet`；其他健康训练不干预。新输出目录不可覆盖，系统异常、非有限logits或数据契约不符终止本次推理；低性能不重跑。运行版本、路径、验证及结果在完成后追加。
