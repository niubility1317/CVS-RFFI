# CVS项目资产治理任务计划

## 目标

在不移动、不覆盖、不删除任何本地或N607资产的前提下，建立可复现的本地/N607资产总表、实验状态索引、Git归属、保留级别和待审批删除清单；完成治理闭环后，再单独进入方法与性能优化。

## 当前边界

- 本阶段采用用户批准的方案A：清单先行、零搬迁。
- 删除、覆盖、移动原件、停止任务、远端写入和管理员访问均不在当前授权范围内。
- 现有脏工作树、未跟踪实验产物、数据集、checkpoint、日志和报告必须原样保留。
- 设计与后续实现位于独立分支`codex/project-governance-20260813`。

## 阶段

| 阶段 | 状态 | 完成条件 |
|---|---|---|
| 1.上下文与风险盘点 | 完成 | 本地、Git和N607只读证据已取得 |
| 2.方案选择与设计确认 | 完成 | 用户明确批准方案A |
| 3.书面设计与自检 | 完成 | 设计文档无占位符、矛盾或未定义审批语义 |
| 4.设计提交 | 完成 | 仅显式暂存本任务文档并形成独立Git提交 |
| 5.用户书面复核 | 完成 | 用户于2026-08-17确认设计通过 |
| 6.实施计划 | 完成 | 已形成逐步、可验证、无删除动作的实施计划 |
| 7.实施方式选择 | 完成 | 用户选择子代理驱动实施，并由主代理负责集成与最终门禁 |
| 8.治理实现 | 完成 | Tasks1–8的模型、采集器、索引、分类、报告器和CLI已测试、复审并提交 |
| 9.实施总验收 | 完成 | 静态安全复核、256项回归和独立审查达到`P0=0、P1=0` |
| 10.正式只读盘点 | 准备执行 | 规模性能与终态证据已修复、回归和复审；提交当前日志后以新ID执行唯一一次扫描 |
| 11.决策交付 | 待开始 | 交付资产、实验、Git、保留和待审批删除清单及后续优化建议 |

## 错误记录

| 错误 | 尝试 | 处理结果 |
|---|---:|---|
| 普通PowerShell会话找不到`python` | 1 | 改为串行使用`conda run -n ssr-gpu` |
| 非交互PowerShell无法直接`conda activate ssr-gpu` | 1 | 使用命名环境运行器，未修改Conda配置 |
| 会话索引搜索触发Conda GBK编码错误 | 1 | 设置`PYTHONUTF8=1`和`PYTHONIOENCODING=utf-8`后成功 |
| 首个远端多行Bash统计脚本末尾语法错误 | 1 | 不采信未输出部分，改用流式`python3 -`只读补扫并成功 |
| 设计元数据使用Markdown硬换行导致`git diff --cached --check`报告尾随空格 | 1 | 改为项目符号元数据，重新暂存后复查 |
| 畸形UTF-8测试样本触发Conda外层GBK输出包装错误 | 1 | 显式设置`PYTHONUTF8=1`和`PYTHONIOENCODING=utf-8`后重跑，确认并修复真实测试问题 |
| 首次手工预检路径未整体加引号，被Git Bash剥除反斜杠 | 1 | 该次exit127且未连接N607；对根权威Windows路径整体加引号后预检通过 |
| 正式扫描会话未保留到明确exit，且未生成收据或输出目录 | 1 | 将`PGOV_20260817T185755Z`封存为`UNKNOWN`、禁止重跑；补充会话持续轮询规则后才允许新ID |
| 第二个扫描ID也未取得原始session终态，最终只留下0文件Git目录 | 1 | 将`PGOV_20260817T191013Z`封存为`UNKNOWN / NO_RECEIPT`；保留空目录，不删除、不复用ID |
| 第三个扫描ID因界面会话丢失停在`GIT/STARTED`且无收据 | 1 | 以progress精确PID核验终态，封存为`UNKNOWN_ABRUPT_EXIT / NO_RECEIPT`；后续使用独立后台wrapper和原子退出码文件 |
| 第四个扫描ID在Git阶段触发linked-worktree重复展开 | 1 | 精确终止本任务PID并保留退出码143；TDD按common Git目录去重，提交`eb42737a`后才允许新ID |
| 第五个扫描ID在去重后仍于Git阶段持续占满单核 | 1 | 10秒累计CPU与无Git子进程证实资产×仓库根重复解析；精确终止并保留退出码143，提交`04dcce54`缓存仓库根后才允许新ID |
| 第六个扫描ID的N607直连采集达到旧45秒整段总时限 | 1 | 保持结果`UNKNOWN`并精确确认child/proxy/SSH/TCP22均已闭合；连接仍10秒，采集预算TDD调整为15分钟并提交`e702fcec` |
| Git porcelain v2的NUL分隔状态无法写入CSV | 1 | 本地诊断复现准确堆栈；仅在CSV单元格转义NUL，JSON保真，提交`92cb63eb`并通过304项回归 |
| `ps -ef`按scan ID匹配命中监控脚本自身，错误报告进程持续存在 | 1 | 用`ps -W`和`tasklist.exe`交叉确认无扫描进程；后续仅接受原始session或启动时精确PID/父子绑定作为终态证据 |
| 真实规模实验索引出现全资产与同组笛卡尔重复遍历 | 1 | 已以TDD建立等价路径索引、binding缓存和token倒排；盘符根/UNC语义修复后独立复审通过 |
| 输出目录先创建、但CLI无阶段持久证据，执行session丢失后无法追溯 | 1 | 已增加独占progress journal、精确PID、收据三态和单次终态追加；三轮故障注入复审后P0/P1/P2均为0 |

## 下一门禁

历史失败ID均已封存且不得复用；最近一次正式扫描已证明全链可到达N607并暴露两个确定性缺口。CSV与N607有界采集预算均已TDD修复，完整治理/N607 304项回归通过，当前门禁保持`P0=0、P1=0`。下一步提交本日志后使用全新ID执行一次不可覆盖的本地/N607只读扫描，并以最终收据验证服务器清单、删除授权为0及连接闭合。移动、覆盖、删除、停止任务、远端写入和管理员访问仍不在授权范围内。
