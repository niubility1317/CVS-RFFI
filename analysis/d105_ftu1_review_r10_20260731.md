# D105-FTU1独立代码审查R10

状态：`LOCAL_CODE_REVIEW_GO / P0=0 / P1=0 / P2=2 / NOT_N607_AUTHORIZATION / NO_PERFORMANCE_RESULT`

审查人：独立代码审查子agent`d105_ftu1_review_r10`。本收据只覆盖本地代码闭环；不授权N607同步、启动、Target25、authority/seal或任何性能声明。

## 1.范围与工作树完整性

- 基线commit：`c91fadc14471dadbb6aaf6e13a322ab00217e2da`。
- 审查开始和结束时，未暂存diff均严格只有：
  - `code/cvsrffi/stage2_d105_phase1_bundle.py`
  - `tests/test_stage2_d105_phase1_bundle.py`
- `git diff --check`通过；两处受检Python文件`py_compile`通过。
- 本审查没有修改实现、测试、配置或既有报告，没有访问N607，也没有创建commit。

## 2.独立复核的功能结论

|审查点|独立证据|结论|
|---|---|---|
|Phase1正式前向|`_strict_forward`只延迟导入`extract_d105_feature_tap`和`DualFeatureForwardError`，随后唯一调用D105专用tap；不存在GRB调用或id-only回退分支|通过|
|同IQ双backbone路径|`dual_feature_forward`将同一个`x`分别交给`id_backbone`和`dom_backbone`各一次；随后只从`feat_imp`取得域特征，并执行`dom_enhancer(feat_imp,x)`|通过|
|`z_dom`来源|新入口级回归在checkpoint形状的真实CVSincNet模型上记录`id_backbone`、`dom_backbone`、`dom_enhancer`各一次，验证enhancer第一输入等于`dom_backbone["feat_imp"]`、第二输入等于同一received IQ；导出的`z_dom`与直接计算结果精确相等|通过|
|`z_id/pre_relu`绑定|D105专用tap先对Torch张量验证linear输出、ReLU输出和`z_id`的字节相等；Phase1再逐字段验证float32、`[N,160]`、finite及`z_id==ReLU(pre_relu)`|通过|
|字段级fail-closed|对`hook_exact_bytes`、`z_id`dtype、`z_dom`形状/finite、`pre_relu`dtype、ReLU绑定和`execution_path`的7个破坏样例都在写出目录前拒绝|通过|
|R5复现防护|入口级回归故意把GRB的`strict_zid_with_hook`替换为抛错函数；Phase1真实入口和实际strict-tap export仍成功，故任何退回R5旧调用都会直接使回归失败|通过|
|协议/方法语义|diff只改变Phase1 source-only特征出口和诊断测试；没有新增Target、query真值、role/quota、跨query重排、clean/source Phase2运行时入口，也没有改CBRC、LPO-RC、qKNN、矩阵、seed或门槛|通过|

## 3.TYPE_CHECKING与legacy路径判定

### 3.1`TYPE_CHECKING`静态依赖

`stage2_grb_jp4_adv_drqknn_bcrr.StrictForward`只位于`if TYPE_CHECKING:`块中，且别名未被Phase1运行代码使用。独立fresh Python进程导入`cvsrffi.stage2_d105_phase1_bundle`后：

```text
grb_loaded_after_phase1_import=False
strict_forward_legacy_name=False
```

AST复核也仅发现这一条type-only import。判定：**接受，不是本次D105-FTU1的必须移除项。**它不产生运行时导入、闭包欺骗或可达的Phase1旧前向。该模块仍列在较大的候选runtime文件清单中，不能仅凭本次Phase1调用图把它断言为其他D105入口的无用文件。

### 3.2历史receipt兼容

低层`_validate_strict_tap_receipt`仍可读取历史`execution_path="strict_zid_with_hook"`，这是既有归档兼容面，不是当前export路径：FTU1 export只写`eager_forward_hook`。所有正式source-held、truth-open、score、gate和component消费者都会调用`_validate_candidate_lock_for_tap`，强制strict-tap receipt与当前candidate runtime SHA和method-lock SHA一致。因此旧runtime产物不能混入当前FTU1正式链。

判定：**不构成P1，不要求本次扩大改动。**新release的真实checkpoint source-only smoke仍应显式断言新receipt的`execution_path=eager_forward_hook`及当前runtime/method-lock绑定，防止以后兼容层被误用。

## 4.执行的本地核验

|命令或检查|结果|
|---|---|
|`git status -sb`、`git diff --name-status c91fadc1 --`、`git diff --check`|仅两处预期diff；无空白错误|
|fresh Python导入检查`sys.modules`、`_strict_forward.__code__.co_names`、AST import检查|GRB没有运行时加载；strict forward没有旧tap符号；仅type-only静态import|
|`pytest -q tests/test_stage2_d105_phase1_bundle.py -k "minimal_checkpoint_loader_matches_exact_ssdg_model_and_taps or export_fail_closes_each_strict_forward_field"`|8项通过|
|`pytest -q`全部9个D105测试文件|205项通过，exit0|
|`pytest -q tests/test_stage2_d105_phase1_bundle.py tests/test_stage2_d105_feature_tap.py tests/test_dual_feature_forward.py`|通过，exit0|
|`python -m py_compile code/cvsrffi/stage2_d105_phase1_bundle.py tests/test_stage2_d105_phase1_bundle.py`|通过，exit0|

唯一观察到的是既有`torch.cuda.amp.autocast`FutureWarning；不影响测试exit0或本次判定。

## 5.残余风险与下一门

|级别|事项|处理|
|---|---|---|
|P2|低层历史receipt兼容仍允许旧路径字符串|保持当前runtime/method-lock强绑定；新release的真实checkpoint smoke和archive验证必须确认只生成`eager_forward_hook`|
|P2|本审查独立执行的是checkpoint形状的真实CVSincNet入口级export，而非另行读取SHA钉定生产`.pth`|按release readiness在提交后执行同代、SHA钉定真实checkpoint的source-only no-truth smoke；不得将本收据替代该门|

最终裁决：`GO`仅表示D105-FTU1当前本地代码与定向回归可进入后续Git提交、真实checkpoint smoke、archive验证和新run预登记。它不表示Phase1资产、Target25或性能结果已完成，也不授权N607操作。
