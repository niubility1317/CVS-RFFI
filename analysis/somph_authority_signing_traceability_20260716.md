# SOMP-H authority signing CLI traceability

| ID | Source section | Requirement | Target files | Status | Verification | Notes |
|----|----------------|-------------|--------------|--------|--------------|-------|
| AS01 | Parent task | Read an exact authority lock and bind its canonical SHA256 into the fixed authority envelope schema/domain/issuer/key ID. | `code/scripts/sign_cvs_somph_authority_lock.py` | verified | Focused signer tests and the 279-case combined verification set pass | Reuses `somph_lineage_authority` canonical JSON and formal identity validator；生产验签identity使用函数体literal，不依赖可变展示global。 |
| AS02 | Parent task | Sign the authority message with an explicitly supplied Ed25519 private key through OpenSSL `pkeyutl -sign -rawin`. | `code/scripts/sign_cvs_somph_authority_lock.py` | verified | Temporary Ed25519 key signs successfully through OpenSSL 3.0 | Production path and SHA are pinned to the approved OpenSSL 3.0.18 binary; uses a closed binary temporary message file, empty config and minimal environment. |
| AS03 | Parent task | Verify the generated envelope with local `_verify_signed_envelope` and therefore the pinned public key before publishing outputs. | `code/scripts/sign_cvs_somph_authority_lock.py` | verified | Wrong-key and tamper tests pass | Verification occurs before either output is created. |
| AS04 | Parent task | Publish an exact envelope and detached receipt with `O_EXCL`, no overwrite and file `fsync`. | `code/scripts/sign_cvs_somph_authority_lock.py` | verified | Overwrite preservation and second-file rollback tests pass | Outputs are distinct, read-only and newly created. |
| AS05 | Parent task | Receipt records only lock/envelope SHA bindings, pinned public key/key ID, actual OpenSSL binary SHA and a false launch-authority guard; no private-key path, SHA or content. | `code/scripts/sign_cvs_somph_authority_lock.py`; `tests/test_sign_cvs_somph_authority_lock.py` | verified | Exact nine-key schema and serialized leakage assertions pass | The receipt SHA is returned for an external controller/report to pin. |
| AS06 | Parent task | Cover success, wrong key, tampering, overwrite and private-key non-disclosure without accessing the real offline-controller key. | `tests/test_sign_cvs_somph_authority_lock.py` | verified | Eleven focused tests pass；combined verification set共279项PASS | Tests use a private test-only verifier injection and separately prove the production hard-coded verifier rejects a generated untrusted key. |
