# ADV3B02域适应125实验配置、基准结果与后续对比启动规范

版本：2026-07-16

适用范围：CVS-RFFI/CV-SincNet的Stage2-B旧类目标域适应，以及后续Stage2-C旧类适应与新类注册对比实验。

历史诊断矩阵：`adv3b02_three_da_leoweakonly_20260715_v1`

当前Stage2协议状态：自2026-07-16起禁止使用任何源域数据；该历史矩阵中的MRIOR-SDA和DADDA-SDA读取了sealed source LEO_weak cache，因此不再属于当前正式Stage2基准。

## 1.技术摘要

2026-07-15完成的“125实验”是Stage2-B旧类域适应历史矩阵：

```text
5个target receiver×5个seed×5个K=125行/方法
```

ProtoNet CDA、MRIOR-SDA和DADDA-SDA各运行125行，共375行。375行全部完成，失败0；对应135,000条逐样本评分记录和8,625条loss trace。三种方法共享同一个ADV3B02 checkpoint，不接触clean样本或clean派生信号，query逐样本面对全部注册旧类，不使用角色Oracle、真实类别数量、类别配额或全局批量分配。但是MRIOR-SDA和DADDA-SDA在Stage2适应时读取了source LEO_weak样本并执行source replay，违反2026-07-16新增的“Stage2禁止源域数据”协议。

旧协议结果中MRIOR-SDA的旧类适应性能最强：平均old accuracy从73.60%提高到82.58%，增益为+8.98pp；DADDA-SDA提高到78.35%；ProtoNet CDA下降到66.85%。这些数值仍可用于说明source replay的性能上界和计算代价，但MRIOR-SDA、DADDA-SDA不能进入当前Stage2正式排名、晋升门槛或论文主表。

本矩阵只包含6个旧类，不包含target-new。它不能回答新类注册后的`seen_new_acc`、`H_old_new`和遗忘率。后续轻量qKNN候选必须在无源域数据的新Stage2-B/Stage2-C封存矩阵上重新建立正式基线，不能继续以旧MRIOR-SDA、DADDA-SDA作为强制门槛。

### 1.1Stage2禁止源域数据

Stage2-A/B/C运行时只允许以下输入：

```text
sealed Phase1 ADV3B02 checkpoint
当前target receiver的LEO_weak support/query
注册类别表
与数据无关且实验前锁定的算法配置
```

Stage2禁止：

```text
source IQ或source LEO_weak样本
source标签和source dataloader
source feature/logit/prototype/covariance/BN统计
source cache/anchor/memory bank/replay buffer
额外source adapter、ground adapter或source-derived head
运行时source CE、source replay、MMD/LMMD、DV-KL或CORAL source-target对齐
```

Phase1 ADV3B02 checkpoint是唯一允许继承的源域训练产物。固定超参数可以在Phase1/source validation阶段提前确定，但必须在Stage2启动前锁定，不能携带source样本、source统计或可查询source状态进入Phase2。

## 2.实验身份与证据位置

|字段|内容|
|---|---|
|实验ID|`adv3b02_three_da_leoweakonly_20260715_v1`|
|实验阶段|Stage2-B target-old few-shot domain adaptation|
|执行日期|2026-07-15|
|当前状态|`ARTIFACT_COMPLETE`，同时为`PROTOCOL_INVALID_FOR_CURRENT_STAGE2_SOURCE_ACCESS`|
|完成行数|375/375|
|失败行数|0|
|方法数|3|
|每方法行数|125|
|评分样本数|135,000|
|loss trace行数|8,625|
|基座模型|ADV3B02|
|特征|`z_id160`，160维|
|checkpoint SHA256|`2699eedcafe8cec880828592d2d65ba3781a9948939da5cf5c82b47143d59c98`|
|正式报告|`E:\type10-7\automation_reports\CV-SincNet\adv3b02_three_da_leoweakonly_20260715_v1\report.md`|
|逐行结果|`E:\type10-7\automation_reports\CV-SincNet\adv3b02_three_da_leoweakonly_20260715_v1\summary\per_run_results.csv`|
|总体汇总|`E:\type10-7\automation_reports\CV-SincNet\adv3b02_three_da_leoweakonly_20260715_v1\summary\method_overall_summary.csv`|
|逐K汇总|`E:\type10-7\automation_reports\CV-SincNet\adv3b02_three_da_leoweakonly_20260715_v1\summary\method_k_summary.csv`|
|逐receiver汇总|`E:\type10-7\automation_reports\CV-SincNet\adv3b02_three_da_leoweakonly_20260715_v1\summary\receiver_summary.csv`|
|逐场景汇总|`E:\type10-7\automation_reports\CV-SincNet\adv3b02_three_da_leoweakonly_20260715_v1\summary\scenario_summary.csv`|
|逐类汇总|`E:\type10-7\automation_reports\CV-SincNet\adv3b02_three_da_leoweakonly_20260715_v1\summary\class_summary.csv`|
|artifact审计|`E:\type10-7\automation_reports\CV-SincNet\adv3b02_three_da_leoweakonly_20260715_v1\summary\audit.json`|
|基准配置|`paper_reproduction/configs/adv3b02_stage2b_three_da_leo_weak_only_20260715_n607.json`|

历史`adv3b02_three_da_20260714_233837`在Phase2内部读取raw/clean IQ后生成LEO视图，不满足当前clean不可达协议，统一标记为`PROTOCOL_INVALID_FOR_PHASE2`。历史结果只能用于诊断，不能与本正式矩阵合并排名或用于候选晋升。

## 3.125行矩阵如何构成

### 3.1矩阵维度

|维度|取值|数量|
|---|---|---:|
|target receiver|`20-1`,`3-19`,`7-14`,`7-7`,`8-8`|5|
|seed|`713101`,`713102`,`713103`,`713104`,`713105`|5|
|K-shot|`1,2,5,10,20`|5|
|方法|ProtoNet CDA、MRIOR-SDA、DADDA-SDA|3|

每种方法的行数为：

\[
5\times5\times5=125
\]

三种方法总行数为：

\[
125\times3=375
\]

每个实验行的唯一比较主键是：

```text
method+target_receiver+seed+K
```

后续加入新方法时，配对主键必须扩展为：

```text
target_receiver+seed+K+support_id_digest+query_id_digest+
scenario+checkpoint_sha256+View策略
```

### 3.2类别集合

6个旧类：

```text
14-10
14-7
20-15
20-19
6-15
8-20
```

本Stage2-B矩阵没有新类：

```text
target_new_tx_labels=[]
```

source receiver：

```text
1-1
1-19
14-7
18-2
19-2
2-1
2-19
```

target receiver：

```text
20-1
3-19
7-14
7-7
8-8
```

source receiver与target receiver互斥。

### 3.3support/query构造

|项目|设置|
|---|---|
|support池上限|每类20个物理样本|
|K|`1,2,5,10,20`|
|query|每类每场景20个|
|query类别数|6个旧类|
|场景数|3|
|单实验行query总量|6×20×3=360|

同一个receiver和seed下，support采用嵌套关系：

```text
K1⊂K2⊂K5⊂K10⊂K20
```

同一个receiver和seed下，5个K使用相同query集合。query从预留20-shot support池之后抽取，因此不会与support重叠。

这个设计意味着125行不是125组完全独立的query。每种方法实际上包含25个`receiver×seed`簇，每个簇在相同query上重复评估5个K。后续总体置信区间应优先使用：

- 按K分别进行25行配对统计；
- 或按`receiver×seed`进行cluster bootstrap；
- 或使用包含receiver随机效应的层级模型。

不能把125行简单视为125个完全独立样本后夸大统计显著性。

### 3.4LEO_weak场景

三种预叠加场景：

```text
leo_clear_weak
leo_low_elev_weak
leo_rain_weak
```

每个场景独立适应、独立预测，最终在实验行中汇总。该矩阵没有5-view TTA、FFT96、60epoch feature adapter或query自适应多View。它评估的是三种域适应方法在三个单场景LEO_weak输入上的基础性能。

## 4.Phase2数据与预测权限

正式行必须满足：

```text
phase2_sample_view_policy=leo_weak_only_no_clean_access
clean_sample_access=false
clean_derived_signal_access=false
phase2_clean_dataset_reachable=false
phase2_clean_cache_reachable=false
phase2_clean_control_flow_reachable=false
phase2_pretrained_artifact_policy=sealed_phase1_checkpoint_only
phase2_source_sample_access=false
phase2_source_cache_access=false
phase2_source_label_access=false
phase2_source_derived_signal_access=false
phase2_source_replay=false
phase2_external_source_adapter_access=false

phase2_query_decision_policy=per_sample_all_registered_classes
phase2_query_role_oracle_access=false
phase2_query_true_batch_class_count_access=false
phase2_query_class_quota_access=false
phase2_query_batch_global_assignment=false

target_query_used_for_training=false
target_query_used_for_model_selection=false
```

预测流程必须分为两个隔离阶段：

1. predictor只读取密封checkpoint、target LEO_weak support、target LEO_weak query、注册类别表和opaque query ID；
2. predictor先生成不可变prediction artifact并记录SHA256；
3. 独立scorer之后才连接truth sidecar；
4. scorer输出不能返回adapter更新、阈值选择、模型选择、回滚、排序或预测。

后续qKNN、adapter、多View和新类注册实验必须沿用同一边界。predictor package成员白名单和运行时访问账本不得出现source dataset、source cache或额外source-derived artifact。不得用query真old/new角色选择不同分支，不得按真实batch类别数量分配预测，不得设置每类query配额，也不得用Hungarian、最优传输或query-query图结构替代逐样本分类。

## 5.基础配置

|参数|值|
|---|---:|
|ADV3B02 feature dim|160|
|support pool max K|20|
|query per TX per scenario|20|
|batch size|128|
|target batch size|64|
|adapt steps per scenario|200|
|场景数|3|
|MRIOR/DADDA总backbone更新|600次/实验行|
|loss trace间隔|20step|
|target CE weight|1.0|
|alignment weight|1.0|
|unknown rejection|关闭|

远端checkpoint：

```text
/home/szu2070436088/2510044040/CV-SincNet/
runs/phase1_adv3_mechanism32_queue_20260701/
ADV3B02_CORE90_SOFT_E200/best_joint_safe_ssdg.pth
```

## 6.三种方法

### 6.1ProtoNet CDA

#### 输入

- 冻结ADV3B02；
- target-old K-shot LEO_weak support；
- target-old LEO_weak query。

#### 方法

对类别\(c\)的K个support特征求均值：

\[
p_c=\frac{1}{K}\sum_{i:y_i=c}f_{\theta}(x_i)
\]

query按欧氏距离选择最近原型：

\[
\hat y=\arg\min_c\lVert f_{\theta}(x)-p_c\rVert_2^2
\]

#### 参数更新与资源

- backbone梯度更新：0；
- adapter更新：0；
- 只计算特征和类别原型；
- 平均时延约0.046秒/实验行。

#### 作用

ProtoNet是最轻的闭式K-shot基线，用于判断ADV3B02原始特征空间是否已经与target receiver对齐。结果显示LEO_weak域偏移下的简单类均值原型通常不可靠。

### 6.2MRIOR-SDA：旧协议source-replay诊断

#### 输入

- 同场景sealed source LEO_weak数据；
- target-old K-shot LEO_weak support；
- ADV3B02 identity backbone。

#### 训练目标

MRIOR使用source监督交叉熵、target support监督交叉熵和DV-KL正则。概念性目标为：

\[
\mathcal L_{\text{MRIOR}}=
\mathcal L_{\text{CE}}^{source}
+\mu\mathcal L_{\text{CE}}^{target}
+\lambda_{\text{DVKL}}\mathcal L_{\text{DVKL}}
\]

其中：

```text
mrior_adapt_learning_rate=0.0006
mrior_estimate_steps=7
mrior_mu=0.5
dvkl_weight=0.005
class_balance_smoothing=0.0
```

每个adapt step先对估计器执行7次上升，再对ADV3B02模型执行1次下降。

#### 参数更新与资源

- 每场景200个模型更新step；
- 三场景共600次ADV3B02 backbone梯度更新；
- 平均时延17.90秒/实验行；
- 更新完整identity backbone；
- 属于重型性能对照，不属于星上极轻型方法。

该方法读取source cache并执行source-target联合优化，因此自2026-07-16起标记为`PROTOCOL_INVALID_FOR_CURRENT_STAGE2_SOURCE_ACCESS`。本节仅记录历史方法和数值，不代表当前正式Stage2允许MRIOR-SDA。

### 6.3DADDA-SDA：旧协议source-replay诊断

#### 输入

- 同场景sealed source LEO_weak数据；
- target-old K-shot LEO_weak support；
- ADV3B02 identity backbone。

#### 训练目标

DADDA同时使用source/target监督分类和分布对齐：

\[
\mathcal L_{\text{DADDA}}=
\mathcal L_{\text{CE}}^{source}
+\lambda_t\mathcal L_{\text{CE}}^{target}
+\lambda_a(\mathcal L_{\text{MMD}}+
\alpha\mathcal L_{\text{LMMD}})
\]

其中：

```text
dadda_adapt_learning_rate=0.0001
dadda_momentum=0.9
dadda_weight_decay=0.0005
target_ce_weight=1.0
alignment_weight=1.0
```

MMD对齐全局`z_id`分布，LMMD对齐类别条件下的ID子特征，动态alpha控制全局与局部对齐强度。

#### 参数更新与资源

- 每场景200个模型更新step；
- 三场景共600次ADV3B02 backbone梯度更新；
- 平均时延14.62秒/实验行；
- 更新完整identity backbone；
- 属于重型比较方法。

该方法依赖source CE、MMD和LMMD，必须同时读取source与target support。它不满足当前Stage2无源域数据协议，只能保留为历史诊断。

## 7.总体性能

总体结果按每方法125个`receiver×seed×K`实验行汇总。

|方法|before old_acc|after old_acc|95%CI|平均增益|胜/负任务|平均时延|backbone更新|
|---|---:|---:|---:|---:|---:|---:|---:|
|MRIOR-SDA|73.60%|**82.58%**|81.06%–84.09%|**+8.98pp**|105/20|17.90s|600|
|DADDA-SDA|73.60%|78.35%|76.79%–79.91%|+4.75pp|99/26|14.62s|600|
|ProtoNet CDA|73.60%|66.85%|64.49%–69.20%|−6.75pp|10/115|0.046s|0|

结论：

- MRIOR是旧source-access协议三方法中的准确率上界；
- DADDA在旧协议下整体正收益，但比MRIOR低4.23pp；
- ProtoNet计算极轻，却比直接ADV3B02低6.75pp；
- “低计算量”不能单独证明适应有效，至少必须同时报告相对direct ADV3B02的配对收益。

## 8.不同K值性能

每个K包含5个receiver×5个seed，共25行。由于query固定，direct ADV3B02的before old accuracy在五个K下均为73.60%。

|方法|K|before old_acc|after old_acc|平均增益|胜/负|平均时延|
|---|---:|---:|---:|---:|---:|---:|
|MRIOR-SDA|1|73.60%|77.22%|+3.62pp|18/7|17.78s|
|MRIOR-SDA|2|73.60%|79.51%|+5.91pp|19/6|17.65s|
|MRIOR-SDA|5|73.60%|82.59%|+8.99pp|22/3|17.71s|
|MRIOR-SDA|10|73.60%|85.82%|+12.22pp|22/3|18.35s|
|MRIOR-SDA|20|73.60%|87.74%|+14.14pp|24/1|18.00s|
|DADDA-SDA|1|73.60%|74.94%|+1.34pp|15/10|14.34s|
|DADDA-SDA|2|73.60%|76.14%|+2.54pp|17/8|14.58s|
|DADDA-SDA|5|73.60%|78.19%|+4.59pp|21/4|14.66s|
|DADDA-SDA|10|73.60%|80.31%|+6.71pp|22/3|14.86s|
|DADDA-SDA|20|73.60%|82.16%|+8.56pp|24/1|14.65s|
|ProtoNet CDA|1|73.60%|58.67%|−14.93pp|0/25|0.043s|
|ProtoNet CDA|2|73.60%|64.70%|−8.90pp|1/24|0.057s|
|ProtoNet CDA|5|73.60%|68.98%|−4.62pp|2/23|0.056s|
|ProtoNet CDA|10|73.60%|70.42%|−3.18pp|3/22|0.056s|
|ProtoNet CDA|20|73.60%|71.48%|−2.12pp|4/21|0.021s|

K值增加后，MRIOR和DADDA持续受益。ProtoNet只是逐步减小负迁移，K=20仍低于direct ADV3B02。后续轻量方法尤其需要解决K=1和K=5下的目标域对齐，而不是只在K=20依赖support数量。

## 9.不同target receiver性能

下表中每个方法×receiver包含5个seed×5个K，共25行。

|target receiver|direct ADV3B02|MRIOR-SDA|DADDA-SDA|ProtoNet CDA|
|---|---:|---:|---:|---:|
|`20-1`|64.61%|83.43%|75.91%|60.98%|
|`3-19`|60.33%|69.06%|65.31%|48.50%|
|`7-14`|90.06%|89.93%|89.93%|83.53%|
|`7-7`|80.22%|86.78%|82.49%|74.81%|
|`8-8`|72.78%|83.69%|78.10%|66.42%|

主要问题：

- `3-19`是最困难的target receiver，MRIOR适应后仍只有69.06%；
- `20-1`的域偏移大，但MRIOR获得+18.82pp，说明support中存在可利用的目标域信号；
- `7-14`的direct基线已经约90%，MRIOR和DADDA平均下降0.12pp；
- 后续轻量适配必须加入只基于support的trust gate或回退机制，防止高基线receiver过度适应；
- 逐receiver聚合不能低于MRIOR，是当前项目对轻量候选的正式门禁之一。

## 10.不同LEO_weak场景性能

|场景|direct ADV3B02|MRIOR-SDA|DADDA-SDA|ProtoNet CDA|
|---|---:|---:|---:|---:|
|`leo_clear_weak`|75.90%|85.99%|81.15%|70.08%|
|`leo_low_elev_weak`|72.53%|81.29%|77.39%|65.99%|
|`leo_rain_weak`|72.37%|80.45%|76.50%|64.48%|

rain和low elevation场景的特征破坏更明显。自适应多View应优先由逐样本低置信度、margin不足、视图分歧或support稳定性触发，而不能默认对全部query执行5-view。

## 11.不同旧类性能

逐类均值覆盖5个receiver×5个seed×5个K×3个场景，共375个类别单元/方法。

|旧类|MRIOR-SDA|DADDA-SDA|ProtoNet CDA|
|---|---:|---:|---:|
|`14-10`|72.83%|65.49%|57.32%|
|`14-7`|68.13%|60.72%|57.29%|
|`20-15`|91.44%|82.47%|74.68%|
|`20-19`|75.91%|78.19%|54.77%|
|`6-15`|93.31%|91.16%|74.75%|
|`8-20`|93.85%|92.07%|82.28%|

`14-7`、`14-10`和`20-19`是主要弱类。MRIOR总体达到82.58%，但最低类别均值只有68.13%，远低于当前项目要求的K10最低旧类88%。后续算法不能只追求总体均值，还必须针对弱类报告：

```text
min_old_class_acc
receiver×class accuracy
scenario×class accuracy
worst-row min_old_class_acc
```

现有`per_run_results.csv`没有直接保存同row的`min_old_class_acc`字段，只能从详细score table重新聚合。这是下一轮正式比较前必须修复的结果schema缺口。

## 12.当前项目成功门槛

以下门槛来自2026-07-15版`项目.md`，优先于历史口头目标和旧报告。

### 12.1K10绝对性能门槛

|指标|正式目标|
|---|---:|
|Stage2-B/Stage2-C旧类总体`old_acc`|≥92%|
|最低旧类`min_old_class_acc`|≥88%|
|5个新类`seen_new_acc`|≥92%|
|10个新类`seen_new_acc`|≥90%|
|20个新类`seen_new_acc`|≥86%|

Stage2-C必须在同一实验行中同时达到旧类、新类和`H_old_new`要求。不能把一个实验的old_acc和另一个实验的new_acc拼接为结果。

### 12.2K5稳健性

K5复用对应K10的前5个物理support、完全相同query和同一锁定candidate。相对K10：

```text
old_acc下降≤3pp
min_old_class_acc下降≤3pp
seen_new_acc下降≤3pp
H_old_new下降≤3pp
```

不得为K5重新选择adapter、epoch、阈值、View门限或超参数。

### 12.3K1强制压力门槛

K1必须复用K10的第1个物理support和相同query。必须报告：

```text
old_acc_before_increment
old_acc_after_increment
old_adaptation_gain
average_forgetting
delta_vs_direct_ADV3B02_K1
```

正式要求：

- 总体和每个target receiver的`old_adaptation_gain>=0`；
- 总体`delta_vs_direct_ADV3B02_K1>=+2pp`；
- matched row配对95%CI下界大于0；
- 每个target receiver的`delta_vs_direct_ADV3B02_K1>=0`。

旧协议MRIOR-SDA的K1 old accuracy为77.22%，但该方法使用source replay，该数值不再定义当前正式门槛。当前K1正式比较以direct ADV3B02、identity-only单qKNN和其他无源域数据的target-support-only基线为准。

### 12.4分K遗忘门槛

固定遗忘锚点：

```text
K∈{1,5,10,20}
```

同一K、同一query和同一View策略下：

\[
\text{average forgetting}(K)=
\text{old acc before increment}(K)-
\text{old acc after increment}(K)
\]

\[
\text{old adaptation gain}(K)=
-\text{average forgetting}(K)
\]

还应汇总：

```text
worst_K_forgetting=max_K average_forgetting(K)
mean_positive_forgetting=mean_K max(average_forgetting(K),0)
```

### 12.5无源域数据matched基线门槛

对每个`K∈{1,5,10,20}`，轻量候选的Stage2-B old_acc，以及Stage2-C的old_acc、seen_new_acc和H_old_new，必须与以下无源域数据基线配对：

```text
direct ADV3B02
identity-only单qKNN
ProtoNet CDA
预登记的target-support-only轻量适应基线
```

正式候选至少满足：

- K1相对direct ADV3B02总体提高至少2pp，paired 95%CI下界大于0；
- K5/K10/K20的遗忘不高于identity-only单qKNN；
- 每个target receiver聚合不得低于direct ADV3B02；
- 在满足绝对准确率门槛后，再按准确率、最低类、遗忘、时延、显存和持久状态进行Pareto比较。

资源门槛直接采用项目统一绝对约束，不再根据旧MRIOR-SDA的600次更新或17.90秒时延按比例推导。

## 13.后续候选方法必须保持相同的内容

### 13.1必须完全一致

```text
ADV3B02 checkpoint及SHA256
target receiver
seed
K
旧类集合
support物理sample ID
query物理sample ID
LEO_weak场景
satellite seed
query View策略
query决策类别集合
独立prediction/scorer边界
```

### 13.2允许变化但必须显式记录

```text
adapter类型与更新层
adapter rank
可训练参数
学习率
epoch
optimizer step
support-only增强View数
query自适应View触发规则
FFT维度与融合权重
原型构建方法
qKNN邻居数和距离度量
support-only trust gate
回退规则
```

### 13.3禁止变化

```text
禁止使用query标签或query真实角色
禁止读取query真实类别数量
禁止类别配额
禁止Hungarian/OT/global assignment
禁止query-query dense graph
禁止使用query选择epoch、阈值或candidate
禁止clean样本和clean派生信号
禁止source样本、source cache和source标签
禁止source feature/logit/prototype/covariance/BN统计
禁止source replay和source-target联合损失
禁止Phase1 checkpoint之外的source adapter或ground adapter
禁止按old/new角色应用不同adapter或权重
禁止跨seed、跨K、跨新类规模拼接最优值
```

## 14.建议的后续启动顺序

### 14.1第一阶段：正式Stage2-B K10配对

先使用相同target support/query ID重新生成无source成员的25个K10 predictor package，并运行候选：

```text
5 receiver×5 seed×K10=25行/候选
```

启动条件：

- 完全复用已封存的target support/query ID；
- 使用相同三个LEO_weak场景；
- package与运行时访问账本不包含任何source数据或source-derived artifact；
- 锁定一套候选和超参数；
- query默认1-view；
- 额外View只能由预注册的逐样本自适应门控触发；
- 输出同row old_acc、min_old_class_acc和资源字段。

必须对比：

```text
direct ADV3B02
identity-only单qKNN
ProtoNet CDA
target-support-only轻量适应基线
候选方法
```

旧MRIOR-SDA和DADDA-SDA只在单独的“旧协议source-replay诊断”表中展示，不参与当前Stage2候选排序。

### 14.2第二阶段：Stage2-B完整125行

K10通过后扩展到：

```text
K∈{1,2,5,10,20}
5 receiver×5 seed×5 K=125行/候选
```

主判断：

- K1是否显著优于direct ADV3B02；
- K1是否无负迁移；
- K5相对K10是否稳健；
- K10是否达到92%/88%绝对门槛；
- K20是否仍有收益或已经饱和；
- 弱receiver和弱类是否改善；
- 平均和P95 View计算是否满足资源预算。

### 14.3第三阶段：Stage2-C新类注册

Stage2-B通过不代表新类注册通过。Stage2-C必须使用预先锁定的嵌套真实新类集合：

```text
Y_new^5⊂Y_new^10⊂Y_new^20
```

推荐先运行K10确认矩阵：

```text
5 receiver×5 seed×3新类规模
=75行/方法
```

锁定候选后，再加入K1、K5和K20：

```text
5 receiver×5 seed×4 K×3新类规模
=300行/方法
```

每个Stage2-C实验行必须同时输出：

```text
old_acc_before_increment
old_acc_after_increment
old_adaptation_gain
average_forgetting
min_old_class_acc
seen_new_acc
H_old_new
old_to_new_confusion
new_to_old_confusion
```

历史MRIOR-SDA和DADDA-SDA不能代替Stage2-C无源域数据基线。正式新类比较必须使用相同新类集合、support/query ID、K、seed、receiver和View策略，重新运行target-support-only基线。

## 15.建议的实验命名

Stage2-B：

```text
<method>_stage2b_rx<receiver>_k<K>_seed<seed>_<view_policy>_<protocol>
```

Stage2-C：

```text
<method>_stage2c_rx<receiver>_k<K>_new<N>_seed<seed>_<view_policy>_<protocol>
```

例：

```text
qknn_lite_stage2b_rx20-1_k10_seed713101_adaptview135_leoweak_v1
qknn_lite_stage2c_rx20-1_k10_new5_seed713101_adaptview135_leoweak_v1
```

## 16.候选配置登记模板

```yaml
experiment_id:
candidate_id:
stage:
protocol_version:

base_model: ADV3B02
checkpoint_path:
checkpoint_sha256:
feature_dim: 160

target_receivers:
  - 20-1
  - 3-19
  - 7-14
  - 7-7
  - 8-8

seeds:
  - 713101
  - 713102
  - 713103
  - 713104
  - 713105

k_grid:
  - 1
  - 5
  - 10
  - 20

old_tx_labels:
  - 14-10
  - 14-7
  - 20-15
  - 20-19
  - 6-15
  - 8-20

new_tx_set_id:
new_class_count:

adapter_type:
adapter_layers:
adapter_rank:
trainable_parameters:
learning_rate:
epochs:
optimizer_steps:
optimizer:

support_view_policy:
query_view_policy:
adaptive_view_trigger:
mean_backbone_forwards:
p95_backbone_forwards:

fft_dimension:
fft_weight:
prototype_method:
qknn_k:
distance_metric:
trust_gate:
fallback_policy:

phase2_sample_view_policy: leo_weak_only_no_clean_access
clean_sample_access: false
clean_derived_signal_access: false
phase2_clean_dataset_reachable: false
phase2_clean_cache_reachable: false
phase2_clean_control_flow_reachable: false
phase2_source_sample_access: false
phase2_source_cache_access: false
phase2_source_label_access: false
phase2_source_derived_signal_access: false
phase2_source_replay: false
phase2_external_source_adapter_access: false
phase2_pretrained_artifact_policy: sealed_phase1_checkpoint_only

phase2_query_decision_policy: per_sample_all_registered_classes
phase2_query_role_oracle_access: false
phase2_query_true_batch_class_count_access: false
phase2_query_class_quota_access: false
phase2_query_batch_global_assignment: false
target_query_used_for_training: false
target_query_used_for_model_selection: false
```

## 17.逐行结果登记模板

|字段|说明|
|---|---|
|experiment_id|实验行唯一ID|
|candidate_id|候选方法|
|stage|Stage2-B或Stage2-C|
|receiver|target receiver|
|seed|确认seed|
|K|物理support/类|
|new_class_count|0/5/10/20|
|scenario|LEO_weak场景|
|support_id_digest|support物理ID摘要|
|query_id_digest|query物理ID摘要|
|checkpoint_sha256|基座checkpoint|
|old_acc_before_increment|新类注册前旧类准确率|
|old_acc_after_increment|新类注册后旧类准确率|
|old_adaptation_gain|注册后旧类−注册前旧类|
|average_forgetting|注册前旧类−注册后旧类|
|min_old_class_acc|同row最低旧类准确率|
|seen_new_acc|已注册新类准确率|
|H_old_new|旧类与新类调和均值|
|delta_vs_direct|相对直接ADV3B02|
|delta_vs_identity_qknn|相对identity-only单qKNN|
|delta_vs_target_support_baseline|相对预登记target-support-only基线|
|trainable_parameters|实际可训练参数|
|optimizer_steps|实际优化步数|
|adaptation_latency_sec|端到端适配时延|
|peak_memory_mib|峰值显存|
|persistent_state_bytes|持久状态|
|mean_backbone_forwards|平均backbone前向|
|p95_backbone_forwards|P95前向|
|protocol_audit|PASS/FAIL|
|prediction_sha256|不可变预测artifact摘要|
|verdict|PROMOTABLE/DIAGNOSTIC/FAILED|

建议CSV最少包含：

```text
experiment_id,candidate_id,stage,receiver,seed,k_shot,new_class_count,
scenario,support_id_digest,query_id_digest,checkpoint_sha256,
old_acc_before_increment,old_acc_after_increment,old_adaptation_gain,
average_forgetting,min_old_class_acc,seen_new_acc,h_old_new,
delta_vs_direct,delta_vs_identity_qknn,delta_vs_target_support_baseline,
trainable_parameters,optimizer_steps,
adaptation_latency_sec,peak_memory_mib,persistent_state_bytes,
mean_backbone_forwards,p95_backbone_forwards,protocol_audit,
prediction_sha256,verdict
```

## 18.结果统计规范

### 18.1配对比较

候选与direct ADV3B02、identity-only单qKNN、ProtoNet CDA及预登记target-support-only基线必须在同一实验主键上计算差值：

\[
\Delta_i=
\text{metric}_{candidate,i}-
\text{metric}_{baseline,i}
\]

禁止比较两个矩阵的独立总体均值后宣称配对提升。

### 18.2必须报告的统计

```text
paired mean delta
paired median delta
paired 95%CI
wins/losses/ties
receiver-level delta
K-level delta
scenario-level delta
class-level delta
worst receiver
worst old class
worst K forgetting
```

### 18.3晋升判定顺序

1. 协议审计PASS；
2. checkpoint、support/query ID和View策略完全匹配；
3. K10绝对准确率门槛；
4. K1无负迁移且显著高于direct ADV3B02；
5. K5稳健性；
6. 分K遗忘；
7. Stage2-C新类与H指标；
8. 相对无源域数据matched基线的配对提升；
9. 参数、step、时延、显存、持久状态和View资源门槛。

任一协议门禁失败时不得进入性能排名。性能通过但资源失败时只能标记为`Pareto candidate`或`NON_DEPLOYMENT_DIAGNOSTIC`，不能声明满足星上部署目标。

## 19.当前结论与下一步

2026-07-15历史125实验说明：

- ADV3B02直接旧类准确率为73.60%；
- source-replay MRIOR提供最高历史平均旧类性能，但计算重且不满足当前Stage2无源域数据协议；
- source-replay DADDA整体正收益，但同样不满足当前Stage2协议；
- ProtoNet极轻，却在大多数实验行中负迁移；
- 仅靠原始ADV3B02特征均值原型不足以解决LEO_weak目标域对齐；
- 后续重点应放在少参数关键层适配、support-only稳健对齐、弱类保护和逐样本自适应多View；
- 所有轻量候选必须同时完成Stage2-B旧类适应和Stage2-C新类注册，不能只报告注册前旧类准确率。

建议下一次实验复用2026-07-15矩阵的target support/query ID，但重新生成不含source成员的predictor package，先运行25个K10行。候选锁定后扩展到完整125行，再构建5/10/20新类的Stage2-C matched矩阵。

## 20.验证记录

本文件基于以下当前证据生成：

- `项目.md`版本2026-07-16；
- 正式配置`adv3b02_stage2b_three_da_leo_weak_only_20260715_n607.json`；
- `per_run_results.csv`的375行复算；
- 三种方法各125行；
- receiver、seed和K集合完整；
- `audit.json`确认`artifact_complete=true`、`errors=[]`；
- 总体、逐K、逐receiver、逐场景和逐类数值与正式summary一致。

量化内容采用Markdown表而未增加图表，原因是本文件的首要用途是后续实验配置查找、逐行配对和启动审计，精确值与字段完整性比趋势可视化更重要。

验证结论：`Ready to share`。

已知缺口：当前三方法summary未统一保存可训练参数、峰值显存、持久状态和同row最低类字段；后续正式比较前必须补齐。
