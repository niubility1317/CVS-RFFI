# CVS 项目场景与数据协议

版本：2026-07-17
适用范围：`E:\type10-7` 中 CVS-RFFI / CV-SincNet 的科研叙事、算法优化、实验矩阵、数据协议、自动化控制面、N607 运行设计、论文/汇报写作与结果解释。

## 1. 文件地位

本文件是 CVS 项目的场景与数据协议源文件。后续任何涉及 CVS 研究方向、实验设计、候选矩阵、数据划分、Stage2-A/B/C 解释、论文叙事、优化路线、自动化决策或指标口径的工作，都必须先读取并遵守本文件。

`AGENTS.md` 仍然拥有工程安全、N607 访问、环境、报告、版本管理和远端同步规则的最高优先级。本文件拥有 CVS 科研场景、数据协议、任务边界和声明口径的优先级。若本文件与旧报告、旧记忆、旧 prompt、旧矩阵或历史 launcher 冲突，以本文件为准；若本文件与 `AGENTS.md` 冲突，以 `AGENTS.md` 为准，并在报告中记录冲突。

## 2. 项目主场景

CVS 面向的主场景是：

> 天基射频指纹识别中的弱标注跨接收机域泛化与在轨跨域少样本适应。

这个场景由四个约束共同定义：

1. **星上算力受限**：卫星端难以开展完整模型训练。项目采用“地面训练、天上部署”架构。地面端完成主干训练、模型选择并封存Phase1 ADV3B02 deployment bundle；Stage2只允许基于当前target support进行推理、原型注册、轻量校准、阈值微调或小adapter更新。经2026-07-17用户特许，该不可分割bundle可额外携带仅由Phase1/offline生成的域×类int8聚合原型及其量化尺度；不得携带原始IQ、样本级原始/全精度feature、exemplar、source cache、可逆样本索引、source统计或额外source adapter。
2. **身份标签极少但域标签丰富**：广域接收能产生大量 raw IQ，但发射机身份标签难以可靠获得。地面训练阶段 TX 身份标注比例低于 `0.1`；无 TX 标签样本可以大量存在，并携带 receiver、day、rx_day、观测窗口、信道场景或其他 domain label。
3. **星地信道破坏发射机特征**：星地链路中的残余 Doppler/CFO、相位噪声、低 SNR、低仰角、弱多径和弱 Rician/shadowed-Rician fading 会破坏 raw IQ 中的发射机相关细节。路径损耗和绝对 slant range 默认只用于 metadata/link budget，不得作为强幅度捷径直接主导 IQ；IQ imbalance 和接收链路差异归入 `R_d` 接收机响应，不再作为默认传播信道 `H_d` 扰动。Phase2数据入口只接收已叠加`LEO_weak`信道的样本，接触不到clean样本。
4. **Phase2主线是在轨目标域少样本适应与新类学习**：部署后，卫星接收机可能获得少量旧类带标签目标域样本，也可能获得少量新辐射源带标签目标域样本。旧类是地面训练已知 TX；新类必须是地面训练未见 TX。Phase2主线必须使用同一个目标卫星接收机域 `R_t` 中叠加简化LEO星地信道后的旧类样本和新类样本，完成目标域适应、旧类校准和新类学习。

项目不应被表述为普通 WiSig 少样本分类、普通全监督域泛化、纯 few-shot learning，或真实卫星部署已完成验证。

## 3. 核心科学问题

CVS 要回答的问题是：

> 在身份标签极少、无标签信号大量存在、接收机域变化强、星地信道破坏严重、部署后还会出现新辐射源的条件下，如何先在地面学习稳定的发射机身份表征，再在星上利用叠加LEO星地信道的少量目标域旧类样本和新类样本完成跨域适应、旧类校准和新类学习。

该问题的核心矛盾是 identity-style conflict：发射机硬件指纹、接收机响应、日期漂移、采集域差异和星地信道扰动在 raw IQ 中纠缠。CVS 的价值不在于堆叠多个 DG trick，而在于用物理先验和身份-域双表征控制这种纠缠。

未知类拒识、open-set / open-world 学习和 unknown FAR 优化自 2026-07-07 起下沉为 Phase3 备用项。它们可以作为安全扩展或诊断项保留，但不得作为 Phase2 主线成功门槛、Phase2 主线优化目标或论文主线结论。

## 4. 符号与集合定义

设接收 IQ 样本为 `x`，发射机身份为 `y`，域标签为 `d`。天基 RFFI 观测可抽象为：

```text
x = R_d( H_d * T_y(s) ) + n
```

其中：

- `T_y`：发射机硬件非理想性，是应保留的身份来源。
- `H_d`：传播/星地信道，是域扰动来源。
- `R_d`：接收机链路响应，是跨接收机偏移来源。
- `n`：噪声。

接收机集合：

```text
R_s = {r_1, ..., r_m}        # 地面/source training receivers
R_t = {r_a, ..., r_b}         # target receiver domain / deployment proxy domain
intersection(R_t, R_s) = empty
|R_t| >= 1
```

`R_t` 是第二阶段的 target receiver domain `R_t`。`R_t` and `R_s` must be disjoint；single `r_sat` is allowed but not mandatory。关键不是强制单接收机，而是保证 target receiver domain 不泄漏到地面训练 receiver 域，并且后续 target-old / target-new / unknown 的 support/query 权限都按 `R_t` 定义。

发射机集合：

```text
Y_old = ground-training transmitter set
intersection(Y_new, Y_old) = empty
intersection(Y_unknown, union(Y_old, Y_new)) = empty
```

严禁把与地面训练 TX 一致的发射机写成“新类”。如果一个 TX 在地面训练中出现过，它在部署阶段只能是旧类 target-old；它的问题是目标域校准或少样本域适应，不是新类注册。

## 5. 地面训练阶段协议

地面训练阶段是 weakly labeled / semi-supervised source-domain DG，不是部署意义上的 few-shot learning。

训练数据分两类：

```text
L_s = {(x_i, y_i, d_i): receiver(x_i) in R_s}
U_s = {(x_j, d_j): receiver(x_j) in R_s, y_j hidden or unavailable}
```

身份标注比例：

```text
rho_label = |L_s| / (|L_s| + |U_s|) <= 0.1
```

推荐实验网格：

```text
rho_label in {0.005, 0.01, 0.02, 0.05, 0.1}
```

### 5.1 Phase1半监督对比矩阵固定协议（2026-07-13）

Phase1中的`CVCNN-CE`、`RIEI-FD`与`DRIFT`各自运行两条互斥的无标签数据利用路线，不把两种机制合并为一个候选：

1. `pseudo_label`路线：先进行149个epoch的有标签监督预热，从epoch150开始对源域无TX标签样本使用置信度门控的硬伪标签交叉熵；
2. `augmentation_consistency`路线：对同一源域无TX标签样本的clean视图与星地强增强视图施加soft prediction consistency，不使用硬伪标签损失。

两条路线统一先从源域全池留出`0.30`作为互斥source validation，再将剩余`0.70`训练池按`0.10/0.90`划分为`L_s/U_s`；相对源域全池的等价比例固定为`0.07/0.63/0.30`，因此`rho_label=0.07/(0.07+0.63)=0.10`。三部分互斥，且不得包含目标接收机域`R_t`。两条路线均默认对有标签训练样本启用clean+satellite双视图星地信道增强，训练与正式测试场景族固定为`leo_clear_weak`、`leo_low_elev_weak`、`leo_rain_weak`。正式checkpoint统一使用第200个epoch的final模型权重；source validation只用于训练健康审计和最终报告，不参与checkpoint选择或回滚，无标签训练指标、test指标和LEO test指标同样不得参与checkpoint选择。

地面阶段不得使用 `R_t` 的任何样本、统计、BN 信息、阈值、prototype、adapter、伪标签、验证结果或 early stopping 信息。只要目标接收机域数据参与训练或模型选择，该结果就不再是 source-only DG，必须单独标为 DA / TTA / few-shot adaptation。

无 TX 标签样本的使用边界：

- 若 `U_s` 的类别空间确认属于 `Y_old`，可使用闭集 semi-supervised learning，例如 Mean Teacher、FreeMatch/UPS、prototype agreement、class quota、receiver quota。
- 若 `U_s` 可能混入 `Y_old` 外发射机，必须使用 open-set SSL / reject 机制；不能把所有无标签样本强制伪标成旧类。
- 无标签样本的 domain label 可用于 `z_dom` 域监督、`z_id` receiver leakage 抑制、采样平衡、伪标签覆盖审计和 episode 组织。

## 6. CVS 地面训练架构

CVS 地面阶段目标是学习跨域稳定的发射机身份空间：

```text
raw IQ -> CV-SincNet/CVS -> z_id, z_dom
```

- `z_id`：保留发射机硬件指纹，用于分类、原型、少样本注册和旧类校准。
- `z_dom`：吸收 receiver、day、rx_day、channel、satellite-style nuisance，用于域诊断、域监督、adapter gate 和泄漏审计。

推荐机制：

| 模块 | 用途 | 边界 |
|---|---|---|
| 物理先验 CV-SincNet | 保留 raw IQ 中的硬件非理想线索 | 不应被替换成无物理解释的纯黑箱 backbone，除非作为 baseline |
| `z_id/z_dom` 解耦 | 分离身份和域因素 | 不能让 `z_dom` 参与 TX prototype 距离 |
| domain-supervised `z_dom` | 利用无 TX 标签样本的域标签 | 域标签不能替代 TX 标签 |
| GRL / leakage probe | 抑制 `z_id` 中的接收机信息 | 过强会抹掉 TX identity，需报告 probe |
| Mean Teacher / FreeMatch / UPS | 利用无 TX 标签样本 | 必须有不确定性、原型一致性和 quota |
| Prototype agreement | 防止 logit shortcut | 原型更新需 class/receiver balance |
| MLDG / episodic source split | 模拟未见接收机外推 | support/query 只能来自源域 |
| satellite strong-view consistency | 提升星地压力鲁棒性 | 只能使用源域派生视图，不能使用目标卫星接收机 |

## 7. 在轨部署阶段协议

在轨阶段面对目标接收机域 `R_t`。`R_t` 可以是一个接收机，也可以是多个接收机组成的 deployment proxy domain；Phase2 target-old 和 target-new 的 support/query 都必须来自 `R_t`，并处于相同定义的 satellite/LEO target view 下。若启用 Phase3 open-set 备用项，unknown query 也必须来自同一个 `R_t`，但它不得反向改写 Phase2 主线门槛。

Phase2 主线样本选择的核心约束：target receiver domain 必须与地面训练接收机域不同，同时 must include target-old samples from `Y_old` and target-new samples from `Y_new`；这些 support/query 样本必须按简化LEO星地信道目标视图构造和报告。其中 target-old 是与地面训练发射机类别相同的旧类，target-new 是与训练发射机类别不同且不在 `Y_old` 中的新类。若 `R_t` 中缺少旧类或新类样本，不能声明完整 Phase2 主线/Stage2-C；相关 row 必须降级为 Stage2-A/B、`LOCAL_DATASET_EXTENSION_REQUIRED`、`LOCAL_PROTOCOL_REPAIR_REQUIRED` 或 `NON_LAUNCH_DIAGNOSTIC`。

### 7.1 Phase2 LEO_weak-only数据可达性硬约束（2026-07-15）

> **最高优先级强约束：整个Phase2链路必须对clean物理不可达。这里的“不可达”不是“最终指标不使用clean”，也不是“target support/query使用LEO但中间模块仍可读取clean”，而是从Phase2启动前的输入校验、数据加载、support/query构造、缓存导入、特征提取、适配、注册、分类、校准、选择、回滚、排名到正式评估的每一个可执行节点，都不得打开、读取、接收、恢复、重构或受控于任何clean样本及clean派生信号。只要clean在任一节点可达、被实际读取，或能够影响任一参数、状态、分数、候选或决策，该Phase2 run即协议无效。**

Phase2的输入边界不是“以LEO视图为主、clean视图为control”，而是`LEO_weak-only`：所有进入Phase2的数据样本，包括Stage2-A/B/C中的target-old、target-new以及可选Phase3-backup unknown的support/query、适配集、校准集、注册集、模型选择集和正式评估集，都必须在进入Phase2边界之前实际叠加`leo_clear_weak`、`leo_low_elev_weak`或`leo_rain_weak`之一。Phase2不得接收原始clean IQ后再在内部临时叠加信道；允许的数据入口只能是已经具有可验证LEO overlay provenance的样本、缓存或特征。Phase2运行时不得读取、缓存、恢复、旁路传入或由同一raw IQ重新构造clean样本、clean特征或clean logit；Phase2算法视角中clean样本及其派生信号必须在文件、dataset、dataloader、cache、API和控制流层面均不可达。

### 7.1.1 Phase2单物理样本单LEO接收观测硬约束（2026-07-17）

> **每个clean/raw物理IQ样本在进入Phase2前必须且只能叠加一次，并且恰好只能叠加一种`leo_*_weak`星地信道状态，形成唯一的接收IQ观测。禁止把同一个clean/raw物理样本分别叠加`leo_clear_weak`、`leo_low_elev_weak`和`leo_rain_weak`后生成多份平行观测、多场景子样本或support，再用于Phase2训练、适配、注册、校准、选择或评估。单颗卫星接收机对一次发射只能得到一个既定LEO星地信道状态下的IQ观测。**

每个scenario row中每类K-shot必须由K个互不重复的独立物理样本构成。例如`K=5`表示该目标域、该scenario row内每类有5个独立物理样本，每个样本各自只有一份已经叠加一种LEO_weak状态的接收IQ；不得用一个clean/raw样本的5个信道副本、5个随机信道实现或5个计算view冒充K5。

Phase2可以且应继续探索对这K份单一独立接收IQ实施接收侧信道均衡、增强、变换、多表征提取、一致性学习或轻量适配。此类操作必须只以已经接收并密封的固定LEO_weak IQ为输入，不得回读clean/raw IQ，不得把该样本重新叠加为另一种LEO星地信道状态，也不得恢复clean参考。由同一接收IQ派生的均衡/增强/变换view共享同一个稳定根`physical_sample_id`、原始scenario、恰好一个已记录的satellite seed值和support/query角色。只有注册support的派生view可以参与adapter/head拟合、一致性学习、原型更新或其他状态更新；query派生view只能用于当前单个query的逐样本推理，不得更新adapter、head、prototype、threshold、temperature、门控参数、候选选择、早停或回滚状态。所有派生view只是同一个物理support/query的计算派生，不是新的独立物理样本，不得增加K。

三场景正式覆盖必须由独立接收样本单元实现，而不是同一物理样本的三种信道副本。定义`U(scenario)=S_support(scenario)∪S_query(scenario)`；对同一matched receiver×seed×K×new规模，三个场景的全角色物理样本ID并集必须两两不交，同一scenario内support与query也必须不交：

```text
U(clear) ∩ U(low_elev) = empty
U(clear) ∩ U(rain) = empty
U(low_elev) ∩ U(rain) = empty
S_support(scenario) ∩ S_query(scenario) = empty
```

物理样本必须先被分配到唯一scenario，再执行一次LEO_weak overlay并密封；不得先复制clean/raw样本再分别叠加多个scenario。稳定根`physical_sample_id`必须在overlay前绑定不可重命名的原始记录身份。WiSig主线必须至少绑定`dataset_artifact_sha256 + original WiSig coordinate(tx_i,rx_i,day_i,eq_i,sig_i)`或等价的预封存lineage token；cap后局部rank、`Subset`局部index、角色名、显示TX label、文件名、缓存行号和随机token不得作为改变根身份的依据，`source_record_index`只能作为额外审计字段，不能单独决定根ID。三个scenario cache必须继承同一根ID命名空间，禁止通过改写角色、TX字段、局部索引或文件名给同一raw记录换ID。Phase2密封包不得携带原始dataset路径，但必须携带根ID的不可逆token及离线authority对根ID生成规则和唯一分配的签名/哈希证据。每个密封样本必须具有唯一稳定根ID、唯一`target_channel_view`、恰好一个已记录的satellite seed值和一次overlay provenance。

唯一scenario分配还必须避免把采集day与LEO场景机械绑定。正式WiSig缓存采用按`TX×day`分层的确定性互斥分配：每个TX的候选物理样本先按稳定根排序，再在每个day内均衡分配给三个LEO_weak场景；每个scenario仍须得到完全相同的每TX物理样本数，任一day在三个scenario间的样本数差不得超过1。禁止使用按原始行号连续切片的`0:40/40:80/80:120`方式，使clear/low/rain分别对应不同day块。正式assignment策略记录为`physical_sample_scenario_assignment_policy=disjoint_preoverlay_tx_day_stratified_v1`，并由post-build validator重算TX×day计数、根ID互斥和assignment SHA。

每个launchable Phase2 row及最终artifact还必须显式记录：

```text
phase2_physical_sample_observation_policy=single_leo_weak_observation_per_physical_sample
phase2_cross_scenario_physical_sample_reuse=false
phase2_additional_leo_channel_state_generation=false
phase2_post_reception_equalization_augmentation_transform_allowed=true
phase2_post_reception_view_from_fixed_received_iq_only=true
phase2_post_reception_view_counts_as_additional_physical_sample=false
phase2_physical_sample_root_id_policy=immutable_preoverlay_lineage_token
phase2_query_post_reception_view_fit_access=false
```

密封package的pre-open validator必须在读取IQ payload前验证上述字段、稳定根ID证据、逐样本唯一overlay provenance、同场景support/query不重叠以及跨场景全角色物理ID并集互斥。若实际使用接收后view，还必须逐view记录并核验`parent_received_iq_sha256`、`operator_id`、`view_seed`、`post_reception_view_used`和`post_reception_view_count`；合法operator只能读取固定sealed IQ，不得调用LEO channel simulator，不得生成新的`target_channel_view`或overlay provenance。任一字段缺失、同一稳定根ID对应多个LEO状态/overlay、跨场景复用同一稳定根ID、query view参与拟合、或把接收后计算view计作独立物理样本时，必须标记`LOCAL_PROTOCOL_REPAIR_REQUIRED`并阻断matrix、runner、promotion和正式声明。已确认使用同一物理样本多LEO状态平行观测的历史artifact统一封存为`PROTOCOL_INVALID_FOR_PHASE2_SINGLE_OBSERVATION`，不得参与当前候选选择、超参数锁定、正式排名、Pareto比较或性能门槛。

### 7.1.2 合法Phase2数据一次准入、密封保存与复用（2026-07-17）

为避免每个adapter、注册方法、对照方法或矩阵row重复打开Phase1/offline数据并反复执行同一套前期核查，允许且要求采用“一次完整准入审计、一次密封、Phase2内多次快速复用”的数据边界。clean/raw dataset、cache build spec、真实TX/receiver坐标、稳定物理ID和support/query选择信息只能由Phase1/offline controller访问；controller完成完整合法性审计后，应把该row实际允许进入Phase2的固定LEO_weak接收IQ保存为不可变`Phase2 data capsule`。capsule一旦通过准入，即视为正式Phase2数据样本入口，后续方法不得绕过capsule重新访问原始cache或dataset。

首次准入审计必须至少一次性证明：actual cache及其channel/assignment roots与签名authority一致；每个support/query IQ逐行属于已验证LEO_weak cache；每个物理样本只有一个LEO接收观测；receiver、scenario、TX、old/new注册语义、class index、K-shot rank和satellite seed正确；同场景support/query物理ID不重叠；三场景全角色物理ID两两不交；support/query成员不含clean/raw、truth、role、quota或global-assignment输入。任何一项未证明时不得生成可复用capsule。

一个可复用capsule必须同时保存并绑定：

```text
phase2_data_capsule_policy=offline_verified_once_sealed_reusable
phase2_data_capsule_immutable=true
phase2_data_capsule_raw_dataset_reachable=false
phase2_data_capsule_clean_reachable=false
phase2_data_capsule_build_spec_reachable=false
phase2_data_capsule_member_allowlist_enforced=true
phase2_data_capsule_fast_preopen_revalidation=true
```

capsule的不可变manifest和签名authorization必须绑定receiver、seed、scenario集合、K、before/after registration state、registered class registry、package root、member allowlist、每个IQ member SHA、实际selected physical/overlay/membership roots、support/query disjointness状态、single-observation contract、Phase1 sealed runtime SHA、method lock SHA和协议schema版本。原始dataset路径、clean/raw member、cache build spec、source record坐标、可逆物理ID或离线私钥不得进入capsule或Phase2 predictor输入。

完成一次准入并封存后，后续D18、identity-only single-qKNN及其他对照方法可以直接复用同一capsule。每次启动只需要在打开IQ前快速校验：detached seal、package/member SHA、签名authorization、exact schema、registered registry、K/state绑定和runtime access audit；不得再次回到raw/clean dataset，也不要求每层重新运行完整dataset membership扫描。完整准入证据、逐样本合法TX/receiver/support-query清单和membership audit保存在Phase1/offline evidence目录，Phase2只接收其path-free签名根。

capsule的复用范围必须精确：同一receiver×seed×scenario assignment×K×new-class scale×registration state×protocol schema可以跨方法复用；K、类别集合/顺序、support/query选择、任一IQ字节、scenario分配、Phase1 runtime、method lock、member allowlist或协议schema发生变化时，旧capsule立即失效，必须重新执行离线准入和封存。由固定接收IQ生成的合法接收后view可以在capsule内按operator/parent hash记录并复用，但不得新增物理样本、改变K或生成新的LEO信道状态。

support-only capsule只授权适配/注册状态生成，必须记录`formal_metric_claim_allowed=false`和`support_query_disjointness_status=SUPPORT_ONLY_NO_QUERY_CLAIM`；它不能单独产生正式性能声明。只有query capsule、密封逐样本prediction artifact及隔离scorer完成，并且support/query disjointness证据为PASS后，才允许正式metric claim。这样既避免重复前期核查，又不把“数据准入完成”“support适配完成”和“正式性能评估完成”混为一谈。

因此，Phase2统一禁止：

- 把clean样本，或由clean样本提取、拟合、统计、蒸馏、校准得到的feature、logit、prototype、teacher、anchor、loss target、normalization statistics、adapter/head参数、阈值、bias、temperature、support选择分数、cache、sidecar或其他decision signal，用于adapter更新、prototype构建、head拟合、温度/偏置/阈值校准、模型选择、早停、TTA门限确定、回滚判定、候选排序、消融对照或正式指标计算。
- 将clean与`LEO_weak`混合、拼接、配对、一致性约束、teacher-student蒸馏、replay或source-anchor后，仍把该row标为正式Phase2候选。Phase2不得读取任何source anchor；唯一窄例外是第7.3.1节定义、已与checkpoint共同封存的域×类int8聚合原型模型知识。
- 先在clean样本上适配/选参，再只在`LEO_weak`上报告，或用clean结果触发是否追加TTA、是否回滚、是否晋升。
- 把`clean_view_role=control_only`解释为Phase2仍可接触clean control。该字段对Phase2必须升级为`clean_sample_access=false`；clean control仅可在Phase1或与Phase2完全隔离的离线参考流程中单独运行，不得向Phase2训练、适配、选择或评估过程提供任何输入或决策信号。
- 仅用`uses_target_clean=false`证明合规。该字段只能排除target clean，不能排除source clean、clean teacher、clean prototype、clean preservation loss、clean cache或其他clean-derived signal；因此不能单独作为Phase2无clean访问证明。

Phase1/Phase2边界只允许一个明确例外：在任何target support/query可达之前已经完成训练、量化、冻结、登记并整体封存的Phase1 deployment bundle，可以作为Phase2不可变模型入口。bundle主体为ADV3B02 checkpoint，可选包含第7.3.1节定义的域×类int8聚合原型。该例外不允许Phase2回读Phase1 clean/source训练样本，不允许加载原始/全精度样本feature、teacher、cache、normalization sidecar或独立source prototype bank，不允许为了Phase2适配、选参或评估重新运行clean/source分支，也不允许把Phase2期间生成的新artifact伪装成既有Phase1 bundle。除该授权的压缩模型知识外，凡是会在Phase2期间参与计算或改变决策的辅助artifact，都必须自身满足`LEO_weak-only`可达性约束。

每个launchable Phase2 row及最终artifact必须显式记录：

```text
phase2_sample_view_policy=leo_weak_only_no_clean_access
clean_sample_access=false
clean_derived_signal_access=false
phase2_clean_dataset_reachable=false
phase2_clean_cache_reachable=false
phase2_clean_control_flow_reachable=false
phase2_pretrained_artifact_policy=sealed_phase1_deployment_bundle_with_optional_int8_domain_class_prototypes_v1
phase2_authorized_compressed_prototype_access=true|false
phase2_unapproved_source_derived_signal_access=false
```

同时必须记录实际`leo_*_weak`scenario和satellite seed，并保留sample-level证据证明support/query在进入Phase2前已经叠加信道。验证器必须在打开任何Phase2 dataset、cache或feature artifact之前执行fail-closed可达性检查；不能只相信manifest中的自声明布尔值，还必须核对artifact provenance、生成配置、loader入口和运行命令。任一字段缺失、任一clean可达字段不为false、目标视图包含clean、存在clean-derived signal、或无法证明实际叠加时，必须直接标为`LOCAL_PROTOCOL_REPAIR_REQUIRED`并阻断matrix、runner、promotion和正式声明；不得降格为新生成的可运行clean诊断候选。历史clean接触artifact只能封存为`PROTOCOL_INVALID_FOR_PHASE2`，不能参与当前方法排名、超参数选择、性能门槛或结论。

上述clean不可达字段必须由运行时隔离证据支撑，不能只作为JSON自声明：

- Phase1离线打包器可以读取原始ManySig/ManyTx和cache build spec，但它必须在Phase2边界外完成LEO叠加、support/query预登记与密封打包。Phase2 predictor只能接收密封后的LEO inference package及其manifest，不得接收原始数据路径、cache build spec、clean/raw成员、dataset selector或legacy raw/clean loader参数。
- 密封package的成员列表、路径allowlist、scenario、satellite seed、sample-level overlay provenance和SHA256必须在打开IQ payload之前审计。审计失败时不得读取payload、不得生成prediction，也不得降格运行。
- `phase2_clean_dataset_reachable=false`要求Phase2进程的文件allowlist中不存在原始PKL、clean dataset和其路径别名；`phase2_clean_cache_reachable=false`要求package与sidecar中不存在clean/raw/derived cache；`phase2_clean_control_flow_reachable=false`要求正式CLI、API、import入口和环境变量不能切换到legacy raw/clean loader。
- candidate lock和prediction manifest必须绑定密封package、strict loader、path/member allowlist和pre-open audit的代码/配置哈希；运行结束后还必须保存实际打开文件集合或等价访问审计摘要。只有这些证据与四项不可达字段一致，才能标记为`PROTOCOL_VALID`。
- 历史artifact若只是缺少当前证据字段且没有直接证据证明发生clean访问，统一标记为`UNVERIFIED_UNDER_CURRENT_PROTOCOL`，不得晋升、排名或形成正式结论；若确认clean样本或clean-derived signal实际可达、被读取或影响决策，则标记为`PROTOCOL_INVALID_FOR_PHASE2`。证据不完整与确认违规必须分开报告。

K-shot 设置不再是硬枚举。每个 Stage2-B/C run 必须显式记录 `K`，且 `K` 必须为正整数。

```text
K >= 1
recommended anchor K in {1, 2, 5, 10, 15, 20, 50}
```

报告口径：

- `{1,2,5,10,15,20,50}` 是推荐锚点，用于跨 run 对齐和画主曲线，不是唯一允许取值。
- 自动化可选择 `3/4/8/12/16/25/30` 等中间值或任务驱动值，但必须记录 `k_shot`、选择理由和报告分层。
- `K<=20` 可作为 few-shot / low-shot 区间报告。
- `K>20` 应写作 higher-shot、medium-shot 或 saturation point，不能把所有结论都笼统称为 strict few-shot。
- 每个Phase2 row在其进程可达的密封support package、成员allowlist和loader输出中，必须对每个已注册类严格只暴露`K`个互不重复的物理support sample ID。不得在`K=1/5/10`包中隐藏或物理可达更大的`K=10/20/50`support池，再仅靠索引、mask、manifest自声明或算法自律选择前K个；额外support可达即为`PROTOCOL_INVALID_KSHOT_REACHABILITY`。
- 嵌套K必须使用独立密封package或等价的pre-open精确成员裁剪：K5严格复用对应K10有序support的前5个物理sample ID，K1严格复用第1个，但各自Phase2进程仍只能打开1/5个support/类；K20必须另建只暴露20个support/类的package。每个package必须在打开IQ前核验逐类可达计数、ID前缀关系、成员allowlist和package SHA256。

部署阶段默认冻结：

```text
freeze:
  CV-SincNet backbone
  z_id extractor
  classifier embedded in sealed Phase1 checkpoint

update:
  target-old prototype shrinkage
  target-new prototype
  temperature / bias / threshold
  small adapter or BN affine only when explicitly allowed
  preregistered sparse key-layer delta only when explicitly authorized
```

Phase2不得加载独立、未与deployment bundle共同封存的source prototype bank、source classifier artifact或其他未授权source-derived状态；第7.3.1节的int8原型组件不是可单独替换的sidecar。

星上禁止默认采用 full-model fine-tuning、完整 MAML inner loop、无门控伪标签重放、强在线 GRL/DANN、大型生成模型增强、无回滚持续自训练。

若用户对某一次正式优化明确授权，Stage2-C可增加“稀疏关键层差分校准”作为默认冻结的窄例外，但必须同时满足：

- 只能更新实验前预注册的ADV3B02精确层名白名单，不得用模糊正则扩大到其它backbone层；
- 原checkpoint中可训参数不超过50,000，target-support校准不超过5epoch且不超过50个optimizer step；
- 星上优先使用SGD无momentum，不持久化optimizer状态；只保存FP16差分补丁，差分补丁与分类head总状态仍不超过256KB；
- 差分可合并回原线性层，因此必须分别报告“更新的原参数量”和“部署新增MAC”，不得把更新原层误报为0参数adapter；
- 只允许注册target support参与校准。query特征、query标签、query真old/new角色、全批类别数量、每类quota和Hungarian分配仍全部禁止；
- 正式报告必须标为`support-only sparse key-layer delta`，并与完全冻结adapter分开报告。超出任一限制的row只能作为资源消融或非部署诊断。

Phase2 主线的优化目标是 target-old 域适应、target-new 新类学习和二者的同 row 平衡。open-set rejection、unknown FAR、FPR95、AUROC、Weibull/OpenMax类拒识门控和 unknown query 压力测试属于 Phase3 备用项；Phase2 可以保留相关字段作为 diagnostic/safety metadata，但不得把它们列为主线必达指标或用低 unknown FAR 掩盖旧类/新类学习不足。

### 7.2 target query逐样本自主决策硬约束（2026-07-15）

对任一待识别target query，系统在决策前不知道它属于`Y_old`、已注册`Y_new`还是Phase3中的`Y_unknown`。因此，Phase2与Phase3的正式训练后评估、候选优化、消融、模型选择、晋升、论文主表和部署声明统一采用以下硬约束：

- 每个query必须独立面对同一套当前已注册类别和允许的reject/defer机制；不得使用query真实old/new/unknown角色缩小候选类别集合。
- 禁止读取或推断自评测构造的整批old/new数量、每类query数量、每类quota、query排列顺序或标签分块边界；禁止据此进行Hungarian、最优传输、批量重排或等价的全局配额分配。
- query batch可以用于向量化计算，但不得利用评测构造提供的真实角色、类别数量、每类配额、排列顺序或标签分块边界改变任何单样本决策。
- support标签和support中的old/new注册身份可以用于enrollment；该权限不意味着query角色已知。正式推理必须在所有已注册旧类与新类之间执行同一逐样本决策。
- 历史role/quota Oracle artifact可以原样封存用于说明信息泄漏上界，但统一标记为`PROTOCOL_INVALID_FOR_DEPLOYMENT`。自本约束生效后不得生成新的role/quota Oracle候选，不得用于超参数选择、性能门槛、方法排名、统计检验、论文主表或部署声明。

每个launchable Phase2 row必须显式记录：

```text
phase2_query_decision_policy=per_sample_all_registered_classes
phase2_query_role_oracle_access=false
phase2_query_true_batch_class_count_access=false
phase2_query_class_quota_access=false
phase2_query_batch_global_assignment=false
```

其中`phase2_query_true_batch_class_count_access`只禁止访问或推断当前query评测批次中各真实类别的数量，不禁止读取已注册类别清单或已注册类别总数。旧字段`phase2_query_class_count_access`自本版本起仅作为deprecated历史字段识别，不能单独满足launchable row的当前协议。

正式评估必须采用预测与评分权限隔离：评测集构造器可以在Phase2 predictor边界外使用标签预登记support/query并生成独立truth sidecar；predictor输入schema只能包含LEO query、query sample ID、合法provenance、注册support及注册类别信息，不得包含query truth、old/new/unknown role、真实批次类别计数、每类quota、标签分块或排序提示。predictor必须先输出带SHA256绑定的不可变prediction artifact；随后独立post-prediction scorer才能按query sample ID连接truth/role并计算old/new/H、遗忘、逐类和逐receiver指标。scorer输出、评分状态和任何query标签不得反向进入adapter、head、门限、View触发、回滚、候选选择、排名或prediction重算。

缺少任一当前字段、任一访问字段不为false，predictor输入包含上述禁止信息，或命令/配置启用role oracle、真实query-batch class count/quota、Hungarian、optimal transport、batch reassignment、global quota matching等机制时，该row必须直接标为`LOCAL_PROTOCOL_REPAIR_REQUIRED`并阻断matrix、runner、promotion、论文主表和正式声明。历史artifact若只是缺少当前证明字段则标记为`UNVERIFIED_UNDER_CURRENT_PROTOCOL`；确认使用role/quota Oracle的历史artifact继续按`PROTOCOL_INVALID_FOR_DEPLOYMENT`封存，并增加`ROLE_OR_CLASS_QUOTA_ORACLE`子类型。不得将确认违规的artifact降格后生成新的Oracle诊断候选。

本禁令不禁止以下合法行为：使用注册support标签及其old/new enrollment身份；按实验前声明的正整数`K`为每个注册类构造support；在模型完成逐样本预测后仅为计算accuracy/confusion等指标读取query标签。上述信息不得反向进入适配、阈值、模型选择、逐样本候选集合或批量重排。

本禁令只针对部署阶段target query决策，不影响Phase1源域半监督训练中基于已定义源数据池进行的伪标签class/receiver quota审计或采样平衡。

### 7.3 Phase2禁止源域数据硬约束（2026-07-16）

> **自2026-07-16起，Stage2-A/B/C运行时不得使用任何源域样本。Phase2只能加载封存的Phase1 ADV3B02 deployment bundle、当前target receiver的合法LEO_weak support/query、已注册类别表和与数据无关的预登记算法配置。deployment bundle可按第7.3.1节携带特许的域×类int8聚合原型模型知识，但不得携带原始样本或样本级原始/全精度feature。源域训练在Phase1结束；Stage2不得继续执行source replay、source-target联合训练或依赖源域样本的分布对齐。**

Stage2统一禁止读取、接收、缓存或重构以下内容：

- `R_s`中的source IQ、source LEO_weak样本、source标签或source dataloader；
- source样本级feature、logit、未授权prototype/class center、covariance、BN统计、Fisher信息、gradient、teacher输出或蒸馏目标；
- source cache、source anchor、source memory bank、source replay buffer或source batch；
- 在Phase1 checkpoint之外单独训练并导入的source adapter、source head、ground adapter或source-derived校准状态；
- 以MMD、LMMD、DV-KL、CORAL或其他损失在Stage2运行时比较source与target分布。

唯一允许保留的源域训练遗产是封存的Phase1 ADV3B02 deployment bundle。该bundle在Stage2中整体只读，其checkpoint可冻结使用或按本文件的target-support-only轻量更新白名单进行校准；其可选int8原型组件必须满足第7.3.1节且不得更新。不得同时加载source样本、原始/全精度样本feature或其他未授权source-derived artifact。固定超参数可以在Phase1/source validation阶段预先确定，但必须在Stage2矩阵生成前锁定，且不得携带source样本、未压缩统计量或可查询source状态进入Phase2。

每个launchable Stage2 row必须显式声明：

```text
phase2_source_sample_access=false
phase2_source_cache_access=false
phase2_source_label_access=false
phase2_source_derived_signal_access=false  # 指未授权或bundle外source-derived signal
phase2_unapproved_source_derived_signal_access=false
phase2_source_replay=false
phase2_external_source_adapter_access=false
phase2_pretrained_artifact_policy=sealed_phase1_deployment_bundle_with_optional_int8_domain_class_prototypes_v1
phase2_authorized_compressed_prototype_access=true|false
```

manifest自声明不足以证明合规。sealed package成员白名单、predictor输入schema、运行时文件访问审计和实际打开文件账本均不得出现source dataset、source cache、原始/全精度样本feature或未授权source-derived artifact。若启用int8原型，账本只允许打开同一deployment bundle中的授权压缩组件。缺少任一字段或存在source路径/成员/访问记录时，必须标为`LOCAL_PROTOCOL_REPAIR_REQUIRED`并阻断matrix生成、runner执行、候选晋升和正式声明。

### 7.3.1 特许Phase1域×类int8聚合原型模型知识（2026-07-17）

经用户明确特许，Phase1/offline可在任何target support/query可达之前，按预登记算法从Phase1授权地面训练split提取表征，并将每个训练域×旧类的多样本聚合质心量化为int8，作为ADV3B02 deployment bundle的不可分割只读组件。该特许只允许上传压缩后的聚合原型特征，不允许上传原始IQ、单样本feature、全精度prototype、exemplar、source cache、sample ID、可逆样本坐标、协方差、BN/Fisher/gradient、teacher输出或其他可查询source状态。

经2026-07-17用户进一步明确授权，正式默认不再逐项保存冗余的`int8[D,C,P]`全量原型，而采用`int8_domain_class_center_lowrank_residual_radius_v2`：只保存一个由Phase1/offline、target不可达条件下按固定global max-min规则选出的中心域的`int8[C,P]`核心原型；其余域×类原型只保存相对该中心的低秩偏移，即`int8[C,R,P]`残差basis、`int8[D-1,C,R]`域×类coeff、各量化向量的FP16 scale，并为每个域×类原型保存`int8[D,C]`非负稳健半径、逐类FP16 radius scale、固定center-domain/domain/class registry和feature schema/version。D20正式默认`R=3`；center handle、rank、SVD符号规范、量化/舍入规则必须在任何target访问前固定并进入method lock，禁止按target support、query、LEO场景或运行结果选择。方向重建公式固定为`p_hat[d,c]=dequant(core[c])+sum_r(dequant(coeff[d,c,r])*dequant(basis[c,r]))`；中心域直接使用`dequant(core[c])`。core、basis和coeff均使用对称int8量化，`scale=max(abs(v))/127`、`q=clip(round(v/scale),-127,127)`，禁止`-128`，零向量使用固定安全scale。旧`int8[D,C,P]`v1只可作为Phase1/offline压缩输入或历史只读诊断，不再作为D20正式Phase2运行payload。

v2的center、basis和coeff都只能由多样本聚合后的域×类质心在Phase1/offline进一步因子化得到，不得包含单样本残差、成员数量/身份、奇异值full-precision sidecar、协方差或其他可查询source状态。每个原型半径固定定义为该Phase1域×类归一化成员embedding到其聚合质心的P90余弦距离，在Phase1内部完成many-to-one聚合后只保留非负int8量化标量；逐类`radius_scale=max(radius[:,c])/127`，`radius_q=clip(round(radius/radius_scale),0,127)`。禁止上传成员距离列表、成员数、方差/协方差、分位数草图或直方图。半径只可用于预登记的逐样本距离标准化、旧类内部证据可靠度和新类注册半径先验；不得作为query角色、类别quota或batch分配信号。Phase2可为当前计算临时解量化一个core、basis、coeff、radius或单个重建原型，但不得持久化/导出dense FP16/FP32原型bank，不得更新或按query选择offset/radius。若只使用中心核心原型进行推理，应完全跳过偏移重建并将其MAC记为0；若使用偏移，必须按预登记的固定逐样本规则面对全部已注册旧类。每个被因子化的原始域×类cell仍必须由多个Phase1训练样本many-to-one聚合；Phase2不可见成员数量、成员身份或样本坐标，也不得执行membership查询或样本重构。

正式D21半径统计锁定为`fixed_4096_bin_upper_p90_cosine_distance_v1`：Phase1对`[0,2]`余弦距离使用4096个等宽bin，返回经验nearest-rank P90所在bin的上沿，因此相对精确P90为保守上界，确定性误差不超过`2/4096=0.00048828125`。该直方图、逐样本距离和cell count只可存在于Phase1/offline临时内存，生成int8半径和不可逆aggregate generation proof后必须丢弃，不得进入deployment bundle。两遍导出必须对归一化`z_id`、opaque class handle和domain handle的有序stream生成SHA256并证明完全一致；只有count/sum一致不足以作为radius来源证明。

checkpoint、int8原型、量化尺度、registry和schema必须在Phase1/offline共同生成并以同一bundle root、成员allowlist、逐成员SHA256、生成代码/config SHA和detached signature整体封存。原型不得在Phase2被更新、重估、追加、替换或按target/query选择；target-old/target-new support产生的在轨原型必须存入独立target状态。int8原型只允许作为旧类只读锚、类别先验或support融合项，不得用于source replay、source-target分布损失，也不得接收query truth、old/new角色、真实批次类别数、类别quota、query顺序或global assignment。

启用该组件的row还必须记录：

```text
phase2_authorized_phase1_model_knowledge_policy=int8_domain_class_center_lowrank_residual_radius_v2
phase2_phase1_prototype_generation_stage=phase1_offline_before_target_access
phase2_phase1_prototype_payload=int8_center_core_plus_int8_lowrank_domain_class_residual_plus_int8_radius_fp16_scales_registry_only
phase2_phase1_prototype_representation=domain_class_center_lowrank_residual_radius_v2
phase2_phase1_prototype_center_domain_policy=phase1_offline_global_maximin_fixed_before_target_access
phase2_phase1_prototype_center_domain_target_conditioned=false
phase2_phase1_prototype_center_domain_query_conditioned=false
phase2_phase1_prototype_residual_rank=3
phase2_phase1_prototype_radius_definition=p90_cosine_distance_to_phase1_domain_class_centroid
phase2_phase1_prototype_radius_member_values_exposed=false
phase2_phase1_prototype_dense_bank_persistent=false
phase2_phase1_prototype_dequantized_persistence=false
phase2_phase1_prototype_dequantized_export=false
phase2_phase1_prototype_component_immutable=true
phase2_phase1_prototype_update_access=false
phase2_phase1_prototype_offset_update_access=false
phase2_phase1_prototype_member_or_exemplar_access=false
phase2_phase1_prototype_sample_reconstruction_access=false
phase2_nonbundle_source_artifact_access=false
```

资源审计必须分别记录core、basis、coeff、radius、各FP16 scale、registry/schema、压缩文件、临时单原型重建峰值、target原型和adapter状态字节；不得用ZIP/NPZ文件大小代替逻辑部署状态。还必须报告一次性enrollment重建MAC、每query额外MAC、平均/P95时延和峰值RAM/显存。pre-open validator必须精确验证v2 schema、center handle、`R=3`、全部domain/class cell与radius覆盖、int8范围、radius非负、正有限FP16 scale、radius生成证明、checkpoint/class-handle绑定、共同bundle root/allowlist/hash/signature和无dense/full-precision sidecar；radius缺失、来源不明或用prototype偏移伪造时必须阻断正式运行，缺失或冒充v1时为`LOCAL_PROTOCOL_REPAIR_REQUIRED`。若上传物含原始样本、样本级/全精度feature、可逆成员信息，或int8组件未与checkpoint整体封存，该row为`PROTOCOL_INVALID_FOR_CURRENT_STAGE2_SOURCE_ACCESS`。

经2026-07-17用户再次明确要求“不要再执着于前期协议数据封存，快点跑实验”，允许对已在target数据产生前由Phase1/offline生成、且已压缩为本节白名单payload的历史int8组件执行一次性`PRE_FORMAL_SUPPORT_ONLY_INT8_SCREEN`。该筛选不是launchable Stage2 row：只可使用开发seed的合法LEO_weak注册support做leave-sample-out方法筛选，不得打开query、生成正式accuracy声明、进入确认矩阵或替代最终共同bundle。打开support前必须固定组件/checkpoint SHA、真实Phase1 TX列到注册class handle的逐列映射及算法候选；运行时仍不得读取历史全精度prototype、source样本、样本级feature或其他source artifact。筛选完成后若路线为正，正式锁参、query评估和确认矩阵仍必须使用与checkpoint共同封存并重建method lock的不可分割bundle；若路线为负可直接淘汰而不支付完整封存成本。本窄例外仅减少前期工程，不放宽正式Stage2-B/C证据边界。

历史MRIOR-SDA、DADDA-SDA或其他在Stage2读取source LEO cache、执行source CE、MMD/LMMD、DV-KL source-target对齐或source replay的结果，自本约束生效后统一标记为`PROTOCOL_INVALID_FOR_CURRENT_STAGE2_SOURCE_ACCESS`。这些artifact可原样封存用于说明旧协议下的性能上界和计算代价，但不能作为当前Stage2正式基线、候选门槛、论文主表证据或部署声明。若要继续比较其思想，必须设计只使用target support的变体、采用新名称并在新协议下重新运行；不能沿用旧结果或继续称为原source-target域适应实现。

### 7.4 Phase2最小准入与实验优先规则（2026-07-16）

> **本节的目的只有一个：防止任务把主要精力消耗在前期协议准备上。第7.1至7.3节的安全边界必须满足，但检查必须最小化、一次化；检查通过后立即进入实验和优化。不得为了证明合规而新增一套复杂准入系统、重复建设manifest、反复追溯Phase1/source/clean历史，或把协议讨论本身当作工作成果。**

Phase2开始前只回答以下三个二元问题：

1. 输入是否仅包含封存的Phase1 deployment bundle、合法`LEO_weak` target support/query、已注册类别表和预登记算法配置；若bundle启用int8原型，是否仅含第7.3.1节允许的压缩聚合payload。
2. package、loader、cache、feature和运行命令中是否完全不存在clean/source原始样本、样本级/全精度feature及未授权logit、prototype、统计、teacher、anchor、replay、adapter或其他decision signal。
3. predictor是否满足逐样本全注册类别决策，并且不接触query truth、old/new/unknown真实角色、真实批次类别数量、每类quota或全局分配信号。

已有manifest、validator结果、密封package证据和运行时隔离证据能够回答上述问题时，必须直接复用；**本文件不要求新增独立`phase2_input_admission.json`、新的组合哈希系统、额外审计报告或新的控制面工程。**

处理结果只有两种：

- 三项均满足：标记`EXPERIMENT_READY`，立即继续候选设计或修改、矩阵/命令生成、最窄本地验证和Runner gates。不得停在协议PASS、dry-run、报告更新、state更新或“建议下一步实验”。
- 任一项不满足：标记`LOCAL_PROTOCOL_REPAIR_REQUIRED`，只列出直接失败项并做最小修复；修复后重新检查该项。不得借机重做Phase1、分析source/clean数据、补clean control、清理全部历史artifact或扩建无关治理工具。一个失败项不能阻塞其他已满足三项检查的row或lane。

除非checkpoint、target package、loader/predictor输入边界或本文件安全规则发生实质变化，已通过的检查不得重复。target-support-only方法、超参数、prototype规则、adapter设置、K值或资源档变化不构成重新审计前期工作的理由。

Phase2任务的默认完成物必须是实验推进物：可执行候选/矩阵、完成的本地验证、真实runner结果、完成结果表，或基于完整日志的机制级修订。报告中的协议部分只记录“PASS/FAIL、失败项、证据路径”，主要内容必须用于`old_acc`、`min_old_class_acc`、`seen_new_acc`、`H_old_new`、遗忘/适应增益、资源开销和下一轮机制优化。仅重复“禁止clean/source”不算一次优化轮次。

## 8. WiSig / ManySig 数据协议

### 8.1 数据集角色

- 地面训练使用 WiSig/ManySig 中的源接收机域数据。
- 在轨推理少样本阶段使用同一数据体系下的目标卫星接收机代理域，或明确保留的其他 TX 子集。
- WiSig/ManySig 是 terrestrial proxy benchmark / ground-accessible source domain family，不是真实卫星训练集。
- satellite-channel augmentation 和 satellite stress 是 physics-informed deployment stress，不是真实在轨 IQ 验证。

### 8.2 接收机划分

主协议必须使用与 source training receivers 不相交的目标接收机域：

```text
source receivers: R_s
target receiver domain: R_t
intersection(R_t, R_s) = empty
|R_t| >= 1
```

示例：

```text
source receivers: rx0-rx6
target receiver domain: rx7-rx11
```

`R_t` 可以只包含一个 receiver，例如 `{rx7}`，也可以包含多个 receiver，例如 `{rx7, rx8, rx9, rx10, rx11}`。自动化不得再以 exactly-one `r_sat` 作为 launchability gate；它必须检查 `R_t` 是否与 `R_s` disjoint，并检查 `R_t` 中是否存在目标阶段需要的 old/new/unknown 样本覆盖。多接收机目标域的结果必须报告为 target receiver domain / deployment proxy domain，不再写成“单星接收机”证据。

### 8.3 发射机划分

旧类：

```text
Y_old:
  used in ground training
  used for target-old support/query on R_t
```

新类：

```text
Y_new:
  not used in ground training
  used for Stage2-C seen-new support/query on R_t
```

未知类：

```text
Y_unknown:
  not used in ground training
  not used as Stage2-C seen-new support
  used only for rejection query on R_t
```

`Y_unknown` 是 Phase3 open-set 备用项使用的集合。Phase2 主线只要求 `Y_old` 与 `Y_new` 的合法 target-domain support/query；若某个 Phase2 row 同时携带 `Y_unknown`，必须标明 unknown query 为 evaluation-only / Phase3-backup metadata，不能参与 Phase2 阈值拟合、adapter 更新、主排序或成功声明。

若当前 ManySig 仅提供六个旧类 TX，可采用：

```text
Y_old = {0,1,2,3,4,5}
Y_new = held-out non-ManySig TX set from another WiSig subset
Y_unknown = held-out non-ManySig TX set disjoint from Y_new
```

如果本地数据暂时不能提供非旧类 TX，Stage2-C 不能声明新类识别，也不能作为 Phase2 主线完成；只能做 Stage2-A/B，或标为 `LOCAL_DATASET_EXTENSION_REQUIRED`。

### 8.4 已确认的 Phase2 WiSig 候选样本池

当前 N607 已确认存在以下原始 WiSig compact subsets：

```text
/home/szu2070436088/2510044040/CV-SincNet/Dataset_WigSig/ManySig.pkl
/home/szu2070436088/2510044040/CV-SincNet/Dataset_WigSig/ManyTx.pkl
/home/szu2070436088/2510044040/CV-SincNet/Dataset_WigSig/ManyRx.pkl
/home/szu2070436088/2510044040/CV-SincNet/Dataset_WigSig/SingleDay.pkl
```

本地若只存在 synthetic smoke features，不能作为部署证据；真实 Phase2 样本选择应以 N607 上的 pkl 为准，或先把真实 pkl 显式同步/登记到本地。

当前主候选池定义如下：

```text
Y_old = {14-10, 14-7, 20-15, 20-19, 6-15, 8-20}  # ManySig old/source TX
R_s = {1-1, 1-19, 14-7, 18-2, 19-2, 2-1, 2-19}  # ManySig source receivers
```

Phase2 matrix generator should first choose target receiver domains from this confirmed pool before proposing new dataset routes. 当前 Phase2 主线优先构造 target-old + target-new 的简化LEO目标域少样本适应/新类学习 row：

| target receiver label in `R_t` | ManySig target index | ManyTx receiver index | target-old source | target-new / unknown source | confirmed non-old TX count in ManyTx | confirmed non-old eq1 samples |
|---|---:|---:|---|---|---:|---:|
| `20-1` | 7 | 10 | ManySig / `Y_old` | ManyTx / non-`Y_old` TX | 144 | 27,638 |
| `3-19` | 8 | 12 | ManySig / `Y_old` | ManyTx / non-`Y_old` TX | 143 | 26,887 |
| `7-14` | 9 | 13 | ManySig / `Y_old` | ManyTx / non-`Y_old` TX | 137 | 26,445 |
| `7-7` | 10 | 14 | ManySig / `Y_old` | ManyTx / non-`Y_old` TX | 142 | 26,868 |
| `8-8` | 11 | 17 | ManySig / `Y_old` | ManyTx / non-`Y_old` TX | 140 | 26,474 |

这些候选满足 Phase2 主线样本选择核心条件：`R_t` 与 `R_s` 不相交，`R_t` 中存在与训练 TX 相同的 target-old 样本，也存在与训练 TX 不同的 target-new 样本。矩阵生成时必须按 receiver label 对齐不同 pkl 的 `rx_list`，不能把 ManySig 的 index 直接当成 ManyTx 的 index。

`ManyTx` 是当前 Stage2-C target-new 的主来源；若进入 Phase3，`ManyTx` 也可为 `Y_unknown` 提供备用 open-set 样本。`ManyRx` 可作为 receiver-rich 或低新类数量 control；`SingleDay` 可作为 single-day smoke/control，但它缺少 `3-19` target receiver，且只有单日样本。若使用 `ManyRx` 或 `SingleDay` 替代 `ManyTx`，矩阵必须显式标明这是 control/sensitivity 设计，而不是默认主线 Stage2-C 候选。

生成 Stage2-C 时，`ManyTx` 中 non-`Y_old` TX 必须提供合法的 `Y_new` target-new support/query。若同一 row 额外携带 Phase3 open-set 备用信息，则 `Y_new` 与 `Y_unknown` 必须互斥；不得把同一个 TX 同时用于 seen-new enrollment 和 unknown rejection query。每个 Phase2 主线 row 必须记录 `target_receiver_ids`、`source_receiver_ids`、`target_old_tx_ids`、`target_new_tx_ids`、support/query 划分、`K`、satellite/LEO view 和阈值选择 label scope；Phase3 备用 row 还必须记录 `target_unknown_tx_ids` 和 unknown query evaluation-only 范围。

`ManyTx` 行的 `target_new_tx_ids` 以及对应 `target_new_tx_labels` 必须是从 `ManyTx.pkl` 的 `tx_list` 解析出来的真实 TX label（例如 `1-16`、`10-1`），不得使用 synthetic numeric IDs、rank 占位或“稍后解析”的说明文字作为 launchable 字段。Phase3 备用 row 的 `unknown_tx_ids` / `unknown_tx_labels` 也必须遵守同一真实标签规则。矩阵生成器在写入 launchable row 前必须按目标 receiver label 预筛每个 `Y_new` TX 的可用样本数，确认满足该 row 的 support/query 需求；若启用 Phase3，则也必须预筛 `Y_unknown`。只记录 aggregate non-old TX count 或 aggregate samples 不足以证明 launchable。若任何 TX 与 `Y_old` 重叠、无法按 `ManyTx.tx_list` 解析、在目标 receiver 下样本不足或过滤后会产生空 tensor，该 row 必须标为 `LOCAL_PROTOCOL_REPAIR_REQUIRED` / `LOCAL_DATASET_EXTENSION_REQUIRED` / `NON_LAUNCH_DIAGNOSTIC`，不得发往 N607 runner。

### 8.5 星地信道视图

deployment support/query 必须按 satellite/LEO 视图报告。自 2026-06-24 起，主协议采用简化 LEO 残余信道版本：LEO-only 几何，高度、仰角和 slant range 只用于 metadata/link budget；完整 Doppler 视为已由星历/同步补偿，IQ 上只叠加 residual Doppler/CFO；默认使用 mild phase noise、flat/Rician fading、AWGN/SNR、RMS/AGC normalization，并加入弱多径。低仰角或边界场景可以使用弱 shadowed-Rician，但不得默认启用 severe LOO、强风暴多径、MEO/GEO 混合或未补偿完整 Doppler。

推荐主报告视图：

```text
target_channel_view in {
  leo_clear_weak,
  leo_low_elev_weak,
  leo_rain_weak
}
```

自2026-07-13起，CVS后续Phase1/Phase2训练验证、候选比较、checkpoint终局评估和主报告中的默认测试增强统一使用上述三个`leo_*_weak`视图。训练和测试可以使用不同随机种子、不同样本扰动和独立固定评估缓存，但测试场景族不得再默认切换到`legacy_full`。任何launcher、runner或评估脚本若未显式指定测试场景，也必须解析为：

```text
eval_sat_scenarios = leo_clear_weak,leo_low_elev_weak,leo_rain_weak
```

自2026-07-17起，星上轻量化中的计算view只能作用于已经接收并密封的唯一LEO_weak IQ，不构成新的LEO信道状态或新的物理样本。允许在完全相同的唯一接收IQ、scenario、satellite seed、support/query划分、checkpoint和adapter下比较`none`及预登记的接收侧信道均衡、增强、变换或表征分支；每个分支必须保留相同稳定根`physical_sample_id`并记录`parent_received_iq_sha256`、`operator_id`和`view_seed`，不得调用LEO channel simulator，不得生成新的`target_channel_view`或overlay provenance，不得访问clean/raw IQ或按独立样本增加K。`rx_shift3`、`rx_cfo3`、`rx_light5`等历史名称只有在实际operator满足上述接收后边界时才可重新登记；任何会注入另一LEO状态或重新执行LEO信道模拟的实现必须停用。正式晋升必须显式报告每个物理样本的view count、backbone forward count、FFT count、是否共享同一IQ payload以及相对单view的`old_acc`、`seen_new_acc`、`H_old_new`和资源变化。历史legacy角色/类别配额Oracle只允许作为`PROTOCOL_INVALID_FOR_DEPLOYMENT`封存artifact，不得生成新候选或参与任何正式比较。

自2026-07-17起，允许探索固定已接收IQ上的信道均衡、增强、变换与多表征压缩，但禁止从同一clean/raw样本生成多LEO状态教师输入。地面预研只能锁定接收后operator、结构和超参数；除第7.3.1节与checkpoint共同封存的int8域×类聚合原型外，不得把独立source-trained adapter、head、prototype、统计或其他可变状态带入Phase2。任何Phase2可变adapter/head状态都只能由合法注册target support拟合；授权int8原型始终只读。星上自适应view必须先执行base representation，并只依据当前单个query的margin、entropy、表征分歧等部署时可得量决定是否追加接收后计算分支；门限只能由source validation或注册support确定，禁止使用query标签、真实old/new/unknown角色、整批类别比例、每类quota、query排序或Hungarian分配。正式结果必须同时报告平均与P95 backbone forward count、各view触发率、每个物理样本是否仅有一个IQ payload和相同row性能；若view实现生成额外LEO状态、携带bundle外source-derived状态或按多个物理样本参与适配，则协议无效。

自2026-07-13起，CVS与外部方法的正式对比实验中，所有进入论文主表、主图、统计检验或方法排序的测试样本都必须实际叠加上述简化LEO星地信道之一；不得把未叠加星地信道的clean测试混入正式主结果。clean只允许作为单独control/reference，必须与deployment-primary结果分表。若测试入口没有记录scenario、satellite seed或增强是否实际启用，该测试结果视为artifact-incomplete，不得形成论文结论。

每个正式实验的最终测试artifact必须同时保留sample-level score table和分组详细统计。分组至少包含逐receiver、逐transmitter、receiver x transmitter以及receiver x transmitter x day四个层级；每组必须记录sample count、correct count、accuracy和稀疏confusion明细。Phase2还必须保留target-old/target-new角色、support/query sample ID和support/query overlap检查。只有overall accuracy而没有逐接收机/逐发射机详细结果的实验，不满足CVS发表证据要求。

该约束用于统一项目主指标口径并避免把不同星地信道实现的结果直接横向比较。由于训练与测试使用同一简化LEO场景族，主报告必须明确写作`leo_weak`族内独立随机压力鲁棒性，不得把它扩大解释为跨信道模型、跨实现或真实在轨泛化。需要检验跨增强族能力时，旧场景必须以显式`legacy stress/control`附加评估运行，单列结果，不得参与默认checkpoint选择、候选promotion或deployment-primary成功门槛。

旧场景 `clear_leo`、`low_elev_leo`、`rain_leo`、`storm_mp`、`mixed_orbit` 保留为 legacy stress/control。`storm_mp` 和 `mixed_orbit` 只能作为 diagnostic/sensitivity，不再作为默认 deployment-primary 成功门槛。

clean view只允许用于Phase1或与Phase2完全隔离的离线control/reference。Phase2本身禁止访问clean样本及其派生feature/logit/prototype；不得把clean结果作为Phase2适配、校准、选择、回滚、排序或评估信号，更不得把clean view成功提升为satellite/LEO deployment success。

## 9. Stage2-A/B/C 协议

Stage2-A/B/C 自 2026-07-07 起共同归入 Phase2 deployment adaptation 主线。Phase2 主线的中心是：在叠加简化LEO星地信道的目标接收机域 `R_t` 上，利用少量 target-old 和 target-new 样本完成跨域适应、旧类校准和新类学习。unknown/open-set 拒识从 Phase2 主线中移出，作为 Phase3 备用项管理。

### 9.1 Stage2-A：Zero-label target-domain LEO reference

目的：无目标标签时建立目标接收机域简化LEO视图下的旧类识别和非旧类观测参考。Stage2-A 是 Phase2 前置参考/安全基线，不是新类学习完成项，也不是 open-set 主线。

```text
support:
  empty

query on R_t:
  target-old query from Y_old
  target-new query from Y_new for non-old reference only
  optional Phase3-backup unknown query from Y_unknown, evaluation-only
```

允许声明：

- old-class target recognition。
- target-new is non-old reference / not-yet-enrolled reference。
- optional Phase3-backup unknown diagnostic，必须标为非Phase2主线。

禁止声明：

- new identity recognition。
- target-label threshold fitting。
- seen-new accuracy。
- Phase2 open-set success。

Stage2-A 是目标域LEO参考底线，不是新类注册，也不是 Phase3 open-set 结果。

### 9.2 Stage2-B：Target-old few-shot domain adaptation

目的：在同一个卫星接收机域和简化LEO目标视图下，用少量旧类标注样本进行目标域适应和旧类校准。

```text
support on R_t:
  K shots per old TX from Y_old

query on R_t:
  held-out target-old query from Y_old
  target-new query from Y_new as not-yet-enrolled reference
  optional Phase3-backup unknown query from Y_unknown, evaluation-only
```

允许声明：

- target-old full accuracy / accepted accuracy。
- old_acc_delta_pp。
- old retention。
- rescue / harm / net_gain。
- target-old adaptation under LEO target view。
- optional Phase3-backup unknown diagnostic，不作为 Phase2 主线成败。

禁止声明：

- seen-new identity accuracy。
- target-new support 使用。
- unknown query 参与阈值拟合。
- 把 low unknown FAR 或 high AUROC 写成 Phase2 主线成功。

### 9.3 Stage2-C：Old + seen-new enrollment

目的：在同一个卫星接收机域和简化LEO目标视图下，同时利用旧类 support 校准和新类 support 注册。这是 Phase2 当前主线目标。

```text
support on R_t:
  K shots per old TX from Y_old
  K shots per seen-new TX from Y_new

query on R_t:
  held-out target-old query from Y_old
  held-out seen-new query from Y_new
  optional Phase3-backup unknown query from Y_unknown, evaluation-only
```

允许声明：

- target-old performance。
- seen-new identity accuracy。
- `H_old_new`。
- output semantics: old label, seen-new label, uncertain, defer；若启用 Phase3 备用项，可额外报告 reject。

禁止声明：

- 把 `Y_unknown` 当 seen-new 识别。
- 用 unknown query 调阈值。
- 把 clean-view success 写成 deployment success。
- 把 Phase3 open-set 指标写成 Phase2 主线门槛。

Stage2-C 是 Phase2 主线目标，但只有在 `Y_new` 与 `Y_old` 不相交、`R_t` 与 `R_s` 不相交、target-old 与 target-new support/query 都来自 `R_t`，并且这些样本按 satellite/LEO target view 构造时才成立。

### 9.4 Phase3：Open-set backup

Phase3 是备用项，不是当前 Phase2 主线。只有在 Phase2 已完成或用户明确要求 open-set 安全扩展时，才把 unknown/open-set 学习作为正式优化对象。

Phase3 support/query 边界：

```text
support on R_t:
  optional target-old support from Y_old
  optional target-new support from Y_new

query on R_t:
  held-out target-old query from Y_old
  held-out seen-new query from Y_new when Y_new is enrolled
  unknown query from Y_unknown, evaluation-only unless a Phase3 protocol explicitly allows a separate calibration split
```

Phase3 可以报告：

- unknown FAR / unknown rejection。
- FPR95 / AUROC。
- open-set confusion。
- reject / uncertain / defer 行为。
- Phase2 old/new 性能在加入拒识门控后的保留率。

Phase3 禁止：

- 用 unknown query 反向调 Phase2 阈值、adapter、prototype 或主排序。
- 用 open-set 指标替代 Phase2 的 target-old / seen-new 同 row 学习指标。
- 把 Phase3 备用项写成当前主线。

### 9.5 当前阶段化优化优先级：PHASE2_ADAPT_NEWCLASS_FIRST

当前H06/Phase2修复路线采用阶段化优化顺序：

1. 先提升旧类目标域准确率，阶段门槛为`old_acc>=0.80`。该门槛用于判定是否进入下一阶段优化，不等同于部署成功、论文主结论或Stage2-C成功。
2. 在同一协议边界下达到`old_acc>=0.80`后，Phase2下一主目标是Stage2-C的`seen_new_acc`、`H_old_new`和旧类/新类同 row 平衡。
3. 若当前row仍是Stage2-B old-only / old + target-new reference，则不得报告`seen_new_acc`，此时只能把下一阶段写成“OLD80达成后进入Stage2-C seen-new enrollment优化”。
4. unknown拒识性能（`unknown_FAR`/`unknown_rejection`）下沉为Phase3备用项。Phase2阶段可同步记录unknown diagnostic，但不得为了unknown FAR牺牲旧类或新类学习后仍称为完成Phase2主线。
5. PHASE2_ADAPT_NEWCLASS_FIRST是当前工程/实验优先级，不会放宽`R_t`/`R_s`不相交、`Y_old`/`Y_new`互斥、target-new query不参与阈值拟合、unknown query不参与Phase2阈值拟合、clean view不代表部署成功等协议约束。

### 9.6 目标域旧类微调上限诊断：TARGET_OLD_ONLY_FT_DIAG

当PHASE2_ADAPT_NEWCLASS_FIRST路线的旧类阶段未达到`old_acc>=0.80`时，可以追加一个诊断性实验：只使用目标接收机域`R_t`中的旧类`Y_old`带标签样本，在目标域内部划分support/query，评估target-only微调或线性头/小adapter对旧类query准确率的上限提升。

该诊断的边界如下：

1. 该诊断只回答“如果暂时不考虑FAR、新类和未知拒识，目标域旧类样本本身能把旧类性能提升到什么程度”。
2. support和query必须都来自`R_t`中的`Y_old`，且query不能与support重叠。可以使用`K`或train-per-TX网格记录目标域样本量。
3. 该诊断不使用`Y_new`support，不使用`Y_unknown`query调阈值，也不报告`seen_new_acc`、unknown FAR、Phase2完成或部署成功。
4. 该诊断可使用feature-level classifier/head/adapter微调或full-model target-only fine-tune，但必须标为`NON_DEPLOYMENT_DIAGNOSTIC`或`TARGET_OLD_ONLY_UPPER_BOUND_DIAGNOSTIC`。如果使用full-model fine-tune，还必须单独报告其星上不可部署或需离线重训练的边界。
5. 若该诊断达到`old_acc>=0.80`，只能说明旧类目标域样本中存在可利用的判别信号；后续仍需回到Stage2-B/Stage2-C协议中重新加入target-new支持、新类学习和同 row 平衡门槛。unknown拒识若继续优化，应作为Phase3备用项单独进入。

## 10. 指标与成功判据

### 10.1 地面训练阶段

- strict UDU / unseen receiver-day accuracy。
- worst receiver / receiver floor。
- pseudo-label audit precision。
- pseudo-label coverage by class and receiver。
- `z_id -> receiver` leakage probe accuracy。
- satellite stress mean/floor，作为 deployment-oriented validation-control。

### 10.2 Stage2-B

- target-old full accuracy。
- target-old accepted accuracy + coverage。
- old_acc_delta_pp。
- rescue / harm / net_gain。
- PHASE2_ADAPT_NEWCLASS_FIRST第一阶段门槛：`old_acc>=0.80`，仅表示旧类校准阶段可继续进入新类学习优化。
- rollback trigger rate。
- TARGET_OLD_ONLY_FT_DIAG诊断指标：target-old query accuracy、相对source-only baseline的delta、support/query划分、train-per-TX，不报告FAR或seen-new。
- optional Phase3-backup unknown diagnostic：unknown_FAR、FPR95、AUROC，必须标为备用项，不参与Phase2主线排序。

### 10.3 Stage2-C

- seen_new_acc。
- old_acc。
- `H_old_new`。
- new_acc_drop_pp <= 2 pp，或明确标为 exploratory。
- old->new, new->old confusion。
- latency / memory / prototype storage。
- 当前优化顺序必须先满足旧类`old_acc>=0.80`阶段门槛，再优化`seen_new_acc`和`H_old_new`；未达OLD80时不得把seen-new单点作为主线成功。
- optional Phase3-backup unknown diagnostic：unknown_FAR、unknown_rejection、FPR95、AUROC、unknown->old / unknown->new confusion，必须与Phase2主线指标分表或分栏报告。

#### 10.3.1 极轻型快速适应目标模式（2026-07-14）

2026-07-14起，Phase2 Stage2-B/Stage2-C采用“极轻型快速适应目标模式”。该模式不替代第9节的数据权限与声明边界：Stage2-B只评估合法target-old support/query上的目标域旧类校准，Stage2-C同时评估合法target-old/target-new support/query上的旧类适应与新类学习；两者都必须使用deployment-primary LEO视图。协议准入按第7.4节一次性完成；取得`EXPERIMENT_READY`后，本模式的工作重心必须是target-support-only方法、同row指标和资源Pareto，不得把重复审计Phase1/source/clean历史当作当前优化。

- target-old总体`old_acc>=0.92`，并且独立确认集上每个旧类的聚合准确率下限`min_old_class_acc>=0.88`。该旧类门槛同时用于Stage2-B旧类校准和Stage2-C同row旧类适应；Stage2-B通过不能替代Stage2-C的新类准确率与`H_old_new`门槛。
- 当同一row注册5个真实target-new TX时，`seen_new_acc>=0.92`；注册10个真实target-new TX时，`seen_new_acc>=0.90`；注册20个真实target-new TX时，`seen_new_acc>=0.86`。
- 上述绝对性能门槛默认必须在统一`K=10`物理support/类下达到。开发阶段只能用K10选择一套统一candidate和超参数，锁定后再评估嵌套`K=5`低support敏感性；不得为K5单独选arm、权重、epoch或阈值。
- K5必须复用对应K10的前5个物理support sample ID、完全相同的query sample ID和同一candidate，并使用只暴露这5个support/类的独立密封package；对应K10的第6–10个及任何K20余量在K5 Phase2进程中必须不可达。对每个matched receiver×seed×场景×新类规模，K5的`old_acc`、`min_old_class_acc`、`seen_new_acc`和`H_old_new`相对K10均不得下降超过3个百分点。K5稳健性不能替代K10绝对门槛，且不得跨row拼接最小drop。
- K1自2026-07-15起作为极少support下旧类适应的强制压力门槛。K1必须复用对应K10的第1个物理support sample ID、完全相同的query sample ID和锁定candidate，并使用只暴露这1个support/类的独立密封package；同类其余support在K1 Phase2进程中必须不可达。不得使用K1 query重新选择adapter、域适应方法、超参数、epoch或门限。每个K1 row必须同时报告加入全部注册类前的`old_acc_before_increment`、加入全部注册类后的`old_acc`以及`old_adaptation_gain=old_acc-old_acc_before_increment`；独立确认矩阵的总体聚合和每个target receiver聚合均要求`old_adaptation_gain>=0`，否则只能标记为K1负迁移诊断，不能声明极少shot适应有效。K1正增益不能替代K10绝对性能门槛，也不能用跨row拼接或单个最好seed证明。
- K1允许使用Phase1/source validation预先锁定的固定超参数、第7.3.1节授权的只读int8域×类聚合原型，以及注册support标签进行角色对称的轻量域对齐，包括target-support-only对角CORAL、低参数FiLM、固定已接收IQ上的均衡/增强/变换view一致性、稳健原型和仅基于已注册support原型Gram矩阵的去混淆；所有已注册类必须使用同一规则。除授权int8组件外，任何source旧类prototype bank、source covariance、样本级source feature或source memory均不得进入Stage2。K1的计算view必须来自同一个密封LEO_weak IQ并按同一物理sample ID计为1-shot，不得生成另一LEO状态或独立物理support，且必须单列一次性enrollment前向与状态开销。
- 自2026-07-15起，极轻型模式把分K旧类遗忘与绝对准确率并列为正式优化目标。固定遗忘锚点为嵌套`K∈{1,5,10,20}`；每个matched receiver×seed×场景×新类规模×K row必须报告`old_acc_before_increment(K)`、`old_acc_after_increment(K)`、`average_forgetting(K)=old_acc_before_increment(K)-old_acc_after_increment(K)`和`old_adaptation_gain(K)=-average_forgetting(K)`。同一K的前后比较必须复用同一旧类query和同一推理View策略，不得把不同row的前后值拼接为遗忘率。
- 分K遗忘门禁不能通过牺牲新类或最低类性能规避。K1仍执行总体及逐target receiver的`average_forgetting<=0`硬门槛；K5/K10/K20要求锁定candidate在独立确认集上的平均遗忘率不高于对应K的identity-only单qKNN基线，且必须同时满足该K原有的`old_acc`、`min_old_class_acc`、`seen_new_acc`和`H_old_new`约束。报告另给出`worst_K_forgetting=max_K average_forgetting(K)`与`mean_positive_forgetting=mean_K max(average_forgetting(K),0)`；准确率门禁通过后，候选Pareto比较优先最小化这两项，不能仅按某个最好K或某个最好epoch声明抗遗忘。
- 开发阶段仍不得使用K1/K5/K20 query重新选择candidate、adapter、epoch、域适应超参数或门限。跨K抗遗忘机制只能通过source validation、注册support上固定接收IQ计算view的leave-one-view/sample-out稳定性和K10开发row确定；锁定后K1/K5/K20只用于压力评估与独立确认。若锁定candidate在任一K产生较identity-only更大的遗忘，该结果用于下一轮机制级修订，不得在同一query上继续逐K调参。
- K1还必须显著优于严格直接ADV3B02旧类分类头。由于直接ADV3B02没有target-new输出头，该比较只定义在完全相同target-old query sample ID和同一基础LEO View上的旧类准确率：`delta_vs_direct_ADV3B02_K1=old_acc_after_increment(K1)-direct_ADV3B02_old_acc`。独立确认矩阵总体聚合要求该差值至少`+0.02`且matched row配对95%置信区间下界大于0；每个target receiver聚合要求差值不小于0。直接基线必须严格加载同一base checkpoint，不得使用target support、query拟合、FFT辅助、TTA或adapter。该门槛不能替代K1的`average_forgetting<=0`、新类准确率、最低类准确率或K10绝对门槛。
- 三种新类规模必须使用按数据覆盖预先确定的嵌套真实TX集合`Y_new^5⊂Y_new^10⊂Y_new^20`。不得按query性能挑选TX，不得使用合成`new-*`类别ID替代真实ManyTx TX标签。
- 主确认矩阵固定覆盖5个target receiver、至少5个独立确认seed和`leo_clear_weak`、`leo_low_elev_weak`、`leo_rain_weak`三个场景，并同时保留matched K10/K5结果。三个场景必须使用两两互斥物理样本ID集合构成独立接收样本单元，不能由同一clean/raw IQ生成三种LEO状态副本；每个物理样本只属于一个scenario且只具有一次overlay provenance。开发seed只允许在K10选择一套统一超参数；选定后必须锁定，并在独立确认seed上同时评估5/10/20类和K5稳健性。不得跨K、跨seed或跨新类规模拼接最佳指标。
- 正式推理必须逐样本可部署：禁止使用query真实old/new角色、全批类别数量、每类quota、query标签或query集合图结构。query不得参与adapter拟合、阈值拟合、模型选择或早停。
- 默认冻结ADV3B02 backbone，不执行backbone梯度更新。经2026-07-17用户更新活动目标，极轻型正式资源硬上限调整为adapter可训练参数不超过80,000、适配不超过30epoch、无dense query图、持久化适配状态不超过256KB；授权int8原型组件计入部署状态但不计为可训练adapter参数。第7.1节已授权的`support-only sparse key-layer delta`仍执行其更严格的50,000原checkpoint可训参数、5epoch和50 optimizer step窄例外，不因一般adapter上限放宽而改变。推理可以使用单view或固定已接收IQ上的逐样本轻量均衡/增强/变换/多表征门控，但不得生成另一LEO状态；必须报告平均与P95 backbone forward count、FFT/表征分支数、MAC、时延和峰值显存。任何参数、epoch或状态超限row只能作为资源消融或非部署对照，不得进入正式候选、125确认矩阵或成功声明。
- 当前极轻型正式路线不得使用超过80,000参数、30epoch或256KB状态的`performance-relaxed`档逼近门槛；`support-only sparse key-layer delta`不得超过其50,000参数、5epoch、50 step专项上限。若未来确需再次放宽，必须先由用户明确授权并先修订本文件；资源放宽无论如何都不得改变无角色Oracle、无类别配额、无query拟合、无dense query图、单物理样本单LEO接收观测和逐样本决策硬约束。
- 自2026-07-16起，使用source replay的MRIOR-SDA不再是当前Stage2正式基线，其既有结果仅保留为旧协议性能上界诊断。当前强制基线改为同一ADV3B02 checkpoint上的`target-support-only`方法，包括直接ADV3B02、identity-only单qKNN、ProtoNet CDA以及预登记的轻量target-support-only适应基线。若需要保留MRIOR或DADDA思想，必须移除source CE、source cache和source-target分布项，重新命名为CVS support-only ablation并在相同receiver、seed、K、support/query ID、新类集合、LEO场景和View策略下重跑。旧MRIOR-SDA/DADDA-SDA数值不得用于“高于matched baseline 2pp”的正式门槛，也不得据其step、时延或显存比例定义星上资源上限。
- 允许把已注册的K个独立物理support样本分别从各自唯一密封LEO_weak IQ计算至多3个预登记接收侧均衡/增强/变换representation view，用于一次性support-only enrollment或一致性学习；这些view必须共享原IQ payload、`physical_sample_id`、scenario和satellite seed，不得重新叠加另一LEO状态或被当作独立物理support。K仍按互不重复的物理sample ID计数。query不得用于拟合；正式query推理以单view为首选，若使用自适应多表征门控，必须逐样本决定并单列每个物理query的backbone/FFT前向次数、触发率和计算量。该机制只能声明为`fixed-received-IQ post-reception view`，不能声明为多LEO状态观测、额外卫星接收样本或跨信道泛化。
- 自2026-07-17起，极轻型正式候选的target-old/target-new support与query必须逐行来自对应的`leo_clear_weak`、`leo_low_elev_weak`或`leo_rain_weak`目标缓存，且同一matched实验切片的三个缓存必须使用两两互斥的物理样本ID集合。除第7.3.1节与ADV3B02 checkpoint共同封存的int8域×类聚合原型外，Stage2不得导入新训练的`effective_feature` ground adapter、source receiver holdout、source reference/prototype或其他额外source-derived artifact。历史ADV3B02基础checkpoint既有训练来源不据此改写，但必须与Stage2输入权限分开报告。任何target cache中出现source角色、clean channel、场景字段缺失、场景与row不一致、同一物理样本跨scenario重用或同一物理样本具有多个LEO overlay provenance均应fail closed。
- 报告必须同时给出同row的`old_acc`、`min_old_class_acc`、`seen_new_acc`、`H_old_new`、逐类/逐receiver结果、适配epoch或闭式求解次数、MAC、端到端时延、峰值显存和持久状态，并相对identity-only单qKNN基线报告Pareto变化。
- 达到平均指标但未达到旧类最低类门槛，或只在开发seed达到门槛，均不得声明目标完成。所有适配训练row必须保存完整loss trace；无梯度闭式适配row必须标记`EVAL_ONLY_CLOSED_FORM_ADAPTATION`并保存求解诊断。

### 10.4 Phase3 open-set backup

- unknown_FAR <= 0.05。
- unknown_rejection。
- FPR95 / AUROC。
- old/new性能保留率。
- reject / uncertain / defer coverage。
- unknown->old、unknown->seen-new、old/new->reject confusion。
- Phase3结果必须绑定同一candidate/run，且不得替代Phase2的`old_acc`、`seen_new_acc`和`H_old_new`主线判断。

## 11. 论文与报告声明边界

可以写：

- CVS 面向天基 RFFI 的弱标注跨接收机 DG 与在轨跨域 few-shot 适应。
- WiSig/ManySig 是地面可接入源域代理。
- satellite stress 是物理启发部署压力测试。
- Stage2-B 是旧类目标域校准。
- Stage2-C 是 Phase2 主线的 target-old adaptation + seen-new enrollment，前提是 `Y_new` 与 `Y_old` 不相交，且target-old / target-new support/query来自同一个LEO目标域。
- Phase3 是 open-set / unknown rejection 备用项，不是当前主线。

不能写：

- WiSig/ManySig 是真实卫星训练集。
- satellite augmentation 等价于真实在轨验证。
- source-only DG 等价于 few-shot learning。
- 旧类 target support 带来的提升是新类识别。
- Stage2-A/B 的 rejection 结果是 seen-new identity accuracy。
- open-set / unknown FAR 结果是 Phase2 主线成功。
- target receiver domain 与 source receiver domain 重叠后仍称为部署泛化。
- 缺少 target-old 或 target-new 样本覆盖时仍声称完整 Stage2-C。

## 12. 自动化控制面约束

CVS 自动化不只是工程调度层，也承担实验语义落地责任。任何 monitor、optimizer、runner、launcher、matrix generator、validator、registry、state ledger、report generator 或 N607 launch plan 都必须遵循本文件中的科研场景和数据协议。

自动化生成或放行实验时，必须检查以下语义条件：

- 训练阶段是否仍是 source-domain weak-label / semi-supervised DG，而不是误写成 target few-shot。
- target receiver domain `R_t` 是否与 source receiver domain `R_s` 不相交。
- `R_t` 是否同时包含 target-old `Y_old` 样本和 target-new `Y_new` 样本；缺任一类时不得声称完整 Stage2-C。
- target-old、target-new、target-unknown 是否满足 `Y_old/Y_new/Y_unknown` 的互斥定义。
- Stage2-A/B/C 是否使用对应 support/query 权限，尤其禁止 Stage2-A/B 声称 seen-new identity accuracy。
- Stage2-B/C 是否显式记录正整数 `K`；推荐锚点 `{1, 2, 5, 10, 15, 20, 50}` 可用于主曲线，但中间值允许进入 launchable row。`K>20` 必须解释为 higher-shot / medium-shot / saturation point，而不是 strict few-shot。
- satellite/LEO view 是否按 deployment-primary 处理；clean view只允许存在于Phase1或与Phase2完全隔离的离线control，Phase2不可访问。
- Phase2是否严格满足`phase2_sample_view_policy=leo_weak_only_no_clean_access`与`clean_sample_access=false`，且全部support/query和派生适配/选择信号都来自实际叠加`leo_*_weak`后的样本。
- Phase2主线是否以 target-old适应、target-new新类学习、`old_acc`、`seen_new_acc`和`H_old_new`为核心；不得把open-set / unknown FAR设为Phase2主线必达门槛。
- 若携带unknown query，是否被标为Phase3-backup / evaluation-only metadata，且未参与Phase2阈值拟合、adapter更新、主排序或成功声明。
- target query是否在全部已注册类别上执行逐样本自主决策；只要使用真实old/new/unknown角色、批次类别数量、每类quota、query排序/分块或Hungarian/等价配额重排，该row必须直接阻断，不得降格后继续进入正式候选矩阵。
- Phase2 row是否显式满足`phase2_query_decision_policy=per_sample_all_registered_classes`以及四个query Oracle/类别数量/类别配额/全局分配访问字段均为false；缺字段或冲突时必须`LOCAL_PROTOCOL_REPAIR_REQUIRED`。
- Phase2 row是否同时满足`phase2_source_sample_access=false`、`phase2_source_cache_access=false`、`phase2_source_label_access=false`、`phase2_unapproved_source_derived_signal_access=false`、`phase2_source_replay=false`和`phase2_external_source_adapter_access=false`；若启用特许int8组件，还必须满足第7.3.1节全部字段。Stage2不能读取任何source样本、样本级/全精度feature、未授权统计/prototype、cache、replay或bundle外source-derived artifact。
- Phase2 row是否满足单物理样本单LEO接收观测：稳定根ID必须来自不可重命名的pre-overlay lineage token；每个稳定根ID只能对应一个LEO状态和一次overlay provenance；同scenario support/query不得重叠；matched三场景的support∪query全角色物理ID并集必须两两互斥；接收后均衡/增强/变换view必须绑定同一`parent_received_iq_sha256`和已登记operator，不得计为额外物理样本、生成另一LEO状态或参与query拟合。
- 第7.4节的三个最小问题是否已由现有manifest、validator和隔离证据回答。已有`EXPERIMENT_READY`结论且输入边界未实质变化时必须直接复用，不得新增准入文件、组合哈希或重复追溯Phase1/source/clean历史。
- 指标、阈值和成功声明是否符合第 10 节和第 11 节。
- Phase2 row 是否满足 target receiver domain、support/query、TX split、satellite/LEO view 和本地字段要求；只要某个 Phase2 row 已满足这些要求，自动化就应继续 Runner gates，而不能用旧的整 lane local-patch 状态掩盖该 launchable row。
- 某个row或package准入失败时，只阻断直接失败项并进行最小修复；其他`EXPERIMENT_READY` row必须继续进入Runner gates。协议修复、manifest补写、报告补写和state更新不得被当成已有launchable row的实验完成。

若自动化候选与本文件冲突，默认处理为：

- 不能进入 launchable row。
- 不能写成 deployment success。
- 不能作为论文主结论。
- 只能标为 `NON_LAUNCH_DIAGNOSTIC`、`LOCAL_PROTOCOL_REPAIR_REQUIRED` 或先提交本文件修订。

例外：第7.1节禁止的Phase2 clean接触候选、第7.1.1节禁止的同物理样本多LEO状态候选和第7.2节禁止的target query role/quota Oracle不得按上述通用规则新建为`NON_LAUNCH_DIAGNOSTIC`；只允许封存约束生效前已经存在的历史artifact，并分别统一标记为`PROTOCOL_INVALID_FOR_PHASE2`、`PROTOCOL_INVALID_FOR_PHASE2_SINGLE_OBSERVATION`或`PROTOCOL_INVALID_FOR_DEPLOYMENT`。

自动化可以提出新路线，但新路线必须先被翻译为本文件允许的场景、数据集合、support/query 权限、指标和声明边界；翻译不清楚时，不得由 runner 或 launcher 通过默认参数补齐。

Phase3 open-set 备用路线可以作为单独实验矩阵、后续安全扩展或诊断分支提出；它不能阻塞已经满足 target-old + target-new LEO样本协议的 Phase2 主线 row 进入Runner gates。

## 13. 修改流程

任何后续优化、修改或实验设计如需改变以下内容，必须先更新本文件，再改 prompt、contract、runner、matrix 或报告模板：

- 项目主场景。
- `L_s/U_s` 标注与无标注定义。
- `rho_label` 网格。
- `R_s/R_t` 接收机划分。
- `Y_old/Y_new/Y_unknown` TX 划分。
- K-shot 策略、推荐锚点或报告分层。
- Stage2-A/B/C 边界。
- satellite/LEO 视图是否为 deployment-primary。
- 指标、成功判据或可声明结论。

若某个新想法违反本文件，但仍有探索价值，必须标为 `NON_LAUNCH_DIAGNOSTIC` 或先提交协议修订；不得直接作为 launchable row、论文主结论或部署成功证据。

第7.1节Phase2 clean不可达禁令与第7.2节role/quota Oracle禁令优先于本段通用诊断规则：不得生成新的clean接触或Oracle诊断候选。

## 14. Git与Markdown同步纪律

任何CVS项目相关改动都必须进入Git可追踪流程。改动前必须确认目标文件所在目录是否为Git仓库，并记录`git status -sb`或明确说明该目录尚未纳入Git。改动后必须检查`git diff`/`git status -sb`，完成必要验证，并把变更文件、验证结果和未提交风险写入对应报告或Markdown交接记录。

协作输出规则：对于使用工具或长时间运行的任务，首次工具调用前、关键阶段切换时、重连或上下文压缩恢复后、出现阻塞时，以及持续工作期间至少每60秒，必须发送简洁、基于证据的进度更新；只报告可观察操作、发现和下一步，不披露私有思维链，不倾倒原始日志。仅无工具的简短问答可以省略过程更新。

如果目标目录已经是Git仓库，完成验证后应提交本次意图明确的改动，除非用户明确要求不要提交。若目标目录不是Git仓库，不得把改动描述为“已版本化”；必须先选择或初始化Git承载目录，或同步到已经约定的Git-backed发布工作区/分支。

任何代码、配置、脚本、矩阵、prompt、报告模板或协议改动，都必须同步检查项目相关Markdown是否需要更新：

- 工作流、安全、环境、N607、Git或协作规则改动，更新`AGENTS.md`。
- CVS科学场景、数据协议、receiver/TX划分、`rho_label`、Stage2-A/B/C边界、K-shot、satellite/LEO视图、指标或声明口径改动，先更新本文件。
- README、docs、实验报告、发布说明或交接文档中涉及的用法、结果解释、发布范围和复现边界发生变化时，必须同步更新对应Markdown。
