# CVS项目场景与数据协议

本文件是工作区`项目.md`的Git承载入口。公开协议正文见[docs/PROJECT_PROTOCOL.md](docs/PROJECT_PROTOCOL.md)，一次性Phase2数据验证实现边界见[docs/PHASE2_DATA_VALIDATION_APPENDIX.md](docs/PHASE2_DATA_VALIDATION_APPENDIX.md)，协议schema为`p2_min_v1`。工作区根文件是运行时source of truth；本入口与公开正文保持语义一致，但不追求字节镜像。

文件职责：只描述项目场景、数据集合、Phase1/Phase2数据边界、Stage2-A/B/C权限和声明范围；活动性能目标、候选方法、实验矩阵、资源上限、N607操作与Git流程不写入本文件。

核心Phase2约束：每个clean/raw物理IQ在进入Phase2前仅叠加一次随机允许的LEO弱信道，形成唯一固定接收观测；K-shot由K个独立物理样本组成；support/query物理ID互斥；query只用于逐样本全注册类测试；Phase2禁止clean/source运行时访问，唯一例外是与Phase1 checkpoint共同封存且不可更新的int8多样本聚合模型知识；通过`p2_min_v1`一次验证的data capsule可跨方法复用，method lock、adapter、超参数或模型状态变化不得触发数据重审。

核心Phase3拒识约束：正式registered/unknown query必须在连接真值或角色前走同一LEO弱信道生成与场景分配管线，每个接收记录只绑定一份固定`leo_clear_weak`、`leo_low_elev_weak`或`leo_rain_weak`接收IQ；不得让registered使用clean而只给unknown加弱信道，不得按真实known/unknown角色选择scene/seed，也不得从同一物理记录生成多个弱信道实现后挑选或融合。clean unknown仅可标为`DIAGNOSTIC_UNKNOWN_NO_LEO_NON_FORMAL`的隔离诊断，不能用于正式拒识、晋级或卫星场景声明。

核心Phase1目标域确认约束：训练、模型选择、阈值和source侧开发排序保持source-only；候选与规则完全冻结后，允许一次性、零适配地对同一预注册`R_t`capsule中的registered／unknown单LEO观测封存预测，再由隔离truth-side scorer报告域泛化与拒识指标。该结果不得反馈候选重排、调参、重训或重跑，也不构成Phase3多节点协同、anonymous entity、可信确权或注册授权声明。

作用域例外：上述Phase2数据权限、query访问和决策约束只约束Stage2主方法及其内部候选，不限制CSIL、MoPC-HR等外部论文对比方法。对比方法可按原论文完整流程使用base/source数据、历史统计、训练批次、评估流程和超出主方法预算的网络/优化状态。正式对比默认要求所有新类注册及新类评测样本叠加并记录LEO星地信道；经用户显式要求，可另跑仅用于归因的matched无LEO新类诊断，保持方法、物理样本ID、support/query划分、K-shot、seed和旧类评测条件不变，只替换同一新类物理记录的未叠加IQ。该诊断必须标为`DIAGNOSTIC_NEW_CLASS_NO_LEO_NON_FORMAL`并与正式LEO结果隔离，不能用于CVS卫星场景性能声明、方法晋级或Stage2协议有效性证明。对比方法须公开其权限差异，且其结果不得作为Stage2主方法协议合规证据。

N607是大规模训练、Phase2实验、稳定性screen、独立确认矩阵与资源审计的主要计算和证据承载面，但不是receiver域、数据来源、卫星实体或协议例外。floor评价覆盖全部实际注册类；算法必须使用类别身份无关的共享规则，不得针对具体TX/class ID写死分支、权重、阈值或超参数。
