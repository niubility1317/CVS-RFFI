# ADV3B02三种域适应方法对比实验周报

报告周期：2026-07-15至2026-07-16

实验阶段：Stage2-B target-old域适应

正式实验ID：`adv3b02_three_da_leoweakonly_20260715_v1`

## 一、本周工作概述

本周完成了ADV3B02基座模型下ProtoNet CDA、MRIOR-SDA和DADDA-SDA三种域适应方法的正式对比。实验覆盖5个target receiver、5个独立seed和5个K-shot设置，每种方法运行125个任务，三种方法合计375个任务。全部任务完成，失败0；最终生成135,000条逐样本评分记录和8,625条loss trace。

本轮实验采用修复后的Phase2协议：所有support和query在进入Phase2前已叠加`leo_clear_weak`、`leo_low_elev_weak`或`leo_rain_weak`信道，Phase2进程不能访问clean样本、clean特征、clean prototype或其他clean派生信号。query逐样本面对全部6个注册旧类，不使用query真实角色、真实批次类别数量、类别配额、Hungarian分配或query-query图结构。

实验结果表明，MRIOR-SDA在三种方法中取得最高旧类准确率，但依赖完整ADV3B02 backbone更新，计算开销较大；DADDA-SDA具有稳定的平均正收益，但整体低于MRIOR；ProtoNet CDA虽然几乎不需要训练，却在大多数任务中产生负迁移。当前三种方法均未达到项目设定的旧类总体准确率和最低类别准确率目标，也尚未完成新类注册后的Stage2-C评估。

## 二、实验配置

### 2.1实验矩阵

单种方法的实验规模为：

\[
5个target receiver\times5个seed\times5个K=125个任务
\]

|配置项|设置|
|---|---|
|基座模型|ADV3B02|
|特征空间|160维`z_id`|
|checkpoint SHA256|`2699eedcafe8cec880828592d2d65ba3781a9948939da5cf5c82b47143d59c98`|
|target receiver|`20-1`,`3-19`,`7-14`,`7-7`,`8-8`|
|seed|`713101`～`713105`|
|K-shot|`1,2,5,10,20`|
|旧类|`14-10`,`14-7`,`20-15`,`20-19`,`6-15`,`8-20`|
|LEO场景|clear weak、low elevation weak、rain weak|
|query数量|每类每场景20个，单任务共360个|
|support关系|`K1⊂K2⊂K5⊂K10⊂K20`|
|query关系|相同receiver和seed下，不同K复用相同query|

三种方法使用相同的ADV3B02 checkpoint、support/query划分、receiver、seed和LEO场景。直接ADV3B02在相同query上的平均旧类准确率为73.60%，作为域适应前基线。

### 2.2输入与输出

ProtoNet的输入为冻结的ADV3B02、target-old K-shot support和target-old query；输出为类别prototype、逐样本预测和旧类准确率。

MRIOR和DADDA除target-old support外，还读取同场景封存的source LEO_weak样本。两种方法利用source监督信号和target support标签更新ADV3B02 identity backbone，之后在固定query上生成不可变prediction artifact。独立scorer在预测完成后连接query真值，计算总体、receiver、场景和类别指标。

query标签不参与训练、早停、超参数选择、阈值拟合或回滚判断。

## 三、三种域适应方法

## 3.1ProtoNet CDA：计算最轻，但目标域原型严重失配

ProtoNet冻结ADV3B02，对每个类别的K个support特征求均值：

\[
p_c=\frac{1}{K}\sum_{i:y_i=c}f_{\theta}(x_i)
\]

query按照欧氏距离选择最近类别prototype：

\[
\hat y=\arg\min_c\lVert f_{\theta}(x)-p_c\rVert_2^2
\]

该方法不执行梯度更新，单任务平均时延仅0.046秒，是三种方法中最轻的方案。它可以直接检验ADV3B02原始特征空间是否已经具备目标receiver下的类内聚合能力。

实验结果显示，ProtoNet平均旧类准确率为66.85%，相对直接ADV3B02下降6.75个百分点，125个任务中只有10个任务获得正收益。K=1时准确率仅58.67%，25个任务全部负迁移；K增加到20后准确率上升至71.48%，仍低于73.60%的直接基线。

ProtoNet的问题不是support数量不足这一单一因素。即使K达到20，简单均值prototype仍不能消除receiver响应和LEO信道对特征空间的扭曲。ADV3B02特征在目标域中存在类内多模态、场景偏移和类别尺度不一致，单prototype加固定欧氏距离会把信道差异误认为身份差异。K较小时，异常support还会直接移动类别中心，导致原型估计方差过大。

## 3.2MRIOR-SDA：性能最强，但依赖完整backbone训练

MRIOR使用同场景source LEO_weak样本和target-old support，以source分类、target support分类和DV-KL正则共同优化模型：

\[
\mathcal L_{\mathrm{MRIOR}}=
\mathcal L_{\mathrm{CE}}^{source}
+0.5\mathcal L_{\mathrm{CE}}^{target}
+0.005\mathcal L_{\mathrm{DVKL}}
\]

主要参数为：

```text
adapt learning rate=0.0006
estimate steps=7
adapt steps=200/场景
场景数=3
class balance smoothing=0
```

每个adapt step包含7次估计器上升和1次模型下降。三种场景合计执行600次ADV3B02 backbone模型更新，因此其实际优化计算还包含额外的估计器迭代。

MRIOR将旧类准确率从73.60%提高到82.58%，平均收益为+8.98个百分点，125个任务中105个正迁移、20个负迁移。K=1时达到77.22%，K=10达到85.82%，K=20达到87.74%。MRIOR在`20-1`receiver上的收益最大，旧类准确率从64.61%提高到83.43%，说明target support能够提供有效的receiver域校正信号。

MRIOR仍存在三个核心问题。

第一，计算方式不符合星上极轻型训练目标。每个任务需要600次完整backbone更新，平均适配时延17.90秒，还未把7次估计器迭代单独折算为星上计算负载。该方法适合作为性能上界，不适合作为当前部署方案。

第二，完整backbone更新会产生负迁移。`7-14`receiver的直接准确率已经达到90.06%，MRIOR适应后下降到89.93%；20个任务的适应后准确率低于直接ADV3B02。固定200step不能区分“需要大幅校正的receiver”和“原模型已经足够稳定的receiver”。

第三，平均性能提升没有解决弱类问题。MRIOR在`14-7`、`14-10`和`20-19`上的平均准确率分别为68.13%、72.83%和75.91%，而`6-15`和`8-20`达到93%以上。类别收益高度不均衡，说明source分类保持、target support拟合和DV-KL约束仍会优先强化易分类类别。当前`class_balance_smoothing=0`也没有提供弱类保护。

## 3.3DADDA-SDA：分布对齐有效，但低shot类别条件估计不稳定

DADDA使用source和target support监督分类，同时通过MMD和LMMD进行全局及类别条件分布对齐：

\[
\mathcal L_{\mathrm{DADDA}}=
\mathcal L_{\mathrm{CE}}^{source}
+\mathcal L_{\mathrm{CE}}^{target}
+\mathcal L_{\mathrm{MMD}}
+\alpha\mathcal L_{\mathrm{LMMD}}
\]

主要参数为：

```text
adapt learning rate=0.0001
momentum=0.9
weight decay=0.0005
target CE weight=1.0
alignment weight=1.0
adapt steps=200/场景
```

MMD对齐source和target的全局`z_id`分布，LMMD利用support标签估计类别条件分布，动态alpha调整两种对齐项的权重。DADDA同样在三种场景中执行600次完整backbone更新。

DADDA适应后旧类准确率为78.35%，相对直接ADV3B02提高4.75个百分点，125个任务中99个正迁移。K=1时为74.94%，只比直接模型高1.34个百分点；K=10达到80.31%，K=20达到82.16%。其平均时延为14.62秒，比MRIOR低18%左右，但仍属于完整backbone训练。

DADDA的主要限制在于低shot条件下的类别条件分布估计不稳定。K=1或K=2时，每个类别只有极少target样本，LMMD估计容易被单个support的信道状态和噪声主导。全局MMD只能缩小source-target总体分布差距，却不能保证不同身份类别仍保持足够间隔；过强对齐可能把相近类别拉向同一区域。

DADDA在`20-19`类别上达到78.19%，略高于MRIOR，但在`14-7`和`14-10`上只有60.72%和65.49%。这表明全局与局部对齐对不同类别的作用不一致。DADDA也在`7-14`receiver上产生轻微负收益，说明固定对齐强度缺少域偏移程度感知。

## 四、性能结果

### 4.1总体结果

|方法|适应前old_acc|适应后old_acc|平均收益|正/负迁移任务|平均时延|backbone更新|
|---|---:|---:|---:|---:|---:|---:|
|MRIOR-SDA|73.60%|**82.58%**|**+8.98pp**|105/20|17.90s|600|
|DADDA-SDA|73.60%|78.35%|+4.75pp|99/26|14.62s|600|
|ProtoNet CDA|73.60%|66.85%|−6.75pp|10/115|0.046s|0|

MRIOR在准确率上形成当前最强对照，DADDA取得中等正收益，ProtoNet则证明“不训练backbone、直接用target support求均值”不足以解决目标域偏移。

### 4.2不同K值结果

|K|直接ADV3B02|MRIOR-SDA|DADDA-SDA|ProtoNet CDA|
|---:|---:|---:|---:|---:|
|1|73.60%|77.22%|74.94%|58.67%|
|2|73.60%|79.51%|76.14%|64.70%|
|5|73.60%|82.59%|78.19%|68.98%|
|10|73.60%|85.82%|80.31%|70.42%|
|20|73.60%|87.74%|82.16%|71.48%|

MRIOR和DADDA都随K增加而持续改善，说明target support规模仍是适应效果的关键因素。两者在K=1下的提升有限，尚未解决项目重点关注的1-shot快速适应。ProtoNet在所有K下都低于直接ADV3B02，表明其问题来自特征空间失配，而不只是prototype样本不足。

### 4.3receiver和场景差异

|target receiver|直接ADV3B02|MRIOR-SDA|DADDA-SDA|ProtoNet CDA|
|---|---:|---:|---:|---:|
|`20-1`|64.61%|83.43%|75.91%|60.98%|
|`3-19`|60.33%|69.06%|65.31%|48.50%|
|`7-14`|90.06%|89.93%|89.93%|83.53%|
|`7-7`|80.22%|86.78%|82.49%|74.81%|
|`8-8`|72.78%|83.69%|78.10%|66.42%|

`3-19`是最困难的target receiver，MRIOR适应后仍只有69.06%；`7-14`直接性能最高，却在训练式适应后出现轻微下降。统一的学习率和固定更新步数无法同时适配这两种receiver，因此后续需要只依赖support统计的域偏移强度估计、trust gate和identity回退。

|LEO场景|直接ADV3B02|MRIOR-SDA|DADDA-SDA|ProtoNet CDA|
|---|---:|---:|---:|---:|
|clear weak|75.90%|85.99%|81.15%|70.08%|
|low elevation weak|72.53%|81.29%|77.39%|65.99%|
|rain weak|72.37%|80.45%|76.50%|64.48%|

rain和low elevation场景仍是主要性能瓶颈。三种方法的场景排序一致，说明当前适应并没有完全消除信道扰动。后续自适应多View应优先处理低置信度、低margin和视图分歧较大的query，而不应对所有query固定使用5-view。

## 五、当前暴露的问题

### 5.1当前最强方法仍未达到项目目标

项目当前要求K=10时旧类总体`old_acc>=92%`、最低旧类`min_old_class_acc>=88%`。MRIOR在K=10下为85.82%，距离总体目标仍差6.18个百分点；全K汇总的最低类别均值只有68.13%。三种方法都不能作为已达标方案。

### 5.2性能与资源之间存在明显冲突

ProtoNet满足轻量要求，却产生负迁移；MRIOR和DADDA能提升准确率，却需要600次完整backbone更新。现有结果说明，后续不能在“完全冻结”和“完整backbone训练”之间二选一，需要寻找中间路线：只更新ADV3B02少量关键层、低秩adapter或FiLM参数。若更新原backbone参数，正式`sparse key-layer delta`候选必须限制在5epoch、50个optimizer step以内；超限结果只能作为资源消融。

### 5.3固定训练强度导致receiver相关负迁移

高偏移receiver需要较强适应，高基线receiver则需要保守更新。当前MRIOR和DADDA对所有receiver固定执行200step/场景，没有根据support可信度、source-target距离或prototype稳定性调整训练强度，也没有在预测前决定是否回退到identity模型。

### 5.4低shot下的统计估计不可靠

K=1时，ProtoNet prototype、DADDA的类别条件分布以及MRIOR的target监督梯度都由单个物理support决定。单样本的信道状态、相位和噪声会直接进入适应目标，导致模型把样本特有扰动当成receiver域特征。K=1需要使用support-only多View一致性、稳健中心、全局对角CORAL或低参数调制，不能直接放大单样本监督梯度。

### 5.5弱类保护不足

`14-7`、`14-10`和`20-19`在三种方法中持续偏低。总体准确率提升主要来自`20-15`、`6-15`和`8-20`等易分类类别。若继续只优化总体交叉熵，模型可能进一步牺牲弱类。下一轮应加入类对称的support可靠性加权、prototype Gram去混淆或基于support leave-one-out稳定性的弱类保护，但不能使用query类别配额。

### 5.6现有实验只完成Stage2-B

本轮125实验只评估6个旧类，没有target-new support和query，因此没有以下指标：

```text
seen_new_acc
H_old_new
old_acc_before_increment
old_acc_after_increment
average_forgetting
old_to_new/new_to_old confusion
```

该结果不能证明完整类增量学习效果。MRIOR作为项目强制对比基线，也必须在相同5/10/20新类、receiver、seed、K、support/query ID和View策略下重新运行Stage2-C。

### 5.7资源记录仍不完整

当前summary记录了适配时延和backbone更新次数，但没有统一保存三种方法的：

```text
可训练参数
峰值显存
持久化状态
平均/P95 backbone forward
部署新增MAC
```

因此本轮只能确认MRIOR和DADDA是重型full-backbone方法，尚不能完成精确的星上资源Pareto比较。

### 5.8统计独立性需要进一步处理

同一receiver和seed下，五个K复用相同query，因此125行并非125个完全独立样本。现有总体95%CI是任务行级描述性统计。后续正式显著性比较应按K分别进行25行配对，或以`receiver×seed`为cluster执行bootstrap，避免高估样本量。

### 5.9历史结果不能与本轮正式结果混用

2026-07-14历史矩阵在Phase2内部读取raw/clean IQ后叠加LEO信道，已被标记为`PROTOCOL_INVALID_FOR_PHASE2`。后续周报、论文表格和候选排序只能使用本次2026-07-15 LEO_weak-only结果；历史结果只保留为协议修复前诊断。

## 六、阶段结论

本周实验确认了两个关键事实。

其一，目标receiver的少量support确实包含可利用的域校正信息。MRIOR在`20-1`上获得18.82个百分点提升，并随着K增加持续改善，说明域适应方向本身有效。

其二，现有方法没有同时满足性能、弱类稳定性和星上资源约束。ProtoNet过轻但无法对齐目标特征；MRIOR最强但训练过重；DADDA位于两者之间，却仍需完整backbone更新且低shot对齐不稳定。下一阶段应以MRIOR作为性能教师和强制基线，压缩其有效更新方向，而不是简单缩短完整backbone训练或继续使用未经校正的均值prototype。

## 七、Stage2详细实验规划与数据协议

### 7.1Stage2总体目标与阶段关系

Stage2属于Phase2 deployment adaptation主线。其目标是在与地面训练receiver不重叠的目标接收机域\(R_t\)上，利用叠加简化LEO星地信道后的少量目标域标注样本完成旧类适应和新类注册。

Stage2按证据能力分为三个层级：

|阶段|target support|target query|主要作用|不能声明|
|---|---|---|---|---|
|Stage2-A|无support|旧类query；新类仅作未注册参考|建立无目标标签的LEO目标域底线|新类身份识别、seen-new accuracy|
|Stage2-B|每个旧类K-shot support|独立旧类query；新类可作未注册参考|目标域旧类适应与校准|使用新类support、seen-new accuracy|
|Stage2-C|每个旧类和每个新类均有K-shot support|独立旧类和seen-new query|旧类适应与新类注册的同row平衡|把unknown当seen-new、用unknown调参|

Stage2-A是安全参考，Stage2-B验证旧类适应能力，Stage2-C才是当前Phase2主线完成项。Stage2-B准确率提高不能替代Stage2-C的新类准确率、`H_old_new`和遗忘率。

unknown/open-set拒识不属于当前Stage2主线。若后续加入`Y_unknown`，只能作为Phase3-backup evaluation-only query，不能参与Phase2 adapter更新、阈值拟合、候选排序或成功判定。

### 7.2数据集角色

WiSig/ManySig在本项目中是地面可访问的terrestrial proxy benchmark，不是真实卫星数据。LEO信道叠加是physics-informed deployment stress，也不等价于真实在轨验证。

|数据子集|Stage2用途|边界|
|---|---|---|
|`ManySig.pkl`|提供6个旧类及source/target receiver中的target-old样本|旧类来源|
|`ManyTx.pkl`|提供相同target receiver下的真实non-`Y_old`新类|Stage2-C默认新类来源|
|`ManyRx.pkl`|receiver-rich或低新类规模control|不作为默认Stage2-C主线|
|`SingleDay.pkl`|single-day smoke/control|缺少`3-19`且只有单日，不作为完整主线|

原始数据路径只允许由Phase1/offline打包流程读取。Phase2 predictor不能接收这些pkl路径，也不能调用能够切换到raw/clean loader的旧接口。

### 7.3receiver划分

地面训练receiver集合：

```text
R_s={
  1-1,
  1-19,
  14-7,
  18-2,
  19-2,
  2-1,
  2-19
}
```

Stage2目标receiver集合：

```text
R_t={
  20-1,
  3-19,
  7-14,
  7-7,
  8-8
}
```

必须满足：

\[
R_s\cap R_t=\varnothing
\]

同一target receiver的target-old样本来自ManySig，target-new样本来自ManyTx。不同pkl的receiver index并不一致，矩阵生成器必须按receiver label对齐，不能把ManySig的index直接用于ManyTx。

当前5个target receiver均已确认同时具备旧类样本和大量non-`Y_old`候选样本：

|target receiver|ManySig target index|ManyTx receiver index|ManyTx可用non-old TX数量|
|---|---:|---:|---:|
|`20-1`|7|10|144|
|`3-19`|8|12|143|
|`7-14`|9|13|137|
|`7-7`|10|14|142|
|`8-8`|11|17|140|

### 7.4发射机划分

旧类集合：

```text
Y_old={
  14-10,
  14-7,
  20-15,
  20-19,
  6-15,
  8-20
}
```

这些类别参与地面训练，也在Stage2的\(R_t\)中提供target-old support/query。

新类必须满足：

\[
Y_{new}\cap Y_{old}=\varnothing
\]

新类不能出现在地面训练中，只能在Stage2-C通过target-new support进行注册。本轮规划使用已锁定的真实ManyTx类别集合：

```text
Y_new^5={
  1-16,1-18,18-10,14-11,8-3
}
```

```text
Y_new^10={
  1-16,1-18,18-10,14-11,8-3,
  18-8,10-10,16-19,20-12,4-10
}
```

```text
Y_new^20={
  1-16,1-18,18-10,14-11,8-3,
  18-8,10-10,16-19,20-12,4-10,
  13-14,2-5,1-8,19-13,19-9,
  3-8,19-8,11-19,2-16,19-6
}
```

三者必须保持：

\[
Y_{new}^{5}\subset Y_{new}^{10}\subset Y_{new}^{20}
\]

该集合按数据覆盖和TX label预先选择，不使用target query性能。当前锁定切片为`day_index=0`、`equalized_index=1`，每个入选新类在5个target receiver上至少有50个可用样本，满足最大K=20 support加20个query及预留空间的要求。

若加入unknown集合，则还必须满足：

\[
Y_{unknown}\cap(Y_{old}\cup Y_{new})=\varnothing
\]

同一个TX不能同时用于seen-new enrollment和unknown rejection。

### 7.5support/query划分

每个target receiver、seed和类别先构造一个最大20-shot的预登记support池，然后在该池之后选择独立query。物理sample ID在实验启动前写入manifest并绑定SHA256，不能根据query结果重新抽样。

正式K关系为：

```text
K1⊂K5⊂K10⊂K20
```

历史125实验中的K2保留为少样本曲线辅助诊断，但当前增量学习和遗忘主锚点为`K∈{1,5,10,20}`。

划分要求：

- K1复用对应K10的第1个物理support；
- K5复用对应K10的前5个物理support；
- K20使用同一预登记20-shot support池；
- 同一个receiver和seed下，所有K使用完全相同query；
- target-old support与target-old query零重叠；
- target-new support与target-new query零重叠；
- old/new类别集合互斥；
- support增强View不增加物理K值。

每类每个LEO场景固定使用20个query。Stage2-C单行在三个场景下的预测数量为：

\[
N_{query}=3\times20\times(6+N_{new})
\]

因此：

|新类规模|单行query预测数|
|---:|---:|
|5|660|
|10|960|
|20|1,560|

### 7.6Stage2-A规划

Stage2-A不使用任何target标签或support，直接评估封存ADV3B02及允许的冻结推理头：

```text
support=empty
query=target-old query+
      target-new not-yet-enrolled reference
```

建议矩阵：

```text
5 receiver×5 confirmation seed=25行/基线
```

该25行是旧类主基线。若未注册新类参考也按5/10/20类别规模分别固化展示，则形成`25×3=75`行参考观察表；这些行只用于分析non-old score/margin，仍不得报告`seen_new_acc`或新类身份识别准确率。

每行内部评估三个LEO_weak场景。主要输出：

- direct ADV3B02 old accuracy；
- 逐receiver和逐旧类准确率；
- target-new作为未注册non-old参考时的score/margin分布；
- 1-view推理时延和backbone forward；
- 不报告`seen_new_acc`。

Stage2-A为后续K1增益、适应收益和遗忘率提供共同前置参考。

### 7.7Stage2-B规划

Stage2-B只使用旧类target support：

```text
support:
  K shots per old TX from Y_old

query:
  held-out target-old query
  optional target-new not-yet-enrolled reference
```

第一步是K10候选筛选：

```text
5 receiver×5 confirmation seed×K10
=25行/候选
```

候选必须使用统一参数，不能为receiver或seed单独调节。主要比较：

```text
direct ADV3B02
identity-only单qKNN
ProtoNet CDA
DADDA-SDA
MRIOR-SDA
轻量候选
```

K10候选锁定后，扩展正式主K：

```text
5 receiver×5 seed×4 K
=100行/候选
```

若需要与历史125曲线完全对齐，再加入K2：

```text
5 receiver×5 seed×5 K
=125行/候选
```

Stage2-B必须报告：

- `old_acc`；
- `min_old_class_acc`；
- `old_acc_delta_pp`；
- 逐receiver、逐类和receiver×class结果；
- rescue/harm/net gain；
- trust gate触发和identity回退率；
- 训练与推理资源。

Stage2-B不能使用target-new support，也不能报告seen-new identity accuracy。

### 7.8Stage2-C规划

Stage2-C在同一个target receiver域中同时使用旧类和新类support：

```text
support:
  K shots per old TX from Y_old
  K shots per seen-new TX from Y_new

query:
  held-out target-old query
  held-out seen-new query
```

先执行K10确认矩阵：

```text
5 receiver×5 seed×3 new-class sizes
=75行/候选
```

K10通过后扩展全部正式K：

```text
5 receiver×5 seed×4 K×3 new-class sizes
=300行/候选
```

按每类每场景20个query计算，K10三种新类规模合计产生79,500条预测/候选；完整4K矩阵产生318,000条预测/候选。

Stage2-C每一行必须在同一个prediction artifact中同时输出：

```text
old_acc_before_increment
old_acc_after_increment
old_adaptation_gain
average_forgetting
min_old_class_acc
seen_new_acc
H_old_new
old_to_new_confusion
new_to_old_confusion
```

调和均值定义为：

\[
H_{old,new}=
\frac{2\cdot old\_acc\cdot seen\_new\_acc}
{old\_acc+seen\_new\_acc}
\]

Stage2-C必须重新运行matched MRIOR。旧的Stage2-B MRIOR结果只有旧类，不能代替新类注册后的性能和遗忘基线。

### 7.9LEO_weak-only输入协议

所有Stage2-A/B/C样本必须在Phase2启动前完成LEO信道叠加。正式场景固定为：

```text
leo_clear_weak
leo_low_elev_weak
leo_rain_weak
```

离线打包流程：

1.在Phase1/offline边界读取ManySig或ManyTx原始样本；

2.按预登记scenario和satellite seed叠加LEO_weak信道；

3.生成support/query样本ID、overlay provenance和hash；

4.把LEO IQ、注册support、类别表和opaque query ID写入predictor package；

5.把query truth和old/new角色写入物理隔离的truth sidecar；

6.生成成员allowlist、package SHA256和runtime seal；

7.通过pre-open审计后才允许Phase2 predictor读取payload。

每个launchable row必须声明：

```text
phase2_sample_view_policy=leo_weak_only_no_clean_access
clean_sample_access=false
clean_derived_signal_access=false
phase2_clean_dataset_reachable=false
phase2_clean_cache_reachable=false
phase2_clean_control_flow_reachable=false
phase2_pretrained_artifact_policy=sealed_phase1_checkpoint_only
```

只要clean数据、clean cache、clean prototype、clean logit、clean teacher或clean控制信号可达，该行必须阻断。历史clean-access结果只能封存为`PROTOCOL_INVALID_FOR_PHASE2`。

### 7.10逐样本预测与评分协议

正式query必须独立面对全部当前已注册旧类和新类：

```text
phase2_query_decision_policy=per_sample_all_registered_classes
phase2_query_role_oracle_access=false
phase2_query_true_batch_class_count_access=false
phase2_query_class_quota_access=false
phase2_query_batch_global_assignment=false
```

禁止使用：

- query真实old/new/unknown角色；
- 当前query batch的真实类别数量；
- 每类query配额；
- query排序或标签分块；
- Hungarian、optimal transport或批量重排；
- dense query-query图或标签传播；
- query结果选择adapter、epoch、阈值或View门限。

predictor只能输出`query_sample_id+prediction+score`。prediction artifact完成并绑定SHA256后，独立scorer才连接truth sidecar计算指标。scorer结果不能返回predictor触发重算或候选选择。

### 7.11多View规划

LEO场景和TTA View是两个不同层级。`leo_clear_weak/low_elev_weak/rain_weak`描述物理信道场景；1/3/5-view描述同一物理观测之后的接收侧推理次数。

support侧允许每个物理support最多3个预注册LEO_weak增强View，用于一次性enrollment。增强View不计入K。

query侧采用逐样本自适应策略：

1.默认执行1个base view；

2.只依据当前query的margin、entropy或允许的View分歧追加3-view；

3.仍不稳定时才追加完整5-view；

4.门限只能由source validation或注册support确定；

5.不能依据query真实角色、类别比例或标签调整View。

正式输出必须包含：

```text
mean_backbone_forwards
p95_backbone_forwards
view1_trigger_rate
view3_trigger_rate
view5_trigger_rate
FFT extraction count
相对固定5-view的性能差
```

如果所有query仍固定执行5次backbone前向，则不能声明完成多View压缩。

### 7.12候选开发与确认集隔离

候选结构、更新层、学习率、epoch、qKNN参数和View门限只能使用：

- source validation；
- 注册support的leave-one-sample-out或leave-one-view-out稳定性；
- 预登记的K10 development矩阵。

development seed必须在启动前登记，并与正式confirmation seed隔离。当前正式confirmation seed为：

```text
713101,713102,713103,713104,713105
```

K10锁定candidate后，K1、K5、K20和5/10/20新类确认矩阵只能评分，不能继续调参。若确认结果失败，应返回机制级修改并启动新的独立确认，而不能在相同query上逐项搜索。

### 7.13性能与遗忘门槛

K10绝对性能目标：

|指标|门槛|
|---|---:|
|旧类总体`old_acc`|≥92%|
|最低旧类`min_old_class_acc`|≥88%|
|5个新类`seen_new_acc`|≥92%|
|10个新类`seen_new_acc`|≥90%|
|20个新类`seen_new_acc`|≥86%|

K5必须复用K10前5个support，其`old_acc`、最低类、`seen_new_acc`和`H_old_new`相对K10下降均不得超过3个百分点。

K1必须：

- 复用K10第1个support；
- 总体及每个receiver的`old_adaptation_gain>=0`；
- 相对直接ADV3B02总体至少提高2个百分点；
- matched row 95%CI下界大于0；
- 每个receiver不低于直接ADV3B02。

分K遗忘定义：

\[
average\_forgetting(K)=
old\_acc\_before\_increment(K)-
old\_acc\_after\_increment(K)
\]

还需报告：

```text
worst_K_forgetting
mean_positive_forgetting
```

K5/K10/K20的遗忘不得高于对应identity-only单qKNN基线。候选在每个K上的Stage2-B old accuracy以及Stage2-C old/new/H配对均值还应至少高于matched MRIOR 2个百分点，paired 95%CI下界大于0，逐receiver不得低于MRIOR。

### 7.14星上资源规划

星上正式候选统一采用以下资源门槛。默认冻结ADV3B02 backbone，仅更新prototype、head、归一化仿射或小adapter；若确有必要直接更新ADV3B02原层，只允许预登记的`sparse key-layer delta`白名单，并执行更短的support-only校准。

|资源项|统一星上正式门槛|
|---|---:|
|adapter可训练参数或更新原参数|≤50,000|
|冻结backbone adapter适配|≤20epoch|
|更新ADV3B02原层|≤5epoch且≤50个optimizer step|
|optimizer|优先SGD无momentum|
|持久状态|FP16差分与head合计≤256KB|
|query View|默认1-view，自适应追加3/5-view|
|最大backbone forward|≤5|
|query图|禁止|

任何超过上述参数、epoch、optimizer step、持久状态或forward限制的实验，只能作为非部署资源诊断，不得作为星上正式候选。无clean、无Oracle、无配额、无query拟合和逐样本决策边界在所有实验中均不可放宽。

相对matched MRIOR，轻量候选还应满足：

|资源项|相对上限|
|---|---:|
|可训练参数|≤MRIOR的5%|
|optimizer step|≤MRIOR的10%|
|平均适配时延|≤MRIOR的25%|
|峰值显存|≤MRIOR的50%|
|持久状态|≤256KB|

### 7.15实验启动顺序

建议按以下顺序推进：

|阶段|矩阵|目的|晋升条件|
|---|---:|---|---|
|P0数据审计|5 receiver×全部old/new集合|检查覆盖、互斥、样本数、LEO cache与hash|全部协议字段PASS|
|P1 Stage2-A|25行/基线|建立direct ADV3B02目标域底线|artifact完整|
|P2 Stage2-B K10筛选|25行/候选|锁定轻量适应结构与统一参数|旧类提升、弱类与资源可接受|
|P3 Stage2-B全K|100行/候选|检查K1/K5/K10/K20稳健性|K1正收益、K5下降≤3pp|
|P4 Stage2-C K10|75行/候选|检查5/10/20新类注册|达到old/new/H绝对门槛|
|P5 Stage2-C全K|300行/候选|检查分K遗忘和新类规模扩展|遗忘与matched MRIOR门禁通过|
|P6 View/资源消融|同一锁定row|比较1/3/5-view及统一资源门槛内的机制配置|形成性能—资源Pareto前沿|

K2可作为额外25行历史曲线对齐项，但不参与当前K1/K5/K10/K20主门禁。

### 7.16正式输出artifact

每个实验行至少保存：

```text
resolved_config.json
split_manifest.json
support/query sample ID与overlap audit
LEO overlay provenance
runtime access audit
loss_trace.csv或closed-form求解诊断
immutable prediction artifact及SHA256
score_table.csv
metrics.json
resource_metrics.json
```

分组统计至少包含：

- overall；
- receiver；
- transmitter；
- receiver×transmitter；
- receiver×transmitter×day；
- old/new角色；
- scenario；
- K；
- 新类规模。

资源字段至少包含：

```text
trainable_parameters
updated_original_parameters
optimizer_steps
adaptation_latency_sec
peak_memory_mib
persistent_state_bytes
mean/p95 backbone forwards
FFT count
deployment_added_MAC
```

只有overall accuracy、缺少逐类/逐receiver结果或缺少prediction/scorer隔离证据的实验，不得进入正式候选排名。

## 八、下周工作计划

1.在正式2026-07-15封存矩阵的25个K=10任务上，运行少参数关键层适配候选。优先更新ADV3B02的identity gate、fusion/joint projection或少量归一化仿射参数，可训练参数控制在50,000以内，正式`sparse key-layer delta`候选控制在5epoch、50个optimizer step以内。

2.加入support-only trust gate。根据support prototype稳定性、leave-one-view-out一致性、source-target对角统计差异和适配前后support margin变化，决定更新强度或回退identity模型，不读取query预测结果。

3.实现逐样本自适应多View。query默认1-view；只有低置信度、低margin或视图分歧风险较高时触发3-view或5-view。记录平均和P95 backbone forward，确保额外View的性能收益能够覆盖计算开销。

4.K=10候选锁定后，扩展K=1、K=5和K=20。重点检查K=1相对直接ADV3B02是否至少提高2个百分点、K=1是否无负迁移、K=5相对K=10下降是否不超过3个百分点，以及不同K下的旧类遗忘。

5.构建Stage2-C matched矩阵，使用嵌套真实新类集合`Y_new^5⊂Y_new^10⊂Y_new^20`。同一行同时报告注册前旧类、注册后旧类、新类准确率、`H_old_new`和遗忘率，并重新运行matched MRIOR作为完整类增量对照。

6.补齐资源schema。所有候选和对比方法统一记录可训练参数、optimizer step、端到端时延、峰值显存、持久状态、平均/P95 forward和部署新增MAC，形成性能—资源Pareto表。

## 九、下周目标

下周优先目标不是直接完成全部5/10/20新类矩阵，而是先确定一条在K=10旧类适应上同时满足以下条件的轻量路线：

- old accuracy显著高于直接ADV3B02；
- 尽量接近或超过MRIOR；
- 不损害`7-14`等高基线receiver；
- 明显改善`14-7`、`14-10`和`20-19`弱类；
- 可训练参数不超过50,000；
- 若更新ADV3B02原参数，optimizer step不超过50；
- 平均适配时延不超过matched MRIOR的25%；
- query保持逐样本、无Oracle、无类别配额和无dense query图。

完成该路线锁定后，再进入完整125行和Stage2-C新类注册确认。

## 十、声明边界

本周结果属于WiSig/ManySig地面数据上的LEO_weak部署压力代理实验，不代表真实卫星在轨验证。三种方法均为共享ADV3B02的CVS extension，不是原论文架构的严格复现。本轮只完成Stage2-B旧类适应，不能声明新类注册成功、完整Phase2成功或星上部署成功。

证据来源：

- `E:\type10-7\项目.md`
- `E:\type10-7\github_publish\CVS-RFFI-repo\项目实验.md`
- `E:\type10-7\automation_reports\CV-SincNet\adv3b02_three_da_leoweakonly_20260715_v1\report.md`
- `E:\type10-7\automation_reports\CV-SincNet\adv3b02_three_da_leoweakonly_20260715_v1\summary\`
